
# PII: raw applicant exports must never live in the repo (CR-01)
source-b-applicants.csv
*_source_b_load.sql

# Env / secrets (never commit the service-role key)
.env*
!.env.example

# Next.js scaffold
/node_modules
/.next/
# Build de verificación (NEXT_DIST_DIR), para no pisar el `next dev` que esté vivo
/.next-verify/
/out/
/build
/coverage
.DS_Store
*.pem
npm-debug.log*
*.tsbuildinfo
next-env.d.ts
.vercel
.stitch/

testsprite_tests/tmp/config.json
supabase/.temp/
