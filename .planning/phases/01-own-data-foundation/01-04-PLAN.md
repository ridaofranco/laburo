---
phase: 01-own-data-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - "supabase-app: migration 0005_staging_sheet"
  - "supabase-app: staff_profiles Source-B data import via execute_sql"
autonomous: false
requirements: [DATA-02]
must_haves:
  truths:
    - "Franco's Google Sheet export (~146 applicants) is loaded into a staging table in the app project"
    - "The ~146 Sheet applicants are imported into staff_profiles with organization_id stamped and source='google_sheet'"
    - "No Sheet applicant already present from Source A (matched by email) is imported twice"
    - "Final staff_profiles count equals 7 (Source A) + N_sheet post-dedup, with zero NULL organization_id and zero duplicate emails"
    - "Any CV links present in the Sheet are stored in cv_url/portfolio_url without assuming Storage objects exist"
  artifacts:
    - path: "supabase-app migration 0005_staging_sheet"
      provides: "staging_sheet table for the raw CSV load before normalized INSERT...SELECT"
      contains: "staging_sheet"
  key_links:
    - from: "staging_sheet"
      to: "staff_profiles"
      via: "INSERT ... SELECT with normalized columns, dedup by email against Source A"
      pattern: "source = 'google_sheet'"
---

<objective>
Complete DATA-02 by importing Source B — the ~146 legacy applicants that live in Franco's Google Sheet (Google-Form era), which do NOT exist in HITO's DB (research corrected the "146 in HITO" assumption). This is the ONLY part of Phase 1 gated on Franco: he must export the Sheet CSV. Everything upstream (schema, RPCs, cutover, Source-A backfill) already shipped without him.

Purpose: DATA-02 (Source B) — the existing 146+ applicants copied into the app DB, deduped against Source A, verified no loss.
Output: migration `0005_staging_sheet`, the normalized import, and final verification counts.

Slice (data-layer, SQL-provable): after this plan, `SELECT source, count(*) FROM staff_profiles GROUP BY source` shows the web_somosder + google_sheet split and the full pool is queryable in SQL — the data foundation is complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-own-data-foundation/01-RESEARCH.md
@.planning/phases/01-own-data-foundation/01-CONTEXT.md
@.planning/phases/01-own-data-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Obtain the Google Sheet export from Franco</name>
  <what-built>Schema, RPCs, form cutover and the 7 Source-A applicants are already live in the app DB. The only missing input is the legacy applicant pool, which lives in a Google Sheet that only Franco can access/export.</what-built>
  <how-to-verify>
    1. Ask Franco to export the "Trabajá con nosotros" applicant Google Sheet as CSV (or share the Drive file) and place it at a known path, e.g. `/Users/fridao/Proyectos/SOMOS DER/staff-app/.planning/phases/01-own-data-foundation/source-b-applicants.csv`.
    2. Ask Franco to confirm whether the Sheet has a CV column (Drive links) or none (research A2: CVs are likely Drive links or absent — NOT Supabase objects).
    3. Confirm the CSV opens and has a header row with recognizable applicant fields (name, email, phone, oficios, etc.).
  </how-to-verify>
  <resume-signal>Type "csv ready" once the export file is in place and the CV-column question is answered, or "no sheet" if the pool cannot be produced (then DATA-02 completes with Source A only and this is recorded as a deferred gap).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Stage, normalize, dedup and import the ~146 Sheet applicants</name>
  <files>supabase-app: migration 0005_staging_sheet; staff_profiles Source-B import via execute_sql</files>
  <read_first>
    - .planning/phases/01-own-data-foundation/01-RESEARCH.md § "Backfill: Two Sources" (Source B block) — staging table → INSERT...SELECT, normalize oficios/país/dates/booleans, dedup by email against Source A, source='google_sheet', CV-link handling
    - .planning/phases/01-own-data-foundation/01-RESEARCH.md § "Verification queries" — the exact no-loss assertions
    - .planning/phases/01-own-data-foundation/01-03-SUMMARY.md — the fixed org UUID and the current staff_profiles count (7)
    - The CSV placed by Franco in Task 1 — inspect its actual header/columns before mapping
  </read_first>
  <action>
    `apply_migration` name `0005_staging_sheet` creating a `staging_sheet` table whose columns match the CSV's actual header (all text, loose). Load the CSV rows into `staging_sheet` (MCP CSV load or generated INSERTs). Then `INSERT INTO staff_profiles SELECT <mapped/normalized columns>` from staging: map name→nombre/apellido, email, telefono, coerce Spanish/boolean strings ("Sí"/"No"→bool) for disponibilidad_*/movilidad_propia, split/normalize oficios into the `text[]` vocab the form uses, coerce dates to YYYY-MM-DD, set `organization_id` = fixed org UUID, `source='google_sheet'`, `estado='pendiente'`. Store any Sheet CV/Drive link in `cv_url` or `portfolio_url` as-is; do NOT assume a staff-cvs Storage object exists for these (research A2). Dedup by `email` AGAINST the 7 Source-A rows and within the Sheet itself (skip rows whose email already exists). Drop the `staging_sheet` table after a clean import. Run the § Verification queries and record before/after in the SUMMARY.
  </action>
  <verify>
    <automated>MCP execute_sql on app: SELECT count(*) FROM staff_profiles equals 7 + N_sheet_post_dedup (record N); SELECT count(*) FILTER (WHERE organization_id IS NULL) returns 0; SELECT email, count(*) FROM staff_profiles GROUP BY email HAVING count(*)>1 returns 0 rows; SELECT source, count(*) FROM staff_profiles GROUP BY source shows both web_somosder (7) and google_sheet (N); staging_sheet no longer exists (SELECT to_regclass('public.staging_sheet') IS NULL).</automated>
  </verify>
  <acceptance_criteria>
    - Final `SELECT count(*) FROM staff_profiles` = 7 + N_sheet (post-dedup), N recorded in SUMMARY
    - Zero rows with NULL organization_id; zero duplicate emails across the full pool
    - `SELECT source, count(*)` shows the web_somosder / google_sheet split
    - Sheet CV links (if any) stored in cv_url/portfolio_url; no fake Storage objects assumed
    - `staging_sheet` dropped after import (`to_regclass('public.staging_sheet')` IS NULL)
  </acceptance_criteria>
  <done>The ~146 legacy applicants imported, deduped against Source A, verified no loss; the app data pool is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external CSV → app DB | Untrusted/unclean legacy data crosses into staff_profiles via a staging table |
| legacy PII | ~146 applicants' names/emails/phones/CV links are PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | Integrity / data loss | dedup + normalization | mitigate | Dedup by email against Source A and within the Sheet; verification queries assert 0 dup emails, 0 NULL org; before/after counts recorded |
| T-01-15 | Information Disclosure | legacy applicant PII + CV links | mitigate | Imported rows inherit staff_profiles RLS (member SELECT only); CV links stored as data, not exposed; no public read path exists in Phase 1 |
| T-01-16 | Tampering | staging table left behind | mitigate | `staging_sheet` dropped after import so no un-RLS'd raw copy of PII persists |
| T-01-SC | Tampering | package installs | accept | No package installs; CSV loaded via MCP execute_sql |
</threat_model>

<verification>
- staging_sheet loaded then dropped.
- Final counts: 7 + N_sheet, zero NULL org, zero dup emails, web_somosder/google_sheet split visible.
- If Franco returns "no sheet", DATA-02 completes with Source A (7) and the Source-B gap is recorded for a later close.
</verification>

<success_criteria>
- The existing 146+ applicants are copied into the app DB, deduped and verified no loss (DATA-02 complete).
- No HITO-integration work performed (Sheet is external; HITO untouched).
</success_criteria>

<output>
Create `.planning/phases/01-own-data-foundation/01-04-SUMMARY.md` when done — record N_sheet imported, dedup skips, final pool count, and the CV-link handling decision from Franco's answer.
</output>
