---
phase: 01-own-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "supabase-app: project create (org wsvqlrjmizvivgrgnfpw)"
  - "supabase-app: migration 0001_organizations_members_helpers"
  - "supabase-app: migration 0002_core_schema_staff_gigs_crew_offers"
autonomous: true
requirements: [DATA-01]
must_haves:
  truths:
    - "A new Supabase project exists in org wsvqlrjmizvivgrgnfpw at $0 cost"
    - "organizations, members, staff_profiles, gigs, crew, offers all exist with ROW LEVEL SECURITY enabled"
    - "No anon role can SELECT any of those tables directly (only member policies grant read)"
    - "is_org_member/is_org_writer helpers exist with a pinned search_path (no function_search_path_mutable finding)"
    - "One SOMOS DER organizations row exists with a fixed hardcoded UUID"
    - "gigs has a nullable hito_event_id column present but unused (D-01 future-link, zero cost today)"
    - "get_advisors(security) reports zero function_search_path_mutable and zero rls_disabled/rls_enabled_no_policy findings"
  artifacts:
    - path: "supabase-app migration 0001_organizations_members_helpers"
      provides: "organizations + members tables, is_org_member/is_org_writer, seeded SOMOS DER org"
      contains: "is_org_member"
    - path: "supabase-app migration 0002_core_schema_staff_gigs_crew_offers"
      provides: "staff_profiles (superset of live 31 cols + nullable organization_id), gigs, crew, offers with RLS"
      contains: "token_hash text NOT NULL UNIQUE"
  key_links:
    - from: "crew"
      to: "gigs + staff_profiles"
      via: "UNIQUE (gig_id, staff_profile_id) for accept_offer idempotency"
      pattern: "UNIQUE \\(gig_id, staff_profile_id\\)"
    - from: "offers RLS policies"
      to: "is_org_member/is_org_writer"
      via: "USING (is_org_member(organization_id))"
      pattern: "is_org_(member|writer)\\(organization_id\\)"
---

<objective>
Create the app's own Supabase project and its complete org-scoped, RLS-enabled schema — `organizations`, `members`, `is_org_member`/`is_org_writer` helpers, `staff_profiles` (a superset of HITO's live 31 columns so the repointed form inserts unchanged), `gigs` (with nullable `hito_event_id`), `crew`, and `offers`. This is the data foundation every later plan builds on.

Purpose: DATA-01 — the app owns its data from day 1, multi-tenant and RLS-hardened, with no anon direct table read. Proven entirely in SQL via Supabase MCP; no app code, no UI.
Output: A new Supabase project in org `wsvqlrjmizvivgrgnfpw` ($0) with migrations `0001` (org + helpers) and `0002` (core schema), a seeded SOMOS DER org row, and a clean `get_advisors(security)` run.

Slice (data-layer, SQL-provable): after this plan, `list_tables` shows all six tables RLS-enabled and `get_advisors` is clean — the schema is real and inspectable in SQL before any RPC or form work touches it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-own-data-foundation/01-CONTEXT.md
@.planning/phases/01-own-data-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app Supabase project + org/membership tables + RLS helpers</name>
  <files>supabase-app: project create (org wsvqlrjmizvivgrgnfpw); migration 0001_organizations_members_helpers</files>
  <read_first>
    - .planning/phases/01-own-data-foundation/01-RESEARCH.md § "Target Schema DDL Shape" (Org + membership + helpers block, lines ~109-120) and § "Code Examples" (Org helpers verbatim from HITO 00001/00052) — copy these helper bodies exactly
    - .planning/phases/01-own-data-foundation/01-RESEARCH.md § "get_advisors Expectations" — the exact findings to resolve
    - .planning/phases/01-own-data-foundation/01-RESEARCH.md § "Assumptions Log" A3 — verify pgcrypto lives in the extensions schema before relying on it
  </read_first>
  <action>
    Via Supabase MCP `create_project` (confirm_cost first — verified $0) create a new project in org `wsvqlrjmizvivgrgnfpw`; capture its ref, URL, publishable (anon) key and service key for later plans. Immediately verify pgcrypto is reachable in the `extensions` schema by executing `SELECT extensions.gen_random_bytes(1)` (per A3). Then `apply_migration` name `0001_organizations_members_helpers` creating: `organizations` (id uuid pk default gen_random_uuid(), name text, created_at timestamptz default now()); `members` (organization_id uuid references organizations, user_id uuid, role text default 'writer', created_at, unique(organization_id,user_id)); and the two helper functions `is_org_member(org_id uuid)` and `is_org_writer(org_id uuid)` copied VERBATIM from HITO `00001`/`00052` as `LANGUAGE sql SECURITY DEFINER STABLE` — but ADD `SET search_path = ''` and schema-qualify `public.members`/`auth.uid()` so no function_search_path_mutable advisor fires (per D-01 this schema stays HITO-free; helpers are the only HITO copy). Enable RLS on `organizations` and `members` with member-scoped SELECT policies. Seed exactly one `organizations` row for SOMOS DER with a FIXED hardcoded UUID (record that UUID in the SUMMARY — every later plan/backfill stamps it). Leave `members` empty (auth wired in Phase 2). Run `get_advisors(security)` and resolve any function_search_path_mutable / rls findings to zero.
  </action>
  <verify>
    <automated>MCP execute_sql: SELECT count(*) FROM organizations returns 1; SELECT proname, prosecdef FROM pg_proc WHERE proname IN ('is_org_member','is_org_writer') returns 2 rows with prosecdef=true; get_advisors(security) returns no function_search_path_mutable and no rls_disabled_in_public/rls_enabled_no_policy entries.</automated>
  </verify>
  <acceptance_criteria>
    - `create_project` returned a project in org `wsvqlrjmizvivgrgnfpw`; its ref/URL/anon key/service key are recorded in the SUMMARY
    - `SELECT extensions.gen_random_bytes(1)` succeeds (pgcrypto confirmed in `extensions` schema)
    - `SELECT count(*) FROM organizations` = 1, and the fixed SOMOS DER org UUID is written verbatim in the SUMMARY
    - `is_org_member` and `is_org_writer` exist, are SECURITY DEFINER, and each carries `SET search_path`
    - `get_advisors(security)` shows zero function_search_path_mutable and zero RLS findings for the objects created in this task
  </acceptance_criteria>
  <done>New project created at $0, org/members/helpers migrated, one seeded org with a recorded fixed UUID, advisor clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create core schema — staff_profiles superset, gigs, crew, offers (RLS from day 1)</name>
  <files>supabase-app: migration 0002_core_schema_staff_gigs_crew_offers</files>
  <read_first>
    - .planning/phases/01-own-data-foundation/01-RESEARCH.md § "Ground Truth: Verified Live-DB Facts" — the exact ordinal column list of HITO's live staff_profiles (31 columns); reproduce every one with identical name/type/default
    - .planning/phases/01-own-data-foundation/01-RESEARCH.md § "Target Schema DDL Shape" — the gigs, crew, offers DDL blocks (copy shapes exactly, including the offers status CHECK enum and token_hash column)
    - .planning/phases/01-own-data-foundation/01-CONTEXT.md D-01 — gigs.hito_event_id nullable is the ONLY HITO concession; no bridge, no HITO reads
  </read_first>
  <action>
    `apply_migration` name `0002_core_schema_staff_gigs_crew_offers`. Create `staff_profiles` reproducing EVERY live HITO column with identical name/type/default from the Ground Truth list (id, created_at, estado default 'pendiente', nombre, fecha_nacimiento, documento, email, telefono, pais_residencia, provincia, ciudad, donde_trabajar text[] default '{}', situacion_legal, oficios text[] default '{}', oficios_otro, experiencia bool, anios_experiencia, experiencia_detalle, disponibilidad_finde/viajar/movilidad_propia bool default false, cv_url, portfolio_url, linkedin_url, motivacion, source default 'web_somosder', rating, eventos_trabajados int default 0, notas_internas, disponibilidad_aviso, apellido) PLUS `organization_id uuid REFERENCES organizations(id)` NULLABLE (pitfall #3 — never NOT NULL on a live-insert table; the anon INSERT policy/trigger arrive in plan 01-03). Keep `oficios` as `text[]` tags (A4, sufficient for SRCH-01). Create `gigs` per the DDL block including `hito_event_id uuid` NULLABLE with NO FK (D-01 future-link, unused this phase), `starts_at`/`ends_at timestamptz` + `timezone` default 'America/Argentina/Buenos_Aires', `status` default 'draft'. Create `crew` with `UNIQUE (gig_id, staff_profile_id)` (idempotency anchor for accept_offer). Create `offers` with `token_hash text NOT NULL UNIQUE`, `status text NOT NULL DEFAULT 'sent' CHECK (status IN ('sent','viewed','accepted','declined','expired'))`, `expires_at timestamptz NOT NULL DEFAULT (now() + interval '7 days')`, plus sent_at/viewed_at/responded_at. ENABLE ROW LEVEL SECURITY on all four; add member SELECT policy `USING (is_org_member(organization_id))` and writer ALL policy `USING/WITH CHECK (is_org_writer(organization_id))` on gigs/crew/offers, and a member SELECT policy on staff_profiles. Do NOT add any anon policy here (that is 01-03). Run `get_advisors(security)`.
  </action>
  <verify>
    <automated>MCP list_tables shows staff_profiles, gigs, crew, offers all with rls_enabled=true. execute_sql: SELECT count(*) FROM information_schema.columns WHERE table_name='staff_profiles' returns 32 (31 live cols + organization_id). SELECT 1 FROM pg_constraint WHERE conname LIKE '%gig_id_staff_profile_id%' returns the crew UNIQUE. Attempting an anon SELECT on staff_profiles returns zero rows / permission (no anon read policy exists). get_advisors(security) shows no rls_disabled/rls_enabled_no_policy findings.</automated>
  </verify>
  <acceptance_criteria>
    - `staff_profiles` has all 31 live columns with matching names/types/defaults PLUS a nullable `organization_id`
    - `gigs` has a nullable `hito_event_id` with no foreign key; `crew` has `UNIQUE (gig_id, staff_profile_id)`
    - `offers.token_hash` is NOT NULL UNIQUE and `offers.status` has the 5-value CHECK enum with default 'sent'
    - All four tables report `rls_enabled=true` in `list_tables`; no anon SELECT policy exists yet
    - `get_advisors(security)` shows zero RLS findings on the four new tables
  </acceptance_criteria>
  <done>staff_profiles superset + gigs + crew + offers created, RLS on, anon has no direct read, advisor clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon role → app tables | The published anon key (used later by the form) must never read/write these tables directly except through explicitly granted paths |
| cross-org read | Any org's member must not read another org's rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | staff_profiles/gigs/crew/offers | mitigate | RLS enabled on all tables from creation; SELECT only via `is_org_member(organization_id)`; no anon SELECT policy exists in this plan |
| T-01-02 | Elevation of Privilege | is_org_member/is_org_writer | mitigate | Helpers are SECURITY DEFINER with pinned `SET search_path` and schema-qualified refs; get_advisors must show zero function_search_path_mutable |
| T-01-03 | Tampering | cross-org writes | mitigate | Writer ALL policies gate on `is_org_writer(organization_id)`; members table seeded empty until Phase 2 auth |
| T-01-SC | Tampering | package installs | accept | No npm/pip/cargo installs this phase — pure SQL via Supabase MCP (per RESEARCH § Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `list_tables`: organizations, members, staff_profiles, gigs, crew, offers all present, all RLS-enabled.
- `get_advisors(security)` clean: no function_search_path_mutable, no rls_disabled_in_public, no rls_enabled_no_policy. `extension_in_public` (pgcrypto in extensions) is acceptable/informational.
- Fixed SOMOS DER org UUID recorded in SUMMARY for downstream plans.
</verification>

<success_criteria>
- New Supabase project exists in org `wsvqlrjmizvivgrgnfpw` at $0.
- Six tables exist, org-scoped, RLS-enabled; no anon direct read.
- `gigs.hito_event_id` nullable present (D-01), no bridge logic anywhere.
- `get_advisors(security)` reports zero high findings.
</success_criteria>

<output>
Create `.planning/phases/01-own-data-foundation/01-01-SUMMARY.md` when done — MUST record the new project ref/URL, the anon (publishable) key, the service key, and the fixed SOMOS DER org UUID (plans 01-02/01-03/01-04 depend on these values).
</output>
