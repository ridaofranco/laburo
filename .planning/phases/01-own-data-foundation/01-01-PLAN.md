---
phase: 01-own-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "supabase-hito (staff_app schema): CREATE SCHEMA staff_app"
  - "supabase-hito (staff_app schema): migration staff_app_0001_schema_orgs"
  - "supabase-hito (staff_app schema): migration staff_app_0002_core_tables"
autonomous: true
requirements: [DATA-01]
must_haves:
  truths:
    - "A dedicated schema `staff_app` exists inside HITO's Supabase project luillpzfqzbpoqkgvjuw (D-03 — no new project; free-tier 2-active-project limit)"
    - "staff_app.organizations, staff_app.members, staff_app.staff_profiles, staff_app.gigs, staff_app.crew, staff_app.offers all exist with ROW LEVEL SECURITY enabled"
    - "anon is REVOKED/denied on all staff_app tables — no anon role can SELECT any of them directly (only member policies grant read)"
    - "staff_app.is_org_member/staff_app.is_org_writer helpers exist (own names in the staff_app schema, no collision with HITO's public.is_org_member) with a pinned search_path (no function_search_path_mutable finding)"
    - "One staff_app.organizations row exists for SOMOS DER with a fixed hardcoded UUID"
    - "staff_app.gigs has a nullable hito_event_id column present but unused (D-01 future-link, zero cost today)"
    - "Zero new objects are created in HITO's public schema (all app objects live under staff_app)"
    - "get_advisors(security) reports no NEW function_search_path_mutable and no NEW rls_disabled/rls_enabled_no_policy findings referencing staff_app objects (HITO's pre-existing findings are out of scope — capture a baseline first)"
  artifacts:
    - path: "supabase-hito migration staff_app_0001_schema_orgs"
      provides: "staff_app schema + staff_app.organizations/members tables, staff_app.is_org_member/is_org_writer, seeded SOMOS DER org"
      contains: "is_org_member"
    - path: "supabase-hito migration staff_app_0002_core_tables"
      provides: "staff_app.staff_profiles (superset of live 31 cols + nullable organization_id), staff_app.gigs, staff_app.crew, staff_app.offers with RLS"
      contains: "token_hash text NOT NULL UNIQUE"
  key_links:
    - from: "staff_app.crew"
      to: "staff_app.gigs + staff_app.staff_profiles"
      via: "UNIQUE (gig_id, staff_profile_id) for accept_offer idempotency"
      pattern: "UNIQUE \\(gig_id, staff_profile_id\\)"
    - from: "staff_app.offers RLS policies"
      to: "staff_app.is_org_member/is_org_writer"
      via: "USING (staff_app.is_org_member(organization_id))"
      pattern: "staff_app\\.is_org_(member|writer)\\(organization_id\\)"
---

<objective>
Create the app's own logically-independent schema `staff_app` INSIDE HITO's existing Supabase project (`luillpzfqzbpoqkgvjuw`) and its complete org-scoped, RLS-enabled table set — `staff_app.organizations`, `staff_app.members`, `staff_app.is_org_member`/`staff_app.is_org_writer` helpers, `staff_app.staff_profiles` (a superset of HITO's live 31 columns so the repointed form inserts unchanged), `staff_app.gigs` (with nullable `hito_event_id`), `staff_app.crew`, and `staff_app.offers`. This is the data foundation every later plan builds on. Per D-03 the app does NOT get a new Supabase project (free-tier 2-active-project limit; Franco refused pausing/paying, and chose HITO co-location because the app links to HITO in Phase 6). Independence is LOGICAL: own schema, own RLS, own org-scoping; ZERO writes to HITO's `public.*` tables this phase.

Purpose: DATA-01 — the app owns its data from day 1, multi-tenant and RLS-hardened, with no anon direct table read, isolated in its own schema. Proven entirely in SQL via Supabase MCP; no app code, no UI.
Output: schema `staff_app` in HITO's project with migrations `staff_app_0001_schema_orgs` (schema + org + helpers) and `staff_app_0002_core_tables` (core tables), a seeded SOMOS DER org row, no new objects in `public`, and a clean `get_advisors(security)` run (no NEW staff_app findings vs baseline).

Slice (data-layer, SQL-provable): after this plan, `information_schema` shows all six `staff_app` tables RLS-enabled and `get_advisors` has no new staff_app findings — the schema is real and inspectable in SQL before any RPC or form work touches it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-own-data-foundation/01-CONTEXT.md
@.planning/phases/01-own-data-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create staff_app schema + org/membership tables + RLS helpers (inside HITO's project)</name>
  <files>supabase-hito (staff_app schema): CREATE SCHEMA staff_app; migration staff_app_0001_schema_orgs</files>
  <read_first>
    - .planning/phases/01-own-data-foundation/01-RESEARCH.md § "Target Schema DDL Shape" (Org + membership + helpers block, lines ~109-120) and § "Code Examples" (Org helpers verbatim from HITO 00001/00052) — copy these helper bodies exactly (into the staff_app schema, referencing staff_app.members)
    - .planning/phases/01-own-data-foundation/01-RESEARCH.md § "get_advisors Expectations" — the exact findings to resolve (only NEW staff_app findings are in scope)
    - .planning/phases/01-own-data-foundation/01-RESEARCH.md § "Assumptions Log" A3 — verify pgcrypto lives in the extensions schema before relying on it
    - .planning/phases/01-own-data-foundation/01-CONTEXT.md D-03 — the app lives in a `staff_app` schema inside HITO's project luillpzfqzbpoqkgvjuw; nothing is written to HITO's public.* tables
  </read_first>
  <action>
    Target the EXISTING HITO project `luillpzfqzbpoqkgvjuw` via Supabase MCP — there is NO project creation, no confirm_cost/get_cost, no ACTIVE_HEALTHY polling, and no new keys to record (per D-03; the free-tier 2-active-project limit forbids a new project). First capture a BASELINE of pre-existing `get_advisors(security)` findings (HITO may already have advisor findings that are out of scope) — record them in the SUMMARY so later "no NEW findings" assertions are meaningful. Verify pgcrypto is reachable in the `extensions` schema by executing `SELECT extensions.gen_random_bytes(1)` (per A3). Then `apply_migration` name `staff_app_0001_schema_orgs` (prefix `staff_app_` because this shares HITO's migration history) which: `CREATE SCHEMA staff_app;` then creates `staff_app.organizations` (id uuid pk default gen_random_uuid(), name text, created_at timestamptz default now()); `staff_app.members` (organization_id uuid references staff_app.organizations, user_id uuid, role text default 'writer', created_at, unique(organization_id,user_id)); and the two helper functions `staff_app.is_org_member(org_id uuid)` and `staff_app.is_org_writer(org_id uuid)` copied from HITO `00001`/`00052` as `LANGUAGE sql SECURITY DEFINER STABLE` — but ADD `SET search_path = ''` and schema-qualify `staff_app.members`/`auth.uid()` so no function_search_path_mutable advisor fires. The helpers MUST live in the `staff_app` schema with these names so they never collide with HITO's `public.is_org_member`/`public.is_org_writer` (different schema = no collision; RLS policies below call the schema-qualified `staff_app.is_org_member`). Enable RLS on `staff_app.organizations` and `staff_app.members` with member-scoped SELECT policies, and REVOKE anon/deny direct access. Seed exactly one `staff_app.organizations` row for SOMOS DER with a FIXED hardcoded UUID (record that UUID in the SUMMARY — every later plan/backfill stamps it). Leave `staff_app.members` empty (auth wired in Phase 2). Run `get_advisors(security)` and resolve any NEW function_search_path_mutable / rls findings referencing staff_app objects to zero (ignore HITO's pre-existing baseline findings).
  </action>
  <verify>
    <automated>MCP execute_sql: SELECT count(*) FROM information_schema.schemata WHERE schema_name='staff_app' returns 1; SELECT count(*) FROM staff_app.organizations returns 1; SELECT proname, prosecdef FROM pg_proc p JOIN pg_namespace n ON p.pronamespace=n.oid WHERE n.nspname='staff_app' AND proname IN ('is_org_member','is_org_writer') returns 2 rows with prosecdef=true; get_advisors(security) returns no NEW function_search_path_mutable and no NEW rls_disabled/rls_enabled_no_policy entries referencing staff_app objects (vs the captured baseline).</automated>
  </verify>
  <acceptance_criteria>
    - No project was created; work targeted existing project `luillpzfqzbpoqkgvjuw`; a get_advisors baseline was captured and recorded in the SUMMARY
    - `SELECT extensions.gen_random_bytes(1)` succeeds (pgcrypto confirmed in `extensions` schema)
    - Schema `staff_app` exists; `SELECT count(*) FROM staff_app.organizations` = 1, and the fixed SOMOS DER org UUID is written verbatim in the SUMMARY
    - `staff_app.is_org_member` and `staff_app.is_org_writer` exist in the staff_app schema (no collision with HITO's public helpers), are SECURITY DEFINER, and each carries `SET search_path`
    - Zero new objects were created in HITO's `public` schema
    - `get_advisors(security)` shows zero NEW function_search_path_mutable and zero NEW RLS findings for the staff_app objects created in this task (baseline findings excluded)
  </acceptance_criteria>
  <done>staff_app schema created inside HITO's project, org/members/helpers migrated with own names, one seeded org with a recorded fixed UUID, no public writes, advisor clean vs baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Create core tables — staff_app.staff_profiles superset, gigs, crew, offers (RLS from day 1)</name>
  <files>supabase-hito (staff_app schema): migration staff_app_0002_core_tables</files>
  <read_first>
    - .planning/phases/01-own-data-foundation/01-RESEARCH.md § "Ground Truth: Verified Live-DB Facts" — the exact ordinal column list of HITO's live staff_profiles (31 columns); reproduce every one with identical name/type/default (into staff_app.staff_profiles)
    - .planning/phases/01-own-data-foundation/01-RESEARCH.md § "Target Schema DDL Shape" — the gigs, crew, offers DDL blocks (copy shapes exactly, including the offers status CHECK enum and token_hash column)
    - .planning/phases/01-own-data-foundation/01-CONTEXT.md D-01/D-03 — gigs.hito_event_id nullable is the ONLY HITO concession; no bridge, no HITO reads; all objects live under staff_app
  </read_first>
  <action>
    `apply_migration` name `staff_app_0002_core_tables`. Create `staff_app.staff_profiles` reproducing EVERY live HITO column with identical name/type/default from the Ground Truth list (id, created_at, estado default 'pendiente', nombre, fecha_nacimiento, documento, email, telefono, pais_residencia, provincia, ciudad, donde_trabajar text[] default '{}', situacion_legal, oficios text[] default '{}', oficios_otro, experiencia bool, anios_experiencia, experiencia_detalle, disponibilidad_finde/viajar/movilidad_propia bool default false, cv_url, portfolio_url, linkedin_url, motivacion, source default 'web_somosder', rating, eventos_trabajados int default 0, notas_internas, disponibilidad_aviso, apellido) PLUS `organization_id uuid REFERENCES staff_app.organizations(id)` NULLABLE (pitfall #3 — never NOT NULL on a live-insert table; the anon INSERT policy/trigger arrive in plan 01-03). Keep `oficios` as `text[]` tags (A4, sufficient for SRCH-01). Create `staff_app.gigs` per the DDL block including `hito_event_id uuid` NULLABLE with NO FK (D-01 future-link, unused this phase), `starts_at`/`ends_at timestamptz` + `timezone` default 'America/Argentina/Buenos_Aires', `status` default 'draft'. Create `staff_app.crew` with `UNIQUE (gig_id, staff_profile_id)` (idempotency anchor for accept_offer). Create `staff_app.offers` with `token_hash text NOT NULL UNIQUE`, `status text NOT NULL DEFAULT 'sent' CHECK (status IN ('sent','viewed','accepted','declined','expired'))`, `expires_at timestamptz NOT NULL DEFAULT (now() + interval '7 days')`, plus sent_at/viewed_at/responded_at. ENABLE ROW LEVEL SECURITY on all four; REVOKE/deny anon direct access on all four; add member SELECT policy `USING (staff_app.is_org_member(organization_id))` and writer ALL policy `USING/WITH CHECK (staff_app.is_org_writer(organization_id))` on gigs/crew/offers, and a member SELECT policy on staff_profiles. Do NOT add any anon policy here (that is 01-03). All references stay inside staff_app; nothing touches HITO's public.* tables. Run `get_advisors(security)` and check for NEW staff_app findings only.
  </action>
  <verify>
    <automated>MCP execute_sql: SELECT count(*) FROM pg_tables WHERE schemaname='staff_app' AND tablename IN ('staff_profiles','gigs','crew','offers') AND rowsecurity=true returns 4. SELECT count(*) FROM information_schema.columns WHERE table_schema='staff_app' AND table_name='staff_profiles' returns 32 (31 live cols + organization_id). SELECT 1 FROM pg_constraint WHERE conname LIKE '%gig_id_staff_profile_id%' returns the crew UNIQUE. has_table_privilege('anon','staff_app.staff_profiles','SELECT') returns false (anon denied). get_advisors(security) shows no NEW rls_disabled/rls_enabled_no_policy findings for staff_app objects vs baseline.</automated>
  </verify>
  <acceptance_criteria>
    - `staff_app.staff_profiles` has all 31 live columns with matching names/types/defaults PLUS a nullable `organization_id`
    - `staff_app.gigs` has a nullable `hito_event_id` with no foreign key; `staff_app.crew` has `UNIQUE (gig_id, staff_profile_id)`
    - `staff_app.offers.token_hash` is NOT NULL UNIQUE and `staff_app.offers.status` has the 5-value CHECK enum with default 'sent'
    - All four staff_app tables report `rowsecurity=true`; anon is denied (no anon SELECT policy or grant exists yet)
    - Zero new objects created in HITO's `public` schema
    - `get_advisors(security)` shows zero NEW RLS findings on the four new staff_app tables (baseline excluded)
  </acceptance_criteria>
  <done>staff_app.staff_profiles superset + gigs + crew + offers created, RLS on, anon denied, no public writes, advisor clean vs baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon role → staff_app tables | The published anon key (used later by the form) must never read/write these tables directly except through explicitly granted paths |
| cross-org read | Any org's member must not read another org's rows |
| staff_app schema → HITO public.* | The app schema shares the DB with HITO but must never write to (or create objects in) HITO's public schema this phase (D-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | staff_app staff_profiles/gigs/crew/offers | mitigate | RLS enabled on all tables from creation; anon REVOKED/denied; SELECT only via `staff_app.is_org_member(organization_id)`; no anon SELECT policy exists in this plan |
| T-01-02 | Elevation of Privilege | staff_app.is_org_member/is_org_writer | mitigate | Helpers are SECURITY DEFINER with pinned `SET search_path` and schema-qualified refs; own schema/names avoid collision with HITO's public helpers; get_advisors must show zero NEW function_search_path_mutable |
| T-01-03 | Tampering | cross-org writes | mitigate | Writer ALL policies gate on `staff_app.is_org_writer(organization_id)`; members table seeded empty until Phase 2 auth |
| T-01-17 | Tampering | accidental write to HITO public.* | mitigate | All DDL scoped to `staff_app`; acceptance asserts zero new objects in `public`; co-tenant HITO tables never referenced (D-03) |
| T-01-SC | Tampering | package installs | accept | No npm/pip/cargo installs this phase — pure SQL via Supabase MCP (per RESEARCH § Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `information_schema`/`pg_tables`: schema staff_app exists; organizations, members, staff_profiles, gigs, crew, offers all present under staff_app, all RLS-enabled, anon denied.
- Zero new objects in HITO's `public` schema.
- `get_advisors(security)` clean vs the captured baseline: no NEW function_search_path_mutable, no NEW rls_disabled, no NEW rls_enabled_no_policy referencing staff_app. HITO's pre-existing findings are out of scope. `extension_in_public` (pgcrypto in extensions) is acceptable/informational.
- Fixed SOMOS DER org UUID recorded in SUMMARY for downstream plans.
</verification>

<success_criteria>
- Schema `staff_app` exists inside HITO's project `luillpzfqzbpoqkgvjuw` (no new project — D-03).
- Six staff_app tables exist, org-scoped, RLS-enabled; anon denied direct read; nothing written to HITO's public.*.
- `staff_app.gigs.hito_event_id` nullable present (D-01), no bridge logic anywhere.
- `get_advisors(security)` reports zero NEW high findings referencing staff_app objects.
</success_criteria>

<output>
Create `.planning/phases/01-own-data-foundation/01-01-SUMMARY.md` when done — MUST record the shared project ref (`luillpzfqzbpoqkgvjuw`), the get_advisors baseline captured before migrating, the schema name `staff_app`, and the fixed SOMOS DER org UUID (plans 01-02/01-03/01-04 depend on these values). Note: the project's existing URL/anon/service keys are unchanged and already known — no new keys are created.
</output>
