---
phase: 2
slug: find-staff
status: approved
reviewed_at: 2026-07-14
shadcn_initialized: false
preset: none
created: 2026-07-14
brand: LABURO
---

# Phase 2 — UI Design Contract (LABURO — Find Staff)

> Visual and interaction contract for the FIRST UI of LABURO: phone-first login → search/filter the real 687-applicant pool → open a candidate profile with CV.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.
> Brand direction (locked, CONTEXT D-01): "azul, confianza, atractivo, letras medio glow, tipo globos" — dark base + blue neon glow for BRAND moments only; the working UI stays clean, fast, legible. NOT the SOMOS DER look, NOT the HITO look.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Base UI headless — shadcn N/A, **stack-locked** by RESEARCH.md/CLAUDE.md: HITO parity = Base UI) |
| Preset | not applicable |
| Component library | **@base-ui/react** `^1.6` (headless, accessible primitives; copy HITO's styled wrappers — mechanics, NOT aesthetics) |
| Styling | Tailwind CSS `4.x` (`@tailwindcss/postcss`), CSS custom-property token layer in `globals.css` |
| Icon library | **lucide-react** (HITO parity) |
| Fonts | **Inter** (the UI typeface — the entire type scale) + **Baloo 2** 700 loaded solely as the **logo/lockup asset** for the LABURO wordmark (outside the type scale, see Typography) — both FREE Google Fonts, self-hosted via `next/font/google` (zero-budget, no runtime CDN) |
| Animation | **motion** (`motion/react`) — sparingly, mobile-first, `prefers-reduced-motion` respected |
| Theme | dark-only for v1 (dark base is required for the blue neon to read); `next-themes` wired but locked to dark |

**shadcn gate outcome:** Executed. shadcn NOT initialized — the component library was already decided upstream (Base UI, for verbatim reuse of HITO's proven wrappers). Initializing shadcn would contradict a locked stack decision and add a second, conflicting primitive layer. Registry safety gate therefore not applicable (no registry blocks; all UI deps are npm packages).

---

## Spacing Scale

8-point scale (all multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gaps, chip inner padding-y |
| sm | 8px | Compact spacing, tag gaps, card internal row gaps |
| md | 16px | **Default** — mobile page gutter, card padding, gap between cards |
| lg | 24px | Section padding, header-to-content, filter groups |
| xl | 32px | Major vertical rhythm between screen regions |
| 2xl | 48px | Empty-state hero vertical breathing room |
| 3xl | 64px | Top brand/header band on login |

**Exceptions (mobile touch targets — one-handed use, SRCH-03):**
- Minimum interactive target: **44×44px** (buttons, oficio chips, filter toggles, card tap area, wa.me/tel buttons).
- Oficio chips: `min-height 44px`, padding `8px 16px`.
- Sticky bottom action bar / filter FAB safe-area: add `env(safe-area-inset-bottom)` to bottom padding.
- Mobile page gutter fixed at `16px` (md); content max-width `520px` centered on larger screens (app is phone-first but must not sprawl on desktop).

---

## Typography

One UI typeface (Inter), exactly **2 weights (400 / 600)** and 3 sizes. This table IS the complete type scale — no text renders outside it except the brand lockup asset (below), which is a logo, not type.

| Role | Font | Size | Weight | Line Height | Where |
|------|------|------|--------|-------------|-------|
| Heading | Inter | 20px | 600 | 1.25 | Candidate name on profile, section titles, filter-panel title, screen titles, empty-state heading |
| Body | Inter | 16px | 400 | 1.5 | Profile data, descriptions, input text, default paragraph |
| Label / meta | Inter | 14px | 600 | 1.4 | Chip text, form labels, button labels |

**Weight discipline:** **400 + 600 only.** No 500/700 anywhere in the UI type scale.

**Brand lockup (logo asset — explicitly OUTSIDE the type scale):** the LABURO wordmark is a fixed **logo/lockup treatment**: Baloo 2 700, 28px (login hero: 40px), line-height 1.1, with the text-glow recipe. It is a brand asset in the same category as an SVG logo — not a typographic role — and it renders ONLY the string "LABURO": app-header wordmark, login hero, and as the small visual crown of the empty-state hero. It is never used to set headings, titles, labels, or any other copy. Any in-UI "brand moment" header text (e.g. the empty-state heading "Sin resultados") uses **Inter 20px/600 with the text-glow recipe** — never Baloo 2.

**Card meta variant:** provincia/ciudad and experience line on cards use 14px Inter, weight 400, muted foreground (`--fg-muted`) — the 600 label weight is reserved for chips, form labels, and buttons.

---

## Color

Dark base, blue as the trust color, neon glow reserved for brand moments. 60/30/10 discipline.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#0A0F1F` | App background — deep navy-black so blue neon reads |
| Secondary (30%) | `#141B31` | Cards, nav/header, filter panel, elevated surfaces |
| Secondary-2 | `#1E2740` | Inputs, inactive chips, hover/pressed surfaces, dividers-as-fill |
| Border | `#2A3455` | Card/input borders, separators (1px) |
| **Accent (10%)** | `#2F80FF` | **Trust blue** — see reserved list below |
| Glow | `#4CC9FF` | Electric-blue used ONLY inside text-shadow/box-shadow glow recipes (never as a fill or text color) |
| Destructive | `#F0555C` | Reserved system color — **no destructive action exists in Phase 2** (declared for consistency only) |
| Positive / available | `#3DD68C` | "Disponible" signal pill only (used sparingly — availability filter, SRCH-02) |

**Foreground (text):**
| Token | Value | Usage |
|-------|-------|-------|
| `--fg` | `#F2F5FA` | Primary text on dark |
| `--fg-muted` | `#9AA5C0` | Secondary text, card meta, placeholders |
| `--fg-subtle` | `#6B7699` | Disabled, timestamps, low-emphasis captions |

**Accent (`#2F80FF`) reserved for — explicit list (never "all interactive elements"):**
1. The primary CTA button ("Buscá gente" and the profile quick actions' primary state)
2. Active/selected oficio chips and active filter states
3. Active nav/tab item
4. Focus ring (`0 0 0 3px rgba(47,128,255,0.45)`)
5. Links (portfolio/LinkedIn/CV link text)
6. The LABURO wordmark lockup fill + its glow

Everything else (card surfaces, list rows, inactive chips, form fields, body text) stays neutral (dominant/secondary/border/foreground tokens) — no blue.

**Categorical avatar / oficio-tag palette (carved-out exception to the 10% rule):**
No candidate photos exist (D-03) → initials avatar with a deterministic color derived from the **primary oficio**. Oficio tags may also use this muted categorical set. Low-saturation, dark-surface-safe; tag = color at ~14% opacity fill + color-at-high-lightness text; avatar = solid muted fill + `--fg` initials.

`#5B8DEF` `#2DD4BF` `#E8A13A` `#C77DFF` `#F0708A` `#E86F4E` `#8B95B5` `#3DD68C`

Deterministic: `hash(primary_oficio) % 8` → index into the array (same oficio → same color app-wide). This categorical coloring is data, not accent — it does not count against the 10% blue budget, and stays muted so blue remains the loudest color on screen.

---

## Glow Effect Spec (CSS recipes)

Glow is a **static box/text-shadow** (not animated by default — mobile perf + `prefers-reduced-motion`). ONLY on brand moments.

**Text glow (LABURO wordmark lockup; also applied to Inter 600 brand-moment headers like the empty-state heading):**
```css
color: #EAF4FF;
text-shadow:
  0 0 6px rgba(76, 201, 255, 0.55),
  0 0 18px rgba(47, 128, 255, 0.40),
  0 0 40px rgba(47, 128, 255, 0.20);
```

**Box glow (primary CTA, active oficio chip):**
```css
box-shadow:
  0 0 0 1px rgba(47, 128, 255, 0.65),
  0 0 16px rgba(47, 128, 255, 0.35);
```

**Restraint rule (checker-enforceable):** Glow appears ONLY on — the LABURO wordmark lockup, login hero, empty-state hero heading, the primary CTA, and active filter chips. Working UI (candidate cards, list rows, form fields, profile data blocks, inactive chips) = **flat, no glow**. If everything glows, nothing does.

**Optional micro-pulse (wordmark lockup only, `prefers-reduced-motion: no-preference`):** a 3s ease-in-out opacity breathe on the outer text-shadow layer, amplitude ≤ 15%. Never on the working UI.

---

## Component Anatomy

### Visual anchors (one focal point per screen — declared, checker-enforceable)
- **Search home:** the **search input + oficio chip row is the focal point on load** (top of screen, first visual weight); candidate cards are secondary content below it. The LABURO wordmark lockup sits small in the header and does not compete.
- **Profile:** the **candidate name (header) + sticky bottom action bar is the focal point**; data blocks are secondary reading content between the two.
- **Login:** the **LABURO lockup hero + the auth actions form the single focal group**; nothing else carries visual weight on that screen.

### Candidate card (D-03 — simple, fast, legible, attractive; mobile adaptive)
- **Container:** secondary surface `#141B31`, `border 1px #2A3455`, radius `16px`, padding `16px`, gap between cards `12–16px`. Whole card is one 44px+ tap target → opens profile.
- **Row 1:** initials avatar (40px, oficio-derived color) + **name** (Inter 16px/600, `--fg`) + optional experience badge on the right (see signal).
- **Row 2 (oficios):** up to 3 oficio tags (14px/600, categorical muted palette at 14% fill), then `+N` overflow tag if more. Tags do NOT glow.
- **Row 3 (meta):** location `ciudad, provincia` (Inter 14px/400, `--fg-muted`) with a small `MapPin` lucide icon (16px).
- **Experience signal:** if `experiencia = true` → subtle pill "Con experiencia" or `{eventos_trabajados} eventos` / `{anios_experiencia}` when present; if false/null → omit (never show "0 eventos" as a negative). Neutral surface, `--fg-muted`, no accent.
- **NO glow, NO photo.** Blue appears only if the card is in a pressed/active state (thin accent ring).

### Oficio chip (D-04 — the frequent, 1-tap filter)
- Horizontal scroll row of the most-common oficios pinned above results.
- **Inactive:** `#1E2740` fill, `--fg` text, no border, radius `full`, `min-height 44px`, padding `8px 16px`, Inter 14px/600.
- **Active/selected:** accent `#2F80FF` fill (or accent text + box-glow recipe), check affordance. Active = glow allowed (it's a brand-active state).
- Multi-select; selected count feeds the results query (SRCH-01, multi-select over 64 oficios).

### Filters panel (D-04 — the fine controls)
- Trigger: a "Filtros" button/FAB (shows active-filter count badge). Opens a **Base UI Dialog as a bottom sheet** (mobile-native, slides up).
- Controls: **provincia** (single/multi Select over the 24 official AR jurisdictions — data already normalized in Phase 1), **ciudad** (text/typeahead), **disponibilidad finde** (toggle), **disponibilidad viajar** (toggle), **movilidad propia** (toggle), and the SRCH-02 availability filter ("ocultar ya asignados a un gig solapado" toggle + manual availability note visible on profile).
- Footer: sticky "Aplicar" (primary) + "Limpiar filtros" (text). Respect `safe-area-inset-bottom`.

### Search input
- Full-width, secondary-2 fill, `Search` lucide icon leading, radius `12px`, 44px height, Inter 16px (16px prevents iOS zoom-on-focus).
- Placeholder: see Copywriting. Debounced free-text over name/oficios/experience detail.

### Profile screen (PERF-01, PERF-02)
- **Header:** back affordance + initials avatar (larger, 56px) + name (Inter 20px/600) + estado pill + location meta.
- **Quick actions (Claude's Discretion, locked):** primary **"Escribir por WhatsApp"** (accent, `wa.me/<E164 sin +>?text=…`) + secondary **"Llamar al candidato"** (`tel:`) — the visible label may shorten to "Llamar" when space-constrained, but the accessible label (`aria-label`) is always "Llamar al candidato". Sticky bottom action bar, 44px+, `telefono` in E.164.
- **Data blocks (Inter body, flat, no glow):** oficios (tags), experiencia (`experiencia`/`anios_experiencia`/`experiencia_detalle`), disponibilidad flags (finde/viajar/movilidad as labeled read-only pills), situación legal / donde_trabajar, motivación, internal availability note.
- **Links:** CV (see below), `portfolio_url`, `linkedin_url` as accent links with `ExternalLink` icon; omit rows whose value is null (never render empty links).
- **CV (PERF-02 + Claude's Discretion):** primary "Ver CV" → embedded viewer when viable (own-bucket PDF via short-TTL signed URL from the private `staff-cvs` bucket; Google-Drive `open?id=` links rendered via `/preview` iframe when permissions allow), with a universal fallback **"Abrir CV"** in a new tab. If the link is dead/unavailable → graceful message (see Copywriting), never a crash.

---

## Screen States (every screen declares these)

| State | Treatment |
|-------|-----------|
| Loading (results) | 3–5 skeleton cards (secondary surface, subtle shimmer, `prefers-reduced-motion` → static), no spinner-only screens |
| Loading (profile/CV) | Skeleton blocks for data; CV area shows a framed "Cargando CV…" placeholder |
| Empty (no matches) | Empty-state hero: LABURO lockup (small) as visual crown + heading "Sin resultados" in Inter 20px/600 with text-glow + body copy + "Limpiar filtros" action (see Copywriting) |
| Error (fetch failed) | Neutral card, honest message + "Reintentar" (never a silent failure) |
| Access denied (D-06 gate) | Non-authorized email that logs in sees a dedicated "sin acceso" screen (they must NOT see the 687-person PII pool) + "Cerrar sesión" |
| Offline / dead CV link | Inline fallback message + "Abrir en Drive" / "Abrir CV" escape hatch |

---

## Copywriting Contract

Voice: **Argentine Spanish, voseo, warm, direct.** (See Voice & Tone.)

| Element | Copy |
|---------|------|
| Primary CTA (search home) | **"Buscá gente"** |
| Search placeholder | "Buscá por oficio, nombre o zona…" |
| Filters trigger | "Filtros" (+ active-count badge) |
| Filters apply / reset | "Aplicar" / "Limpiar filtros" |
| Profile quick action (primary) | "Escribir por WhatsApp" |
| Profile quick action (secondary) | **"Llamar al candidato"** (visible label may shorten to "Llamar" if space-constrained; accessible label / `aria-label` always "Llamar al candidato") |
| CV primary / fallback | "Ver CV" / "Abrir CV" |
| Login — Google | "Entrar con Google" |
| Login — magic link | "Mandame un link" (+ input "Tu email") |
| Login — GitHub (only if trivial per D-05) | "Entrar con GitHub" |
| Empty state heading | "Sin resultados" |
| Empty state body | "Probá con otro oficio o sacá algún filtro." (action: "Limpiar filtros") |
| Error state | "No pudimos cargar los candidatos." — "Revisá la conexión y probá de nuevo." (action: "Reintentar") |
| CV dead-link | "No pudimos abrir este CV." — "Probá abrirlo en una pestaña nueva." (action: "Abrir CV") |
| Access denied (D-06) | "Esta cuenta no tiene acceso." — "Entrá con la cuenta autorizada o pedile a Franco que te habilite." (action: "Cerrar sesión") |
| Loading (results) | "Buscando…" (with skeletons) |
| Destructive confirmation | **None** — Phase 2 has no destructive action. "Cerrar sesión" is non-destructive, no confirmation dialog. |

---

## Interaction & Motion (Motion — `motion/react`, sparingly)

| Interaction | Motion | Notes |
|-------------|--------|-------|
| Oficio chip toggle | scale 0.96 tap + color/glow transition (120ms) | tactile, cheap |
| Card press | subtle scale 0.99 + accent ring on active | opens profile |
| Results appear | staggered fade/slide-up (≤ 8 items, 20–30ms stagger) | skip on `prefers-reduced-motion` |
| Filters bottom sheet | slide-up + backdrop fade (Base UI Dialog + Motion) | native-feeling |
| Wordmark lockup | optional static glow / ≤15% breathe | brand moment only |

Global rules: honor `prefers-reduced-motion` (disable transforms/stagger, keep instant); durations ≤ 250ms; no motion on the working data (card contents, form fields). No GSAP/anime/manual CSS transitions (global rule — Motion only).

---

## Voice & Tone

- **Argentine voseo**, warm and human — like Franco talking to his crew, not a corporate SaaS. "Buscá", "Probá", "Mandame", "Entrá".
- Short, action-first. No English in UI copy. No exclamation spam.
- Honest about failures (never fake success). Never blame the user.
- Fits the nightlife/eventos world: confident and friendly, not sterile.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| third-party | none | not applicable |

No component registry is used. All UI dependencies (`@base-ui/react`, `lucide-react`, `motion`, `sonner`, `next-themes`) are standard npm packages vetted via the locked STACK research. Registry vetting gate: **not applicable (no registry blocks declared).**

---

## Data → UI Mapping (from `staff_app.staff_profiles`, Phase 1)

| UI element | Column(s) |
|-----------|-----------|
| Card/profile name | `nombre` + `apellido` |
| Avatar color / oficio tags | `oficios` (text[], primary = `oficios[0]`), `oficios_otro` |
| Location meta | `ciudad`, `provincia` (24 official AR) |
| Experience signal | `experiencia`, `anios_experiencia`, `eventos_trabajados`, `experiencia_detalle` |
| Availability filters (D-04) | `disponibilidad_finde`, `disponibilidad_viajar`, `movilidad_propia`, `disponibilidad_aviso` |
| SRCH-02 overlap filter | app `crew`/`gigs` join (not a profile column) + manual `disponibilidad_aviso` note |
| Free-text search | `nombre`, `apellido`, `oficios`, `experiencia_detalle` |
| Estado pill | `estado` |
| Links | `cv_url` (Drive or own bucket signed URL), `portfolio_url`, `linkedin_url` |
| Quick actions | `telefono` (E.164 for `wa.me`/`tel:`), `email` |
| Legal / scope | `situacion_legal`, `donde_trabajar`, `pais_residencia`, `motivacion` |

Null-safety rule: never render an empty field row or a "0"/false as a negative badge — omit the row instead.

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** pending
