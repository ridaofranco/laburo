---
phase: 02-find-staff
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - lib/oficios.ts
  - lib/provincias.ts
  - lib/avatar-color.ts
  - lib/wa.ts
  - lib/search-params.ts
  - app/(app)/page.tsx
  - app/(app)/search-client.tsx
  - app/(app)/candidate-card.tsx
  - app/(app)/filtros-sheet.tsx
  - app/(app)/results-states.tsx
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03]
must_haves:
  truths:
    - "Franco can type free text and see candidates whose name/apellido/oficios/experience match (debounced)"
    - "Franco can tap oficio chips (multi-select) and results narrow to candidates whose oficios overlap the selection"
    - "Franco can open the Filtros bottom sheet and filter by provincia, ciudad, disponibilidad finde/viajar, movilidad, and 'ocultar ya asignados'"
    - "Results render as per-candidate cards with initials avatar (oficio-derived color), name, oficio tags, ciudad/provincia, and an experience signal — no photo"
    - "The whole flow is one-handed on a phone: 44px targets, 16px inputs, bottom-sheet filters, small payload"
    - "A search with oficios overlap ['Control de accesos'] returns >0 candidates from the live 687-row pool"
  artifacts:
    - path: "app/(app)/page.tsx"
      provides: "Server-side search over public.staff_app_profiles (SRCH-01/02)"
      contains: "staff_app_profiles"
    - path: "app/(app)/filtros-sheet.tsx"
      provides: "Base UI Dialog bottom-sheet fine filters (D-04)"
      contains: "Dialog"
    - path: "lib/oficios.ts"
      provides: "The oficio catalog (copied from somosder-web) for chips + multi-select"
      min_lines: 20
  key_links:
    - from: "app/(app)/page.tsx"
      to: "public.staff_app_profiles"
      via: "supabase.from('staff_app_profiles').overlaps('oficios',…).or(ilike…)"
      pattern: "\\.overlaps\\(.?oficios"
    - from: "app/(app)/page.tsx"
      to: "public.staff_app_crew_busy"
      via: "exclude already-assigned staff_profile_ids (SRCH-02)"
      pattern: "staff_app_crew_busy"
---

<objective>
Build LABURO's search home: a phone-first screen where Franco filters the real 687-applicant pool with 1-tap oficio chips + free text (SRCH-01), a Filtros bottom sheet for provincia/ciudad/availability and an "ocultar ya asignados" toggle (SRCH-02, D-04), and sees results as clean per-candidate cards with initials avatars and oficio tags — no photos (D-03). Filtering is server-side over the `public.staff_app_profiles` security-invoker view so the availability join is possible and the mobile payload stays small (SRCH-03).

Purpose: Success Criteria #2, #3, #5 — Franco finds the right candidate faster than the Google Sheet, one-handed.

Output: The `(app)/page.tsx` server search + client interaction layer, the oficio/provincia catalogs, deterministic avatar coloring, wa.me/tel builders, the candidate card, the Base UI bottom-sheet filters, and all screen states.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-find-staff/02-RESEARCH.md
@.planning/phases/02-find-staff/02-UI-SPEC.md
@.planning/phases/02-find-staff/02-CONTEXT.md
@.planning/phases/02-find-staff/02-01-SUMMARY.md
@.planning/phases/02-find-staff/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalogs + helpers + server-side search query (SRCH-01/02)</name>
  <read_first>
    - .planning/phases/02-find-staff/02-RESEARCH.md § "Pattern 3" (the exact server query: overlaps/or-ilike/eq/range), § "Open Question 1" (SRCH-02 minimum-honest scope), § "Assumptions Log" A5 (catalog source), § "Code Examples" (avatar-color, wa.me/tel), § "Security Domain" V5 (whitelist oficios/provincia params)
    - /Users/fridao/Proyectos/SOMOS DER/somosder-web/src/data/oficios.ts (the bilingual oficio catalog — copy the `es` labels; these are the exact strings stored in staff_profiles.oficios[])
    - /Users/fridao/Proyectos/SOMOS DER/somosder-web/src/data/paises.ts § provinciasAr (the 24 official jurisdictions)
    - .planning/phases/02-find-staff/02-UI-SPEC.md § "Data → UI Mapping", § "Color → categorical avatar palette"
    - supabase/migrations/staff_app_0007_read_layer.sql (view + index names from 02-01)
  </read_first>
  <action>
    Create `lib/oficios.ts` by copying the oficio catalog from `somosder-web/src/data/oficios.ts` — expose a flat list of `es` oficio labels (and the category grouping) so chips + the Filtros multi-select use the EXACT strings stored in `staff_app.staff_profiles.oficios[]`. Create `lib/provincias.ts` with the 24 `provinciasAr` jurisdictions. Create `lib/avatar-color.ts`: the deterministic `oficioColor(primaryOficio)` using the 8-color UI-SPEC categorical palette (`hash(primary_oficio) % 8`) — same oficio → same color app-wide (D-03). Create `lib/wa.ts`: `waLink(phone,text)` (`https://wa.me/<E.164 sin +>?text=…`) and `telLink(phone)` (`tel:+…`), stripping non-digits. Create `lib/search-params.ts`: parse/validate URL search params, WHITELISTING `oficios` against the catalog and `provincia` against the 24-list (V5 — reject/ignore unknown values; never string-concat into SQL).
    Author `app/(app)/page.tsx` as a server component that reads `searchParams` and issues ONE server-side query against `public.staff_app_profiles` via the authed Supabase server client (Franco's JWT → RLS as authenticated): select only the CARD columns (`id,nombre,apellido,oficios,oficios_otro,provincia,ciudad,experiencia,anios_experiencia,eventos_trabajados` — NOT `motivacion`/`experiencia_detalle`, keep payload small). Apply: `.overlaps('oficios', selectedOficios)` when chips selected (SRCH-01, GIN-indexed); `.or('nombre.ilike.%t%,apellido.ilike.%t%,experiencia_detalle.ilike.%t%')` for free text (parameterized via supabase-js, never concatenated); `.eq('provincia', p)`, `.eq('disponibilidad_finde', true)` etc. for toggles; `.order('nombre').range(0,49)` pagination. For SRCH-02 "ocultar ya asignados": when that toggle is on, fetch `staff_profile_id`s from `public.staff_app_crew_busy` and exclude them (`.not('id','in',(…))`). Pass results + the active-filter state to the client component. Commit.
  </action>
  <verify>
    <automated>`npm run build` exits 0. Via Supabase MCP execute_sql (as a seeded admin role): assert `SELECT count(*) FROM public.staff_app_profiles WHERE oficios && ARRAY['Control de accesos']::text[]` returns >0 (proves SRCH-01 overlap works on live data). grep app/(app)/page.tsx for `.overlaps(` AND `staff_app_profiles` AND `.range(`. grep lib/search-params.ts for a whitelist check against the oficios catalog.</automated>
  </verify>
  <acceptance_criteria>
    - `lib/oficios.ts` lists oficio `es` labels matching the strings stored in `staff_app.staff_profiles.oficios[]` (spot-check: `SELECT DISTINCT unnest(oficios) FROM staff_app.staff_profiles LIMIT 20` values appear in the catalog).
    - `lib/provincias.ts` has the 24 official AR jurisdictions (matches `provinciasAr`).
    - `lib/avatar-color.ts` `oficioColor` is deterministic (same input → same palette hex) and uses the 8 UI-SPEC categorical colors.
    - `lib/wa.ts` produces `wa.me/<digits>` (no `+`) and `tel:+<digits>`.
    - `app/(app)/page.tsx` queries `public.staff_app_profiles` server-side with `.overlaps('oficios',…)` + parameterized `.or(ilike…)` + `.eq` toggles + `.range()`; selects only card columns; excludes `staff_app_crew_busy` ids when the availability toggle is on.
    - **Live proof (MCP):** `oficios && ARRAY['Control de accesos']` over the 687-row pool returns >0 rows (SRCH-01/02 data path works on real data).
    - Search params are whitelisted (unknown oficios/provincia ignored) — no raw string interpolation into any filter.
    - `npm run build` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Search client (chips + debounced text + results) + candidate card (D-03) + screen states</name>
  <read_first>
    - .planning/phases/02-find-staff/02-UI-SPEC.md § "Component Anatomy → Candidate card, Oficio chip, Search input", § "Screen States", § "Copywriting Contract", § "Interaction & Motion", § "Visual anchors"
    - .planning/phases/02-find-staff/02-CONTEXT.md § D-03 (cards, no photo, initials avatar), § D-04 (chips)
    - lib/oficios.ts, lib/avatar-color.ts (from Task 1)
    - .planning/phases/02-find-staff/02-RESEARCH.md § "Pitfall 4" (debounce, small payload), § "Pitfall 6" (font weights)
  </read_first>
  <action>
    Author `app/(app)/search-client.tsx` (`"use client"`): the focal region on load (UI-SPEC visual anchor) = search input + horizontal oficio-chip row pinned above results. Search input: full-width, secondary-2 fill, leading `Search` lucide icon, radius 12px, 44px height, Inter 16px (kills iOS zoom), placeholder **"Buscá por oficio, nombre o zona…"**, debounced 250–300ms; on change it updates the URL searchParams (so the server component re-queries). Oficio chips: horizontal scroll of the most-common oficios, `min-height 44px`, inactive `#1E2740`/`--fg`, active `#2F80FF` fill + box-glow recipe, multi-select feeding the query (SRCH-01). A "Filtros" trigger button with an active-count badge opens the bottom sheet (Task 3). Use Motion (`motion/react`) sparingly: chip tap scale 0.96 (120ms), results staggered fade/slide-up (≤8 items, 20–30ms), all gated on `prefers-reduced-motion`.
    Author `app/(app)/candidate-card.tsx` (D-03): container `#141B31`, border `1px #2A3455`, radius 16px, padding 16px, whole card a 44px+ tap target linking to `/staff/[id]`. Row 1: initials avatar (40px, `oficioColor(oficios[0])`) + name (Inter 16px/600). Row 2: up to 3 oficio tags (14px/600, categorical palette at ~14% fill) + `+N` overflow. Row 3: `ciudad, provincia` (Inter 14px/400 `--fg-muted`) + `MapPin` icon. Experience signal: pill "Con experiencia" or `{eventos_trabajados} eventos` when present; OMIT when false/null (never "0 eventos"). No glow, no photo. Author `app/(app)/results-states.tsx`: loading = 3–5 skeleton cards (shimmer, static under reduced-motion); empty = LABURO lockup crown + "Sin resultados" (Inter 20px/600 + text-glow) + "Probá con otro oficio o sacá algún filtro." + "Limpiar filtros"; error = neutral card "No pudimos cargar los candidatos." / "Revisá la conexión y probá de nuevo." + "Reintentar". Wire these into `page.tsx`/`search-client.tsx`. Commit.
  </action>
  <verify>
    <automated>`npm run build` exits 0. grep app/(app)/candidate-card.tsx for `141B31` (card surface) AND `oficioColor`. grep app/(app)/search-client.tsx for `motion` (import from motion/react) AND `Buscá por oficio`. grep app/(app)/results-states.tsx for `Sin resultados` AND `Reintentar`. Assert NO weight 500/700 in the type scale: grep -E "font-(medium|bold)|weight.*(500|700)" app/(app)/*.tsx returns only the Baloo wordmark usage (700 allowed only for the lockup) — flag any other 700.</automated>
  </verify>
  <acceptance_criteria>
    - `search-client.tsx`: search input is 44px tall, 16px font, placeholder exactly "Buscá por oficio, nombre o zona…", debounced; oficio chips are multi-select, 44px min target, active state uses accent `#2F80FF` + box-glow, inactive `#1E2740` (no glow).
    - `candidate-card.tsx` renders name + up to 3 oficio tags (+N overflow) + `ciudad, provincia` + initials avatar colored by `oficioColor(oficios[0])`; experience pill omitted when experiencia is false/null; whole card is a ≥44px tap target linking to `/staff/[id]`; NO photo, NO glow on the card.
    - Screen states present and use the exact UI-SPEC copy: loading skeletons, empty "Sin resultados"/"Limpiar filtros", error "No pudimos cargar los candidatos."/"Reintentar".
    - Type scale discipline: only Inter 400/600 in the working UI; Baloo 2 700 appears only for the "LABURO" lockup; glow appears only on the empty-state heading + active chips (not on cards/rows).
    - Motion respects `prefers-reduced-motion`; durations ≤250ms; no motion on card contents.
    - Live smoke (Playwright MCP, best-effort as a logged-in admin or with a seeded session): typing an oficio narrows results and cards render; capture a mobile-viewport (390px) screenshot for the SUMMARY.
    - `npm run build` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Filtros bottom sheet (D-04) + SRCH-02 availability toggle wiring</name>
  <read_first>
    - .planning/phases/02-find-staff/02-UI-SPEC.md § "Component Anatomy → Filters panel", § "Interaction & Motion → Filters bottom sheet", § "Copywriting Contract" (Aplicar / Limpiar filtros)
    - .planning/phases/02-find-staff/02-CONTEXT.md § D-04 (hybrid filters), § SRCH-02
    - .planning/phases/02-find-staff/02-RESEARCH.md § "Don't Hand-Roll" (Base UI Dialog bottom sheet), § "Open Question 1" (SRCH-02 minimum honest)
    - lib/provincias.ts, lib/search-params.ts (from Task 1); app/(app)/search-client.tsx (from Task 2, the Filtros trigger)
    - /Users/fridao/Proyectos/HITO-by-DER-main (a Base UI Dialog/Select wrapper to copy the mechanics from — restyle to LABURO tokens)
  </read_first>
  <action>
    Author `app/(app)/filtros-sheet.tsx` (`"use client"`): a Base UI `Dialog` styled as a mobile bottom sheet (slides up via Motion + backdrop fade), triggered by the "Filtros" button. Controls (D-04): `provincia` — Base UI `Select` over the 24 `provincias` (single-select is fine for v1); `ciudad` — text input; three availability toggles `disponibilidad finde` / `disponibilidad viajar` / `movilidad propia`; and the SRCH-02 **"ocultar ya asignados a un gig solapado"** toggle. Footer (sticky, respect `env(safe-area-inset-bottom)`): primary **"Aplicar"** + text **"Limpiar filtros"**. Applying writes the chosen filters into the URL searchParams (the server `page.tsx` from Task 1 already reads them: `.eq('provincia',…)`, `.ilike('ciudad',…)`, the boolean `.eq` toggles, and the crew_busy exclusion). The Filtros trigger shows an active-filter count badge. Note in the SUMMARY that SRCH-02 is the minimum-honest version (excludes anyone currently in `crew`; true interval/overnight overlap matures in Phase 3 when real gigs exist) — do NOT mark full availability logic as done. Commit.
  </action>
  <verify>
    <automated>`npm run build` exits 0. grep app/(app)/filtros-sheet.tsx for `Dialog` (Base UI) AND `Aplicar` AND `Limpiar filtros` AND `ocultar ya asignados` (or the availability toggle label). grep for the 24-provincia source import from lib/provincias. Assert the sheet footer references `safe-area-inset-bottom`.</automated>
  </verify>
  <acceptance_criteria>
    - `filtros-sheet.tsx` uses a Base UI `Dialog` as a bottom sheet (slide-up + backdrop, Motion-driven) with provincia Select (24 jurisdictions), ciudad text, finde/viajar/movilidad toggles, and the SRCH-02 "ocultar ya asignados" toggle.
    - Footer has sticky "Aplicar" (primary accent) + "Limpiar filtros" (text) and respects `safe-area-inset-bottom`; the Filtros trigger shows an active-count badge.
    - Applying a provincia filter narrows the server query (`.eq('provincia',…)`); toggling "ocultar ya asignados" excludes `staff_app_crew_busy` ids.
    - Live smoke (Playwright MCP, best-effort): opening Filtros, selecting a provincia, and applying updates the result set; capture a bottom-sheet screenshot for the SUMMARY.
    - SUMMARY explicitly records that SRCH-02 is the minimum-honest version (crew-membership exclusion only; interval overlap deferred to Phase 3).
    - `npm run build` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client search input → DB query | Free text + filter params flow into a PostgREST query |
| authed browser → PII list | Card list renders 687-person PII; only members may see it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering | free-text/ILIKE + oficio/provincia params | mitigate | supabase-js parameterized `.or`/`.ilike`/`.eq` (never string-concat SQL); `lib/search-params.ts` whitelists oficios against the catalog and provincia against the 24-list (V5). |
| T-02-12 | Information Disclosure | over-fetching PII into the list payload | mitigate | List query selects only card columns (no `motivacion`/`experiencia_detalle`); heavy fields fetched only on the profile page (02-04); `.range()` pagination. RLS still scopes rows to members. |
| T-02-13 | Information Disclosure | search runs before the gate resolves | mitigate | Search lives inside `(app)/` behind the 02-02 membership gate; a non-member's identical query returns 0 rows via RLS regardless (defense-in-depth). |
| T-02-SC | Tampering | no new package installs this plan | accept | Uses only libs installed in 02-02 (motion, base-ui, lucide) — all HITO-proven/Approved; no new supply-chain surface. |
</threat_model>

<verification>
- Server-side filtered query over `public.staff_app_profiles` returns member-scoped, card-column-only results; `overlaps('oficios',…)` proven against live data.
- Chips + free text + bottom-sheet filters + availability toggle all drive the query; results render as D-03 cards with no photos.
- One-handed mobile ergonomics: 44px targets, 16px inputs, bottom sheet, small payload, `prefers-reduced-motion` honored.
- Search params whitelisted; no SQL string concatenation.
- All screen states present with exact UI-SPEC copy.
</verification>

<success_criteria>
Success Criteria #2, #3, #5 met: Franco searches/filters the 687-applicant pool by oficio multi-select + free text + provincia/ciudad/availability, one-handed on his phone, and sees clean candidate cards — faster than the Google Sheet.
</success_criteria>

<output>
Create `.planning/phases/02-find-staff/02-03-SUMMARY.md` when done — record the live overlap proof, the SRCH-02 minimum-honest scope note, and mobile screenshots.
</output>
