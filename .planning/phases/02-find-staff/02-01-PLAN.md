---
phase: 02-find-staff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/staff_app_0006_hardening.sql
  - supabase/migrations/staff_app_0007_read_layer.sql
autonomous: true
requirements: [SRCH-01, SRCH-02]
must_haves:
  truths:
    - "A non-member auth user querying public.staff_app_profiles gets 0 rows (RLS enforced through the security_invoker view)"
    - "An authenticated member of the SOMOS DER org sees all 687 rows through public.staff_app_profiles"
    - "public.staff_app_my_membership returns the caller's own members row (>=1 for a seeded admin, 0 for anyone else)"
    - "A member of role 'viewer' cannot write gigs/crew/offers; only 'owner'/'writer' can (is_org_writer enumerates allowed roles)"
    - "New staff_app functions are NOT anon-callable by default (ALTER DEFAULT PRIVILEGES revokes EXECUTE from PUBLIC)"
    - "get_advisors(security) after both migrations shows zero NEW staff_app findings vs the Phase-1 baseline"
  artifacts:
    - path: "supabase/migrations/staff_app_0006_hardening.sql"
      provides: "WR-04 members.role CHECK + is_org_writer enumerate-allowed; WR-05 ALTER DEFAULT PRIVILEGES REVOKE EXECUTE FROM PUBLIC"
      contains: "members_role_check"
    - path: "supabase/migrations/staff_app_0007_read_layer.sql"
      provides: "3 public security_invoker views + GIN/btree indexes + conditional 2-admin members seed"
      contains: "security_invoker"
  key_links:
    - from: "public.staff_app_profiles (view)"
      to: "staff_app.staff_profiles (base table RLS)"
      via: "WITH (security_invoker = true)"
      pattern: "security_invoker\\s*=\\s*true"
    - from: "public.staff_app_my_membership (view)"
      to: "staff_app.members (RLS members_select)"
      via: "security_invoker view = the D-06 gate source"
      pattern: "staff_app\\.members"
---

<objective>
Land the DB read layer that every LABURO screen queries, plus the two Phase-1 REVIEW fixes that gate all new work. PostgREST does not expose `staff_app` (Phase 1 verified `PGRST106`), so this plan exposes the app's own data through a small set of `public` **security_invoker views** (zero shared-project config change, RLS/membership enforced by the DB) and creates the search indexes. It also hardens `is_org_writer` (WR-04) and makes anon-uncallable-by-default the safe default for future `staff_app` functions (WR-05), and seeds the 2 admin `members` rows so the D-06 gate is alive on first login.

Purpose: Franco's search/profile queries (SRCH-01/02) need a queryable, RLS-scoped surface over `staff_app.staff_profiles`; the membership gate (D-06) needs a way to read the caller's own membership; and the Phase-1 fail-open writer role + anon-callable-by-default footguns must be closed before any new object is added to `staff_app`.

Output: Two committed SQL migrations (applied via Supabase MCP), 3 public views, 2-3 indexes, hardened writer helper, and 2 seeded admin members rows — all advisor-clean vs the Phase-1 baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-find-staff/02-RESEARCH.md
@.planning/phases/01-own-data-foundation/01-01-SUMMARY.md
@.planning/phases/01-own-data-foundation/01-REVIEW.md
@supabase/migrations/staff_app_0001_schema_orgs.sql
@supabase/migrations/staff_app_0002_core_tables.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live pre-checks (A2/A4) + WR-04/WR-05 hardening migration</name>
  <read_first>
    - .planning/phases/01-own-data-foundation/01-REVIEW.md § WR-04, WR-05 (the exact fixes)
    - supabase/migrations/staff_app_0001_schema_orgs.sql (current members table + is_org_writer definition to redefine)
    - .planning/phases/02-find-staff/02-RESEARCH.md § "Pitfall 7" and § "Assumptions Log" (A2, A4)
    - .planning/phases/01-own-data-foundation/01-01-SUMMARY.md § "get_advisors Security Baseline" (the diff discipline)
  </read_first>
  <action>
    First run the read-only live pre-checks via Supabase MCP against project `luillpzfqzbpoqkgvjuw` and record results in the plan SUMMARY: (A2) query `auth.users` for `ridaofrancorg@gmail.com` and `franco@somosder.ar` — record which of the 2 admin emails already exist and their `user_id`s (drives the seed in Task 2); (A4) check whether `pg_trgm` is installed/creatable (`SELECT * FROM pg_available_extensions WHERE name='pg_trgm'`) — drives whether Task 2 adds the optional trigram index. Then capture the current `get_advisors(security)` output as the working baseline (Phase-1 SUMMARY documents 20 `function_search_path_mutable`, 85/86 security_definer_executable, etc.).
    Create migration `supabase/migrations/staff_app_0006_hardening.sql` and apply it via MCP `apply_migration`. Contents: (WR-04) `ALTER TABLE staff_app.members ADD CONSTRAINT members_role_check CHECK (role IN ('owner','writer','viewer'));` and redefine `staff_app.is_org_writer` so its predicate is `... AND role IN ('owner','writer')` (enumerate-allowed, not `role <> 'viewer'`) — keep it SECURITY DEFINER STABLE with `SET search_path = ''` and fully schema-qualified refs, identical hardening to 0001. (WR-05) `ALTER DEFAULT PRIVILEGES IN SCHEMA staff_app REVOKE EXECUTE ON FUNCTIONS FROM PUBLIC;` run as the function-creating role (postgres) so every future `staff_app` function is not anon-callable by default. Do NOT change `pgrst.db_schemas`, do NOT touch HITO `public.*` tables. Write the file to disk verbatim of what was applied and commit.
  </action>
  <verify>
    <automated>Via Supabase MCP execute_sql assert: (1) SELECT conname FROM pg_constraint WHERE conname='members_role_check' returns 1 row; (2) is_org_writer body no longer contains "&lt;&gt; 'viewer'" and contains "in ('owner','writer')" (pg_get_functiondef); (3) SELECT defaclacl FROM pg_default_acl da JOIN pg_namespace n ON da.defaclnamespace=n.oid WHERE n.nspname='staff_app' AND da.defaclobjtype='f' shows no PUBLIC EXECUTE. Then run get_advisors(security) and grep the saved output for 'staff_app' — count of NEW staff_app findings vs baseline == 0.</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/staff_app_0006_hardening.sql` exists on disk and was applied via MCP.
    - `pg_constraint` contains `members_role_check` with definition `CHECK (role IN ('owner','writer','viewer'))` (assert via `pg_get_constraintdef`).
    - `pg_get_functiondef('staff_app.is_org_writer'::regprocedure)` contains `role IN ('owner','writer')` and does NOT contain `<> 'viewer'`; function is still SECURITY DEFINER with `SET search_path = ''`.
    - `pg_default_acl` for schema `staff_app`, objtype `f`, does not grant EXECUTE to PUBLIC.
    - A1/A2 result recorded: for each of the 2 admin emails, whether it exists in `auth.users` and its `user_id`. A4 result recorded: pg_trgm available yes/no.
    - `get_advisors(security)` grep for `staff_app`: zero NEW findings vs the captured baseline (byte/tokenized diff discipline from Phase 1).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Security-invoker read views + search indexes + conditional admin members seed</name>
  <read_first>
    - .planning/phases/02-find-staff/02-RESEARCH.md § "Pattern 3" (view + indexes + column list) and § "Code Examples" (staff_app_my_membership view)
    - .planning/phases/02-find-staff/02-RESEARCH.md § "Pattern 5" (members seed — pre-seed if emails exist)
    - supabase/migrations/staff_app_0002_core_tables.sql (exact staff_profiles / crew / gigs columns to expose)
    - .planning/phases/02-find-staff/02-UI-SPEC.md § "Data → UI Mapping" (which columns the UI needs)
    - Task 1 recorded A2 result (which admin user_ids exist to seed)
  </read_first>
  <action>
    Create migration `supabase/migrations/staff_app_0007_read_layer.sql` and apply via MCP. Contents:
    (1) `CREATE VIEW public.staff_app_profiles WITH (security_invoker = true) AS SELECT ...` selecting the display columns from `staff_app.staff_profiles` per RESEARCH Pattern 3: `id, nombre, apellido, oficios, oficios_otro, provincia, ciudad, experiencia, anios_experiencia, eventos_trabajados, experiencia_detalle, disponibilidad_finde, disponibilidad_viajar, movilidad_propia, disponibilidad_aviso, estado, cv_url, portfolio_url, linkedin_url, telefono, email, situacion_legal, donde_trabajar, pais_residencia, motivacion, organization_id`. `GRANT SELECT ON public.staff_app_profiles TO authenticated;` (do NOT grant anon).
    (2) `CREATE VIEW public.staff_app_my_membership WITH (security_invoker = true) AS SELECT organization_id, role FROM staff_app.members;` `GRANT SELECT ... TO authenticated;` — this is the D-06 gate source (members_select RLS scopes it to the caller; a non-member gets 0 rows).
    (3) `CREATE VIEW public.staff_app_crew_busy WITH (security_invoker = true) AS SELECT DISTINCT c.staff_profile_id FROM staff_app.crew c JOIN staff_app.gigs g ON g.id = c.gig_id;` `GRANT SELECT ... TO authenticated;` — the SRCH-02 "ocultar ya asignados" exclusion source (near-empty in Phase 2; matures Phase 3).
    (4) Indexes on the base table: `CREATE INDEX IF NOT EXISTS staff_profiles_oficios_gin ON staff_app.staff_profiles USING gin (oficios);` and `CREATE INDEX IF NOT EXISTS staff_profiles_provincia ON staff_app.staff_profiles (provincia);`. If Task 1's A4 check said pg_trgm is available, also `CREATE EXTENSION IF NOT EXISTS pg_trgm;` (schema `extensions`) + a trigram GIN index on `nombre`; otherwise skip it (687 rows seq-scan on ILIKE is fine).
    (5) Conditional seed: for each of the 2 admin emails found in `auth.users` (Task 1 A2 result), `INSERT INTO staff_app.members (organization_id, user_id, role) VALUES ('aa29aa2f-4d34-4e53-b62c-7397e8a4d123', <user_id>, 'owner') ON CONFLICT (organization_id, user_id) DO NOTHING;` — implementing the D-06 allowlist as membership. Any email NOT yet in auth.users is provisioned at first login by plan 02-02's callback (documented fallback). Write file to disk verbatim and commit.
  </action>
  <verify>
    <automated>Via Supabase MCP execute_sql: (1) SELECT relname, (SELECT option_value FROM pg_options_to_table(reloptions) WHERE option_name='security_invoker') FROM pg_class WHERE relname IN ('staff_app_profiles','staff_app_my_membership','staff_app_crew_busy') AND relkind='v' — all 3 exist with security_invoker=true. (2) has_table_privilege('anon','public.staff_app_profiles','SELECT')=false AND has_table_privilege('authenticated','public.staff_app_profiles','SELECT')=true. (3) SELECT indexname FROM pg_indexes WHERE schemaname='staff_app' AND indexname IN ('staff_profiles_oficios_gin','staff_profiles_provincia') returns 2. (4) get_advisors(security) grep 'staff_app' == 0 new findings.</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/staff_app_0007_read_layer.sql` exists on disk and was applied.
    - 3 views exist in `public` with `security_invoker=true`; each granted SELECT to `authenticated`, none to `anon`.
    - **Membership gate proof (D-06):** create a throwaway `auth.users` row (or use `set_config('request.jwt.claims', ...)` to impersonate a non-member uid), query `public.staff_app_profiles` as that role → **0 rows**; query `public.staff_app_my_membership` → **0 rows**. Impersonating a seeded admin `user_id` → `staff_app_profiles` returns 687 rows and `staff_app_my_membership` returns >=1 row with role='owner'. Clean up any throwaway user.
    - `SELECT count(*) FROM public.staff_app_profiles` run as a seeded admin = 687.
    - Indexes `staff_profiles_oficios_gin` (GIN) and `staff_profiles_provincia` exist; trigram index present only if pg_trgm was available.
    - Seeded `staff_app.members` count = number of the 2 admin emails present in auth.users (0, 1, or 2); each seeded row has role='owner' and org `aa29aa2f-4d34-4e53-b62c-7397e8a4d123`.
    - `get_advisors(security)`: zero NEW `staff_app` findings vs baseline (views with security_invoker must not introduce `security_definer_view` advisor entries).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon web key → public schema | The anon PostgREST key can reach anything in `public`; new views must not leak `staff_app` PII to it |
| authenticated JWT → staff_app RLS | Franco's JWT role must only see rows his membership allows |
| shared HITO project | This plan runs inside HITO's live project; it must not widen HITO's surface or change project-wide config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `public.staff_app_profiles` view | mitigate | `WITH (security_invoker=true)` makes the view obey base-table RLS (`is_org_member`); GRANT SELECT to `authenticated` only, never `anon` — a curious login sees 0 rows. Verified with an impersonated non-member returning 0 rows. |
| T-02-02 | Elevation of Privilege | `is_org_writer` fail-open role (WR-04) | mitigate | Redefine to `role IN ('owner','writer')` + `members_role_check` CHECK; a typo'd/unknown role can no longer gain write. |
| T-02-03 | Elevation of Privilege | future `staff_app` functions anon-callable (WR-05) | mitigate | `ALTER DEFAULT PRIVILEGES ... REVOKE EXECUTE ... FROM PUBLIC` makes not-anon-callable the default state. |
| T-02-04 | Tampering | HITO shared project surface | mitigate | No `pgrst.db_schemas` change (use `public` views); zero writes/creates in HITO `public.*`; advisor-diff proves no new project-wide findings. |
| T-02-05 | Information Disclosure | `staff_app_crew_busy` exposing assignments | accept | View exposes only `staff_profile_id`s of already-crewed people to `authenticated` members (org-scoped via base RLS); no PII, and it is the same org's own data. |
| T-02-SC | Tampering | migration DDL (no package installs this plan) | accept | Pure SQL applied via MCP; no npm/pip/cargo installs in this plan → no supply-chain surface. |
</threat_model>

<verification>
- Both migrations applied and persisted as repo SQL files under `supabase/migrations/`.
- Impersonated non-member → 0 rows from every `staff_app_*` view (the D-06 authority proven at the DB layer, independent of any UI).
- Seeded admin → 687 profiles + membership row.
- `get_advisors(security)` byte/tokenized-diff clean vs Phase-1 baseline; zero new `staff_app` findings; no `security_definer_view` advisory introduced.
- WR-04 + WR-05 both closed and asserted.
</verification>

<success_criteria>
The Next.js server (plan 02-02+) can read the app's own pool and the caller's membership through `public` views with RLS/membership enforced by Postgres, search indexes exist, the Phase-1 writer/anon footguns are closed, the 2 admin members are seeded (or provisioned at login), and the advisor baseline is unchanged.
</success_criteria>

<output>
Create `.planning/phases/02-find-staff/02-01-SUMMARY.md` when done — record A2/A4 live-check results, the seeded member count, and the advisor diff.
</output>
