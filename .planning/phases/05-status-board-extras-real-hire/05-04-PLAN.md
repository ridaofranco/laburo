---
phase: 05-status-board-extras-real-hire
plan: 04
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - app/(app)/staff/[id]/notes-actions.ts
  - app/(app)/staff/[id]/favorite-note.tsx
  - app/(app)/staff/[id]/rating-actions.ts
  - app/(app)/staff/[id]/rating.tsx
  - app/(app)/staff/[id]/page.tsx
autonomous: true
requirements: [XTRA-01, XTRA-04]

must_haves:
  truths:
    - "Franco marca/desmarca un candidato como favorito y escribe una nota privada en su perfil; se persiste vía RPC y sobrevive un refresh"
    - "Franco califica al candidato 1-5 + nota opcional por cada gig en que fue aceptado; se persiste vía RPC y sobrevive un refresh"
    - "favoritos, notas y ratings NUNCA aparecen en ninguna superficie candidate-facing (get_public_offer, /o) — grep-negativo obligatorio"
    - "todas las escrituras pasan por los RPCs public SECURITY DEFINER con el cliente autenticado (nunca insert directo a staff_app, nunca service-role)"
  artifacts:
    - path: "app/(app)/staff/[id]/notes-actions.ts"
      provides: "server action setCandidateNote → rpc staff_app_set_candidate_note, member-gated"
      contains: "staff_app_set_candidate_note"
    - path: "app/(app)/staff/[id]/rating-actions.ts"
      provides: "server action rateStaff → rpc staff_app_rate_staff, member-gated"
      contains: "staff_app_rate_staff"
    - path: "app/(app)/staff/[id]/favorite-note.tsx"
      provides: "client: toggle favorito (Bookmark) + textarea nota + toast"
      min_lines: 30
    - path: "app/(app)/staff/[id]/rating.tsx"
      provides: "client: 5 estrellas (Star) + nota por gig + toast"
      min_lines: 30
  key_links:
    - from: "app/(app)/staff/[id]/notes-actions.ts"
      to: "public.staff_app_set_candidate_note"
      via: "supabase.rpc con el cliente autenticado tras gate de membresía"
      pattern: "staff_app_set_candidate_note"
    - from: "app/(app)/staff/[id]/page.tsx"
      to: "public.staff_app_candidate_notes / public.staff_app_staff_ratings"
      via: "reads con el cliente autenticado (RLS is_org_member)"
      pattern: "staff_app_candidate_notes"
---

<objective>
## Phase Goal

**As a** productor de SOMOS DER (Franco), **I want to** marcar favoritos, escribir notas privadas y calificar 1-5 a cada candidato después del evento, **so that** voy sembrando mi criterio de confianza sobre el pool, invisible para el candidato.

Slice vertical de XTRA-01 (favoritos + notas privadas, D-03) y XTRA-04 (rating post-evento, D-06), colgando ambos del perfil `/staff/[id]`. Todo producer-only: escrituras vía los RPCs `public` SECURITY DEFINER de 05-02 (`staff_app_set_candidate_note`, `staff_app_rate_staff`) con el cliente autenticado; lecturas vía las vistas security_invoker. **Aislamiento crítico (Pitfall 2, D-03): nada de esto puede tocar ninguna superficie candidate-facing.**

Purpose: da a Franco memoria sobre el pool (favoritos/notas) y siembra los datos de confiabilidad del marketplace v2 (ratings), sin exponer nada al candidato.
Output: favorito + nota + rating en el perfil, persistidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-status-board-extras-real-hire/05-CONTEXT.md
@.planning/phases/05-status-board-extras-real-hire/05-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: XTRA-01 — server action de notas + client favorito/nota</name>
  <read_first>
    - app/(app)/staff/[id]/offer-actions.ts (molde EXACTO de server action member-gated: "use server", gate staff_app_my_membership.maybeSingle → throw forbidden, supabase.rpc con cliente autenticado, NUNCA service-role; grep-negativo de createServiceRoleClient)
    - app/(app)/staff/[id]/cv-actions.ts (segundo molde de gate de membresía)
    - app/(app)/staff/[id]/quick-actions.tsx (molde de client component con lucide-react + tokens + botones ≥44px)
  </read_first>
  <action>
    (a) `app/(app)/staff/[id]/notes-actions.ts` — "use server". Exportar `setCandidateNote(input: { staffProfileId: string; isFavorite: boolean; note: string | null }): Promise<{ ok: boolean; reason?: string }>`. Gate de membresía copiado de offer-actions.ts (staff_app_my_membership.maybeSingle → si no, throw "forbidden"). Luego `supabase.rpc("staff_app_set_candidate_note", { p_staff_profile_id, p_is_favorite, p_note })` con el CLIENTE AUTENTICADO. Devolver ok según el jsonb. NO importar createServiceRoleClient. Copy de error en voseo si hace falta.

    (b) `app/(app)/staff/[id]/favorite-note.tsx` — "use client". Props: `staffProfileId`, `initialFavorite: boolean`, `initialNote: string | null`. UI: un botón toggle de favorito con el ícono Bookmark (lucide-react) — activo = relleno/accent, inactivo = outline; y un <textarea> para la nota privada con placeholder tipo "Nota privada (sólo la ves vos)". Un botón "Guardar" que llama a setCandidateNote y muestra un toast sonner ("Guardado") en éxito o error honesto en fallo. useTransition/pending para el estado. El toggle de favorito puede guardar al toque (optimista) o junto con "Guardar" — a discreción, pero el estado debe persistir. Tokens/clases existentes, mobile-first, targets ≥44px. Copy voseo sin em dash. Dejar claro en un microcopy que la nota es privada.
  </action>
  <verify>
    <automated>cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && test -f "app/(app)/staff/[id]/notes-actions.ts" && test -f "app/(app)/staff/[id]/favorite-note.tsx" && grep -q "staff_app_set_candidate_note" "app/(app)/staff/[id]/notes-actions.ts" && grep -q "staff_app_my_membership" "app/(app)/staff/[id]/notes-actions.ts" && ! grep -q "createServiceRoleClient" "app/(app)/staff/[id]/notes-actions.ts" && grep -q "Bookmark" "app/(app)/staff/[id]/favorite-note.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - setCandidateNote es member-gated, usa el cliente autenticado, llama al RPC, no importa service-role.
    - favorite-note.tsx tiene toggle favorito (Bookmark) + textarea nota + guardar con toast.
  </acceptance_criteria>
  <done>Favorito + nota privada guardables desde el perfil vía RPC.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: XTRA-04 — server action de rating + client de estrellas por gig</name>
  <read_first>
    - app/(app)/staff/[id]/offer-actions.ts (mismo molde de server action member-gated + rpc autenticado)
    - app/(app)/staff/[id]/offer-status.tsx (cómo se listan los gigs/ofertas del candidato; para saber por qué gigs se puede calificar)
  </read_first>
  <action>
    (a) `app/(app)/staff/[id]/rating-actions.ts` — "use server". Exportar `rateStaff(input: { staffProfileId: string; gigId: string; score: number; note: string | null }): Promise<{ ok: boolean; reason?: string }>`. Gate de membresía igual que notes-actions. Validar score entero 1..5 client-side y server-side (si no, ok:false reason score_out_of_range sin llamar al RPC). `supabase.rpc("staff_app_rate_staff", { p_staff_profile_id, p_gig_id, p_score, p_note })` con el cliente autenticado. NO service-role.

    (b) `app/(app)/staff/[id]/rating.tsx` — "use client". Props: `staffProfileId`, y una lista `gigs: { gigId: string; gigTitle: string; score: number | null; note: string | null }[]` (los gigs donde el candidato fue aceptado, con su rating actual si existe). Por cada gig, una fila con el título del gig y un control de 5 estrellas (Star de lucide-react, relleno hasta el score elegido) + un input/textarea de nota opcional + guardar → rateStaff + toast sonner. useTransition para pending. Si la lista de gigs viene vacía (el candidato todavía no aceptó ningún gig), mostrar un microcopy "Vas a poder calificarlo cuando trabaje en un gig" y no renderizar controles. Tokens existentes, mobile-first, targets ≥44px, voseo sin em dash.
  </action>
  <verify>
    <automated>cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && test -f "app/(app)/staff/[id]/rating-actions.ts" && test -f "app/(app)/staff/[id]/rating.tsx" && grep -q "staff_app_rate_staff" "app/(app)/staff/[id]/rating-actions.ts" && grep -q "staff_app_my_membership" "app/(app)/staff/[id]/rating-actions.ts" && ! grep -q "createServiceRoleClient" "app/(app)/staff/[id]/rating-actions.ts" && grep -q "Star" "app/(app)/staff/[id]/rating.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - rateStaff es member-gated, valida score 1..5 en server, usa el cliente autenticado, llama al RPC, no service-role.
    - rating.tsx muestra 5 estrellas + nota por gig aceptado y maneja el caso "sin gigs aún".
  </acceptance_criteria>
  <done>Rating 1-5 + nota por gig guardable desde el perfil vía RPC.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Cablear favorito/nota + rating en el perfil + guardia de aislamiento</name>
  <read_first>
    - app/(app)/staff/[id]/page.tsx (RSC del perfil; dónde ya se leen offers de staff_app_offers y se renderiza OfferStatusList; acá se agregan las lecturas de notes/ratings y los 2 componentes nuevos). OJO: el select actual de staff_app_offers (líneas ~129-131) es "id,role,gig_title,status,expires_at,sent_at,viewed_at,responded_at" — NO trae gig_id, y la derivación de ratings lo necesita.
    - app/(app)/staff/[id]/offer-status.tsx (el tipo exportado OfferRow — NO tiene gig_id; NO modificar este archivo en este plan: OfferStatusList no lo necesita y tocarlo sería innecesario)
  </read_first>
  <action>
    En `app/(app)/staff/[id]/page.tsx`, con el cliente autenticado:
    (a) Leer la nota/favorito: `supabase.from("staff_app_candidate_notes").select("is_favorite,note").eq("staff_profile_id", id).maybeSingle()` (RLS scopea; puede no existir → null).
    (b) Leer los ratings: `supabase.from("staff_app_staff_ratings").select("gig_id,score,note").eq("staff_profile_id", id)`.
    (c) EXTENDER el select de offers para exponer gig_id: en la query existente de `staff_app_offers` de este archivo (hoy `.select("id,role,gig_title,status,expires_at,sent_at,viewed_at,responded_at")`) agregar `gig_id` → `.select("id,role,gig_id,gig_title,status,expires_at,sent_at,viewed_at,responded_at")`. NO tocar el tipo exportado `OfferRow` de offer-status.tsx (lo consume OfferStatusList y NO necesita gig_id); en su lugar, para la derivación de ratings castear `offersData` a un tipo LOCAL en page.tsx (ej. `type RatableOffer = { gig_id: string | null; gig_title: string | null; status: string | null }`). Con ese tipo local, derivar la lista de gigs calificables: los `offersData` con status='accepted' (par gig_id + gig_title distintos); hacer merge con los ratings existentes para pasar score/note actuales a `<Rating>`. El `gigId` que se pasa a `rateStaff({staffProfileId, gigId, score, note})` sale de ese `gig_id` real — nunca undefined.
    (d) Renderizar `<FavoriteNote staffProfileId={id} initialFavorite={...} initialNote={...} />` cerca del header (junto al nombre o bajo el CTA "Crear oferta"), y una `<Section title="Calificación">` con `<Rating staffProfileId={id} gigs={...} />` (mostrar la sección siempre que haya al menos un gig aceptado; si no, la sección se omite o muestra el microcopy — a discreción, coherente con null-safety del perfil).
    (e) GUARDIA DE AISLAMIENTO (D-03/Pitfall 2): confirmar que NADA de notes/ratings/favorite se filtra a la superficie candidate-facing. La página /o/[token] NO se toca en este plan; el grep de verificación debe volver VACÍO sobre app/o y sobre cualquier RPC anon.
    Copy voseo sin em dash. Respetar el molde null-safety del perfil (omitir filas/secciones vacías).
  </action>
  <verify>
    <automated>cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && grep -q "staff_app_candidate_notes" "app/(app)/staff/[id]/page.tsx" && grep -q "staff_app_staff_ratings" "app/(app)/staff/[id]/page.tsx" && grep -q "FavoriteNote" "app/(app)/staff/[id]/page.tsx" && grep -q "Rating" "app/(app)/staff/[id]/page.tsx" && grep -q "gig_id" "app/(app)/staff/[id]/page.tsx" && ! grep -riE "candidate_notes|staff_ratings|favorite|is_favorite" app/o && npx tsc --noEmit 2>&1 | grep -v node_modules | grep -iE "staff/\\[id\\]" && echo "TSC-ERRORS" || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - El perfil lee candidate_notes + staff_ratings y renderiza FavoriteNote + Rating.
    - Los gigs calificables se derivan de las ofertas aceptadas.
    - El select de staff_app_offers en page.tsx incluye gig_id; la derivación usa un tipo local (offer-status.tsx OfferRow NO se modifica); rateStaff recibe un gigId real, nunca undefined.
    - grep-negativo: cero referencias a notes/ratings/favorite en app/o.
    - tsc sin errores nuevos en staff/[id].
  </acceptance_criteria>
  <done>Favorito, nota y rating viven en el perfil, persistidos, con aislamiento candidate-facing verificado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated (Franco) → RPC | escrituras de notas/ratings, member-gated + is_org_writer |
| candidato / anon → notas | superficie cero (garantía de aislamiento) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-16 | Information Disclosure | nota/favorito/rating filtrado al candidato | mitigate | grep-negativo en app/o + no consultar estas vistas en ninguna superficie por-token; RLS + REVOKE anon (05-02) |
| T-5-17 | Elevation of Privilege | escritura por no-miembro / service-role client-side | mitigate | gate staff_app_my_membership en la server action + is_org_writer en el RPC; grep-negativo de createServiceRoleClient |
| T-5-18 | Input Validation | score fuera de rango | mitigate | validación 1..5 client + server + CHECK en la tabla (05-02) |
| T-5-19 | Tampering | staff_profile_id/gig_id de otro org | mitigate | los RPCs validan pertenencia al org fijo; RLS en las vistas de lectura |
</threat_model>

<verification>
- Server actions member-gated, RPC autenticado, sin service-role (grep-negativo).
- Perfil lee las 2 vistas y renderiza los 2 componentes.
- Aislamiento: grep-negativo de notes/ratings/favorite en app/o.
- tsc limpio en los archivos tocados.
</verification>

<success_criteria>
- Favorito + nota privada persistidos desde el perfil (XTRA-01).
- Rating 1-5 + nota por gig aceptado persistido (XTRA-04).
- Cero fuga a superficies candidate-facing.
</success_criteria>

<output>
Create `.planning/phases/05-status-board-extras-real-hire/05-04-SUMMARY.md` when done.
</output>
