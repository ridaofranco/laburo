---
phase: 05-status-board-extras-real-hire
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/staff_app_0010_board_reminder.sql
  - supabase/tests/staff_app_0010_board_reminder_harness.sql
autonomous: false
requirements: [STAT-02, XTRA-03]

must_haves:
  truths:
    - "public.staff_app_offers trae staff_nombre y staff_apellido al final (el board ve a QUIÉN se le ofertó cada rol) sin romper las columnas existentes ni el orden previo"
    - "staff_app.offers tiene la columna reminded_at timestamptz (nullable) como ancla de exactly-once del recordatorio"
    - "public.staff_app_offers_due_reminder(p_within_days) devuelve SÓLO ofertas status IN (sent,viewed) con reminded_at IS NULL y expires_at dentro de la ventana, y en la MISMA sentencia les setea reminded_at=now() (una segunda corrida no re-selecciona esas filas)"
    - "el RPC de recordatorio NO rota el token, NO toca token_hash y NO devuelve ningún token (D-05/A1: el link original sigue válido)"
    - "el RPC de recordatorio es EXECUTE sólo para service_role; anon y authenticated no pueden llamarlo; la vista sigue authenticated-only (anon revocado)"
    - "get_advisors(security) no reporta ninguna clase de finding nueva vs el baseline de Fase 4 (sin function_search_path_mutable nuevo, sin security_definer_view)"
  artifacts:
    - path: "supabase/migrations/staff_app_0010_board_reminder.sql"
      provides: "CREATE OR REPLACE VIEW public.staff_app_offers (+staff_nombre/apellido) + ALTER TABLE offers ADD reminded_at + RPC staff_app_offers_due_reminder + WR-05"
      contains: "staff_app_offers_due_reminder"
    - path: "supabase/tests/staff_app_0010_board_reminder_harness.sql"
      provides: "harness re-ejecutable seed→assert→cleanup del due-reminder (exactly-once) + de las columnas nuevas de la vista"
      contains: "reminded_at"
  key_links:
    - from: "public.staff_app_offers_due_reminder()"
      to: "staff_app.offers.reminded_at"
      via: "UPDATE ... SET reminded_at=now() ... RETURNING dentro de un CTE con WHERE reminded_at IS NULL"
      pattern: "reminded_at\\s*=\\s*now"
    - from: "public.staff_app_offers"
      to: "staff_app.staff_profiles"
      via: "JOIN para staff_nombre/staff_apellido en la vista security_invoker"
      pattern: "staff_profiles"
---

<objective>
## Phase Goal

**As a** productor de SOMOS DER (Franco), **I want to** ver de un vistazo qué roles de cada gig están cubiertos y cuáles siguen abiertos, **so that** no tengo que cazar candidato por candidato para saber cómo viene la cobertura.

Migración `staff_app_0010_board_reminder`: la puerta de datos de la Ola 1 para DOS capacidades de Fase 5.
1. **STAT-02 (board):** hoy `public.staff_app_offers` (0009) trae todo lo de la oferta + `gig_title`, pero NO el nombre del candidato, así que el board no puede mostrar a quién se le ofertó cada rol. Este plan hace `CREATE OR REPLACE VIEW` agregando `staff_nombre`/`staff_apellido` AL FINAL del SELECT (Pitfall 7: sólo se pueden agregar columnas al final).
2. **XTRA-03 (recordatorio, D-05/A1):** agrega la columna `offers.reminded_at` (ancla de exactly-once) y el RPC `staff_app_offers_due_reminder()` que el cron (05-05) llama con service-role para obtener las ofertas por vencer y marcarlas recordadas en la misma sentencia. **DECISIÓN A1 BLOQUEADA: el RPC NO rota el token, NO toca `token_hash`, NO invalida el link original y NO devuelve ningún token** — sólo devuelve los datos para nudgear al candidato a revisar el email original.

Purpose: sin esta migración el board (05-03) no tiene nombres y el cron (05-05) no tiene ni la columna ni el RPC. Es Ola 1 bloqueante.
Output: 1 archivo de migración + 1 harness SQL. **[BLOCKING] aplicados y verificados EN VIVO por el orquestador** (los subagentes no tienen las tools MCP de Supabase) ANTES de que corra la Ola 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-status-board-extras-real-hire/05-CONTEXT.md
@.planning/phases/05-status-board-extras-real-hire/05-RESEARCH.md
@.planning/phases/04-accept-close-loop/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir la migración staff_app_0010 (board view + reminded_at + due-reminder RPC)</name>
  <read_first>
    - supabase/migrations/staff_app_0009_public_magic_link.sql (la vista public.staff_app_offers actual + el patrón WR-05 de vista security_invoker que se re-otorga tras el REPLACE)
    - supabase/migrations/staff_app_0008_create_offer.sql (el molde EXACTO de RPC SECURITY DEFINER: org fija forzada como constante, SET search_path pinned, extensions.* schema-qualified, WR-05 REVOKE FROM PUBLIC,anon + GRANT scoped, COMMENT)
    - supabase/migrations/staff_app_0003_magic_link_rpcs.sql (dónde vive token_hash y por qué NO se toca acá — A1)
  </read_first>
  <action>
    Crear `supabase/migrations/staff_app_0010_board_reminder.sql` con un header comentado (mismo estilo que 0008/0009: proyecto luillpzfqzbpoqkgvjuw, aplicado LIVE por el orquestador, notas de seguridad). Tres objetos, en este orden:

    (1) `ALTER TABLE staff_app.offers ADD COLUMN reminded_at timestamptz;` — nullable, sin default. Es el ancla de exactly-once del recordatorio (XTRA-03).

    (2) `CREATE OR REPLACE VIEW public.staff_app_offers WITH (security_invoker = true)` replicando VERBATIM el SELECT actual de 0009 (id, gig_id, staff_profile_id, role, amount, conditions, status, expires_at, sent_at, viewed_at, responded_at, gig_title, organization_id) y AGREGANDO al final del SELECT, tras un JOIN a `staff_app.staff_profiles sp ON sp.id = o.staff_profile_id`, dos columnas nuevas: `sp.nombre AS staff_nombre` y `sp.apellido AS staff_apellido`. Respetar Pitfall 7: las columnas nuevas van AL FINAL, no se reordena ni cambia tipo de las existentes. Tras el REPLACE re-aplicar WR-05 por las dudas: `GRANT SELECT ON public.staff_app_offers TO authenticated;` y `REVOKE ALL ON public.staff_app_offers FROM anon;`. El GRANT SELECT sobre staff_app.staff_profiles TO authenticated ya existe (0007) y sobre staff_app.offers ya existe (0009); no re-crearlos. Actualizar el COMMENT ON VIEW notando el agregado de staff_nombre/apellido (PII org-scoped, authenticated-only, jamás anon; NO tocar staff_app.get_public_offer que es candidate-facing y sólo devuelve first_name).

    (3) `CREATE OR REPLACE FUNCTION public.staff_app_offers_due_reminder(p_within_days int DEFAULT 2) RETURNS TABLE(offer_id uuid, email text, first_name text, gig_title text, role text, expires_at timestamptz)` LANGUAGE plpgsql, SECURITY DEFINER, `SET search_path = staff_app, public, pg_temp`. Cuerpo: un único statement con CTE que UPDATEA y devuelve — `RETURN QUERY WITH due AS ( UPDATE staff_app.offers o SET reminded_at = now() WHERE o.organization_id = 'aa29aa2f-4d34-4e53-b62c-7397e8a4d123' AND o.status IN ('sent','viewed') AND o.reminded_at IS NULL AND o.expires_at > now() AND o.expires_at <= now() + make_interval(days => greatest(1, p_within_days)) RETURNING o.id, o.gig_id, o.staff_profile_id, o.role, o.expires_at ) SELECT d.id, sp.email, sp.nombre, g.title, d.role, d.expires_at FROM due d JOIN staff_app.gigs g ON g.id = d.gig_id JOIN staff_app.staff_profiles sp ON sp.id = d.staff_profile_id;`. CLAVE (A1/D-05): este RPC NO genera token, NO llama extensions.gen_random_bytes/digest, NO escribe token_hash y NO devuelve token — el link mágico original sigue vivo. El exactly-once es real porque el UPDATE setea reminded_at y el WHERE filtra reminded_at IS NULL en la misma transacción (Pitfall 3). La org se fuerza desde la constante fija, nunca de input. WR-05: `REVOKE ALL ON FUNCTION public.staff_app_offers_due_reminder(int) FROM PUBLIC, anon, authenticated;` seguido de `GRANT EXECUTE ON FUNCTION public.staff_app_offers_due_reminder(int) TO service_role;` — SÓLO service_role (el cron usa el cliente service-role; anon/authenticated jamás, porque el RPC muta reminded_at y devuelve emails/PII). Agregar COMMENT ON FUNCTION explicando exactly-once + que NO rota token (A1).

    Nada de fenced code en prosa: seguí los identificadores y firmas de arriba tal cual.
  </action>
  <verify>
    <automated>cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && f=supabase/migrations/staff_app_0010_board_reminder.sql && grep -v '^--' $f | grep -c "reminded_at" | awk '{if($1<3){print "FAIL reminded_at"; exit 1}}' && grep -v '^--' $f | grep -q "staff_nombre" && grep -v '^--' $f | grep -q "security_invoker" && grep -v '^--' $f | grep -q "SET search_path = staff_app, public, pg_temp" && grep -v '^--' $f | grep -q "GRANT EXECUTE ON FUNCTION public.staff_app_offers_due_reminder(int) TO service_role" && grep -v '^--' $f | grep -q "REVOKE ALL ON FUNCTION public.staff_app_offers_due_reminder(int) FROM PUBLIC, anon, authenticated" && ! grep -v '^--' $f | grep -qi "gen_random_bytes\|token_hash" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - El archivo existe y define los 3 objetos (ALTER ADD reminded_at, CREATE OR REPLACE VIEW con staff_nombre/apellido, RPC due-reminder).
    - El RPC due-reminder NO contiene `gen_random_bytes` ni `token_hash` (A1 verificado por grep-negativo).
    - El RPC tiene GRANT EXECUTE sólo a service_role y REVOKE a PUBLIC/anon/authenticated.
    - La vista es security_invoker y re-aplica GRANT authenticated + REVOKE anon.
  </acceptance_criteria>
  <done>La migración staff_app_0010 está escrita con los 3 objetos, WR-05 en cada uno, search_path pinned en el RPC, y sin rotación de token.</done>
</task>

<task type="auto">
  <name>Task 2: Harness SQL re-ejecutable del due-reminder (exactly-once) y de la vista</name>
  <read_first>
    - supabase/tests/staff_app_0009_public_magic_link_harness.sql (molde seed→assert→cleanup con RAISE EXCEPTION en cada assert)
    - supabase/tests/staff_app_0008_create_offer_harness.sql (cómo seedea un gig + candidato + oferta de prueba y limpia todo al final)
  </read_first>
  <action>
    Crear `supabase/tests/staff_app_0010_board_reminder_harness.sql` como un bloque DO/transacción re-ejecutable (mismo estilo que los harness de 0008/0009): seedea un candidato + gig + una oferta con `expires_at = now() + interval '1 day'` y `reminded_at NULL` en la org fija; luego assert:
    (a) `SELECT` sobre `public.staff_app_offers` para la oferta seed devuelve staff_nombre/staff_apellido no nulos (columnas nuevas presentes).
    (b) primera llamada a `public.staff_app_offers_due_reminder(2)` devuelve exactamente esa oferta (offer_id, email, first_name, gig_title, role, expires_at) y su `reminded_at` quedó seteado.
    (c) segunda llamada a `public.staff_app_offers_due_reminder(2)` devuelve 0 filas para esa oferta (exactly-once real).
    (d) una oferta con `expires_at = now() + interval '30 days'` NO aparece (fuera de ventana); una oferta status='accepted' NO aparece.
    Cada assert con RAISE EXCEPTION si falla. Al final `ROLLBACK` o DELETE explícito de todo lo seedeado (crew/offers/gigs/candidato de prueba) para no dejar residuos — igual que 0008/0009. Incluir un comentario indicando que el orquestador lo corre vía Supabase MCP tras aplicar la migración.
  </action>
  <verify>
    <automated>cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && f=supabase/tests/staff_app_0010_board_reminder_harness.sql && test -f $f && grep -v '^--' $f | grep -q "staff_app_offers_due_reminder" && grep -v '^--' $f | grep -qi "RAISE EXCEPTION" && grep -v '^--' $f | grep -q "reminded_at" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - El harness seedea, asserta exactly-once (2 llamadas: 1 devuelve la oferta, la 2da devuelve 0), verifica las columnas nuevas de la vista, y limpia.
    - Usa RAISE EXCEPTION en los asserts.
  </acceptance_criteria>
  <done>El harness re-ejecutable existe y cubre exactly-once + columnas nuevas + límites de ventana/estado.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>staff_app_0010 (SQL + harness) listo para aplicar en vivo.</what-built>
  <how-to-verify>
    [BLOCKING — ORQUESTADOR, no el executor] Aplicar `staff_app_0010_board_reminder.sql` a `luillpzfqzbpoqkgvjuw` vía Supabase MCP `apply_migration`. Luego correr el harness `staff_app_0010_board_reminder_harness.sql` (debe pasar todos los asserts sin excepción). Luego `get_advisors(security)` y confirmar CERO clase de finding nueva vs el baseline de Fase 4 (sin `function_search_path_mutable` en el nuevo RPC, sin `security_definer_view`). Confirmar grants en vivo: `service_role` EXECUTE = true en `staff_app_offers_due_reminder`; anon/authenticated EXECUTE = false; anon SELECT en la vista = false.
  </how-to-verify>
  <resume-signal>Escribí "aplicada" cuando la migración esté aplicada, el harness pase y get_advisors dé limpio; o pegá el finding a corregir.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron (service-role) → RPC | el cron con service-role llama al due-reminder; nadie más debe poder |
| authenticated (Franco) → vista | Franco lee staff_app_offers con su JWT (RLS is_org_member) |
| anon → todo | ninguna superficie de esta migración debe ser anon-callable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01 | Elevation of Privilege | RPC/vista nuevos anon-callable por default privilege | mitigate | WR-05 REVOKE explícito FROM PUBLIC,anon en cada objeto + get_advisors post-apply |
| T-5-02 | Information Disclosure | due-reminder devuelve emails/PII | mitigate | GRANT EXECUTE sólo a service_role; REVOKE a anon+authenticated |
| T-5-03 | Tampering | doble/replay del recordatorio | mitigate | UPDATE reminded_at + WHERE reminded_at IS NULL en la misma sentencia (exactly-once) |
| T-5-04 | Spoofing | search-path hijack en SECURITY DEFINER | mitigate | SET search_path = staff_app, public, pg_temp pinned; extensions no usadas acá |
| T-5-05 | Information Disclosure | staff_nombre/apellido filtrado al candidato | mitigate | agregado sólo a la vista authenticated-only; get_public_offer NO se toca (sigue devolviendo first_name) |
| T-5-06 | Tampering | org tomada de input | mitigate | org forzada desde la constante fija aa29aa2f-... en el RPC, nunca de parámetro |
</threat_model>

<verification>
- `grep -c` con `grep -v '^--'` sobre el SQL confirma reminded_at (≥3), staff_nombre, security_invoker, search_path pinned, GRANT service_role, y grep-negativo de gen_random_bytes/token_hash.
- El harness cubre exactly-once + columnas nuevas.
- [BLOCKING] El orquestador confirma en vivo: apply_migration OK, harness pasa, get_advisors sin finding nuevo, grants correctos.
</verification>

<success_criteria>
- public.staff_app_offers expone staff_nombre/staff_apellido sin romper columnas previas.
- offers.reminded_at existe.
- staff_app_offers_due_reminder marca exactly-once, no rota token, sólo service_role.
- get_advisors limpio.
</success_criteria>

<output>
Create `.planning/phases/05-status-board-extras-real-hire/05-01-SUMMARY.md` when done.
</output>
