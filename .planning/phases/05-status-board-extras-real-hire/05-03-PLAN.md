---
phase: 05-status-board-extras-real-hire
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/(app)/layout.tsx
  - app/(app)/tablero/page.tsx
  - app/(app)/tablero/gig-board.tsx
  - app/(app)/page.tsx
  - lib/search-params.ts
autonomous: true
requirements: [STAT-02, XTRA-03]

must_haves:
  truths:
    - "Franco entra a /tablero desde una nav en el header y ve TODOS los gigs del org, cada uno con sus ofertas agrupadas y el estado derivado por rol (cubierto / pendiente / abierto)"
    - "cada oferta del board muestra el nombre del candidato (staff_nombre) y su etiqueta derivada con offerLabel() — 'Vencida' = now()>expires_at, nunca del enum"
    - "un gig sin ninguna oferta aparece igual (desde staff_app_gigs) como 'sin ofertas todavía'"
    - "un rol abierto (rechazado/vencido) muestra un CTA 'Buscar reemplazo' que abre /?gig=<gigId> con la lista filtrada SIN los candidatos ya ofertados para ese gig"
    - "en la home de búsqueda, con ?gig=<gigId> presente, los staff_profile_id ya ofertados para ese gig quedan excluidos (patrón .not(id,in,...) espejando crew_busy)"
  artifacts:
    - path: "app/(app)/tablero/page.tsx"
      provides: "RSC del board: lee staff_app_gigs + staff_app_offers, agrupa por gig, deriva cobertura"
      min_lines: 40
    - path: "app/(app)/tablero/gig-board.tsx"
      provides: "UI del board: acordeón/cards por gig, badges por oferta, CTA Buscar reemplazo en roles abiertos"
      min_lines: 40
    - path: "lib/search-params.ts"
      provides: "PARAM.gig + parse/build para el re-filtro XTRA-02"
      contains: "gig"
  key_links:
    - from: "app/(app)/tablero/page.tsx"
      to: "public.staff_app_offers"
      via: "supabase.from('staff_app_offers').select(... staff_nombre ...) con el cliente autenticado"
      pattern: "staff_app_offers"
    - from: "app/(app)/page.tsx"
      to: "public.staff_app_offers"
      via: "query de staff_profile_id ya ofertados para gig + .not('id','in',(...))"
      pattern: "not\\(\"id\", ?\"in\""
    - from: "app/(app)/tablero/gig-board.tsx"
      to: "app/(app)/staff/[id]/offer-status"
      via: "import { offerLabel } (fuente única de la etiqueta derivada)"
      pattern: "offerLabel"
---

<objective>
## Phase Goal

**As a** productor de SOMOS DER (Franco), **I want to** ver un tablero por gig con qué roles están cubiertos y cuáles abiertos, y saltar en un tap a buscar reemplazo para un rol abierto sin volver a caer en los que ya oferté, **so that** manejo toda la cobertura de un evento desde una sola pantalla.

Slice vertical de STAT-02 (board de cobertura) + XTRA-02 (re-filtro next-candidate). El board NO tiene slots de rol predefinidos: la cobertura se DERIVA de las ofertas por gig (D-02) — accepted=cubierto, sent/viewed no vencida=pendiente, declined|vencida=abierto. Reusa `public.staff_app_offers` (ahora con staff_nombre por 05-01) + `public.staff_app_gigs` (para gigs sin ofertas) + `offerLabel()` (fuente única de la etiqueta derivada). El CTA "Buscar reemplazo" de un rol abierto es exactamente la puerta de entrada del re-filtro XTRA-02: abre la búsqueda con `?gig=<gigId>` y excluye los ya ofertados con el mismo patrón `.not("id","in",(...))` que crew_busy.

Purpose: cierra el hueco de UX real detectado en testing (Franco tenía que cazar candidato por candidato para ver estado). Es la superficie única de un vistazo.
Output: ruta nueva `/tablero` + nav en el header + re-filtro por gig en la home.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-status-board-extras-real-hire/05-CONTEXT.md
@.planning/phases/05-status-board-extras-real-hire/05-RESEARCH.md
@.planning/phases/04-accept-close-loop/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ruta /tablero (RSC + UI) con cobertura derivada por gig + nav en el header</name>
  <read_first>
    - app/(app)/layout.tsx (header actual con sólo "LABURO"; acá se agrega la nav Buscar / Tablero)
    - app/(app)/page.tsx (molde de RSC que lee una vista con el cliente autenticado + arma un client component)
    - app/(app)/staff/[id]/offer-status.tsx (offerLabel() y badgeClass — la etiqueta derivada a reusar; 'Vencida' = now()>expires_at)
    - app/(app)/staff/[id]/page.tsx (molde de tokens/clases de UI: cards rounded-xl bg-surface-1 border-border, pills, voseo)
  </read_first>
  <action>
    (a) `app/(app)/tablero/page.tsx` — server component. Con el cliente autenticado (createClient de @/lib/supabase/server), dos queries: `supabase.from("staff_app_gigs").select("id,title,starts_at,ends_at,venue_name,status").order("starts_at",{ascending:false})` y `supabase.from("staff_app_offers").select("id,gig_id,staff_profile_id,role,status,expires_at,sent_at,responded_at,gig_title,staff_nombre,staff_apellido")`. La RLS is_org_member scopea ambas al org. En el server, agrupar las ofertas por gig_id (Map), y hacer un left-join en memoria: por cada gig de staff_app_gigs armar `{ gig, offers: [...] }`; los gigs sin ofertas quedan con offers vacío. Pasar la estructura ya agrupada a `<GigBoard>`. No derivar acá el label (lo hace el client reusando offerLabel), pero sí ordenar los gigs (próximos primero). force-dynamic no es necesario (lectura autenticada normal).

    (b) `app/(app)/tablero/gig-board.tsx` — client component ("use client"). Recibe la lista de `{gig, offers}`. Por cada gig, una card/acordeón: título del gig + fecha corta (es-AR) + venue; un resumen chico "{X} cubiertos · {Y} pendientes · {Z} abiertos" derivado de las ofertas via offerLabel() (importado de `@/app/(app)/staff/[id]/offer-status`): Aceptada→cubierto, Enviada/Vista→pendiente, Rechazada/Vencida→abierto. Debajo, la lista de ofertas: cada fila con nombre del candidato (staff_nombre + staff_apellido), rol, y el badge de offerLabel() (reusar badgeClass o su equivalente). Para cada oferta cuyo label sea "Rechazada" o "Vencida" (rol abierto), mostrar un CTA "Buscar reemplazo" que es un <Link href={`/?gig=${gig.id}`}>. Un gig con offers vacío muestra "Sin ofertas todavía" + un CTA para ir al perfil/búsqueda. Copy en voseo, sin em dash. Usar los tokens/clases existentes (surface-1/2, border, text-fg-*). Motion opcional para el acordeón (import motion/react), micro y mobile-first.

    (c) `app/(app)/layout.tsx` — agregar una nav mínima en el header (o justo debajo) con dos entradas: "Buscar" (href="/") y "Tablero" (href="/tablero"), estilo pill/tab con los tokens actuales, targets ≥44px, mobile-first. No romper el lockup "LABURO" existente ni el max-w-[520px] del main.
  </action>
  <verify>
    <automated>cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && test -f "app/(app)/tablero/page.tsx" && test -f "app/(app)/tablero/gig-board.tsx" && grep -q "staff_app_offers" "app/(app)/tablero/page.tsx" && grep -q "staff_app_gigs" "app/(app)/tablero/page.tsx" && grep -q "offerLabel" "app/(app)/tablero/gig-board.tsx" && grep -q "gig=" "app/(app)/tablero/gig-board.tsx" && grep -q "/tablero" "app/(app)/layout.tsx" && npx tsc --noEmit 2>&1 | grep -v "node_modules" | grep -iE "tablero|gig-board" && echo "TSC-ERRORS" || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - /tablero lee gigs + offers con el cliente autenticado y agrupa por gig en el server.
    - El board muestra nombre del candidato + etiqueta derivada por offerLabel(); gigs sin ofertas aparecen.
    - Roles abiertos (Rechazada/Vencida) muestran CTA "Buscar reemplazo" → /?gig=<gigId>.
    - El header tiene nav Buscar / Tablero.
    - tsc sin errores nuevos en los archivos del board.
  </acceptance_criteria>
  <done>El board /tablero renderiza cobertura derivada por gig con nombres y CTA de reemplazo, alcanzable desde la nav.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-filtro XTRA-02 — PARAM.gig + exclusión de ya-ofertados en la home</name>
  <read_first>
    - lib/search-params.ts (PARAM, SearchFilters, parseSearchParams, buildQueryString — acá se agrega gig)
    - app/(app)/page.tsx (el patrón exacto de exclusión crew_busy: leer ids de una vista → query.not("id","in",`(${ids.join(",")})`))
  </read_first>
  <action>
    (a) `lib/search-params.ts` — agregar `gig: "gig"` a PARAM; agregar `gig: string | null` a SearchFilters; en parseSearchParams parsear `first(raw[PARAM.gig])` validando que sea un UUID (regex simple; si no matchea, null — no confiar en input crudo, V5); en buildQueryString, si filters.gig, `p.set(PARAM.gig, filters.gig)`. NO contar gig en activeFineFilterCount (es un modo de re-oferta, no un filtro fino visible).

    (b) `app/(app)/page.tsx` — tras parsear filters, si `filters.gig` está presente: query a `supabase.from("staff_app_offers").select("staff_profile_id").eq("gig_id", filters.gig)` → juntar los staff_profile_id (dedup) → si hay ids, `query = query.not("id", "in", `(${ids.join(",")})`)`. Mismo molde EXACTO que el bloque crew_busy (líneas 59-70). Esto es aditivo: convive con oficios/q/toggles/crew_busy. Si la query de ofertas falla, degradar en silencio (no romper la búsqueda; los ya-ofertados simplemente no se excluyen — comportamiento honesto, no bloquear). Opcional/discreción: cuando gig está presente, mostrar un chip/aviso "Buscando reemplazo — ocultando ya ofertados" con un link para limpiar (href="/"); mantenerlo simple.
  </action>
  <verify>
    <automated>cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && grep -q "gig" lib/search-params.ts && grep -q "PARAM.gig\|gig:" lib/search-params.ts && grep -q 'eq("gig_id"' "app/(app)/page.tsx" && grep -qE 'not\("id", ?"in"' "app/(app)/page.tsx" && npx tsc --noEmit 2>&1 | grep -v node_modules | grep -iE "search-params|\\(app\\)/page" && echo "TSC-ERRORS" || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - PARAM.gig existe, se parsea con validación UUID, y se serializa en buildQueryString.
    - Con ?gig=<id>, page.tsx excluye los staff_profile_id ya ofertados para ese gig via .not("id","in",...).
    - La exclusión es aditiva (convive con los demás filtros) y degrada en silencio si falla.
    - tsc sin errores nuevos.
  </acceptance_criteria>
  <done>Con ?gig=<gigId> la búsqueda oculta a los ya ofertados para ese gig, cerrando el loop desde el CTA del board.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated (Franco) → vistas | board y re-filtro leen staff_app_offers/gigs con el JWT (RLS is_org_member) |
| URL param gig → query | valor no confiable entra al filtro de exclusión |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-12 | Tampering | ?gig= inyecta al filtro | mitigate | validar UUID en parseSearchParams; el .in() usa ids devueltos por una vista RLS-scopeada, no el input crudo |
| T-5-13 | Information Disclosure | board expone ofertas de otro org | mitigate | staff_app_offers/gigs son security_invoker; RLS is_org_member scopea al org del caller |
| T-5-14 | Information Disclosure | fuga de datos privados (notas) en el board | accept | el board sólo lee staff_app_offers (sin notas); notas viven en otra vista, no se consultan acá |
| T-5-15 | Denial of Service | gig con miles de ofertas rompe el .in() | accept | pool y volumen de ofertas chicos (v1 interno); dedup de ids acota; degradación silenciosa si falla |
</threat_model>

<verification>
- Archivos del board existen; leen ambas vistas; reusan offerLabel; CTA a /?gig=.
- search-params tiene PARAM.gig validado; page.tsx excluye ya-ofertados con el patrón crew_busy.
- tsc sin errores nuevos en los archivos tocados.
</verification>

<success_criteria>
- Board /tablero muestra cobertura derivada por gig con nombres y roles abiertos accionables.
- Re-filtro por gig oculta a los ya ofertados (XTRA-02).
</success_criteria>

<output>
Create `.planning/phases/05-status-board-extras-real-hire/05-03-SUMMARY.md` when done.
</output>
