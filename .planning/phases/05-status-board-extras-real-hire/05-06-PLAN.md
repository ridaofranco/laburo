---
phase: 05-status-board-extras-real-hire
plan: 06
type: execute
wave: 3
depends_on: ["05-03", "05-04", "05-05"]
files_modified:
  - .planning/phases/05-status-board-extras-real-hire/05-USER-SETUP.md
autonomous: false
requirements: [SHIP-01, SHIP-02]
user_setup:
  - service: vercel
    why: "Deploy de LABURO a un Vercel project PROPIO (Hobby, gratis), separado de somosder-web"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: ".env.local (mismo valor; proyecto luillpzfqzbpoqkgvjuw)"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: ".env.local (público por diseño)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Project Settings → API (secreto; server-only)"
      - name: SITE_URL
        source: "La URL de producción final (ej. https://laburo.somosder.ar) — la usa el link mágico"
      - name: CRON_SECRET
        source: "Generar random; Vercel lo manda como Authorization: Bearer a /api/cron/reminders"
      - name: MAIL_FROM_NAME
        source: "SOMOS DER"
      - name: MAIL_FROM_ADDRESS
        source: "Dirección branded @somosder.ar (alineación DMARC)"
      - name: SMTP_HOST / SMTP_PORT / SMTP_SECURE / SMTP_USER / SMTP_PASSWORD
        source: "Panel Ferozo (gate de OFER-02 real + XTRA-03 recordatorios)"
    dashboard_config:
      - task: "Supabase Auth → URL Configuration: Site URL + Redirect URL (/auth/callback) del dominio de producción"
        location: "Supabase Dashboard → Authentication → URL Configuration"
      - task: "SPF + DKIM + DMARC TXT records para el dominio Ferozo/DER"
        location: "Panel DNS de Ferozo"
      - task: "NO setear LABURO_DEV_BYPASS (el dev-login debe 404 en prod)"
        location: "Vercel project env"

must_haves:
  truths:
    - "existe 05-USER-SETUP.md con el runbook exacto: env vars, config de Supabase Auth redirect URL, y la FORMA de los registros SPF/DKIM/DMARC"
    - "está confirmado que /dev-login 404ea en producción (doble guardia LABURO_DEV_BYPASS!=1 || NODE_ENV===production)"
    - "npm run build pasa localmente con vercel.json presente (el cron no rompe el build)"
    - "la app está deployada en un Vercel project propio con SPF/DKIM verificados (checkpoint Franco)"
    - "una persona real fue encontrada, ofertada, aceptó por el link, y quedó como crew en la app; el board muestra ese rol como cubierto (checkpoint Franco)"
  artifacts:
    - path: ".planning/phases/05-status-board-extras-real-hire/05-USER-SETUP.md"
      provides: "runbook de deploy Franco-gated: env vars, Supabase Auth, SPF/DKIM/DMARC, checklist de hire real"
      min_lines: 40
  key_links:
    - from: "producción (Vercel)"
      to: "SMTP Ferozo + SPF/DKIM"
      via: "MAIL_FROM_ADDRESS alineado al dominio con SPF/DKIM para no caer en spam"
      pattern: "SPF"
---

<objective>
## Phase Goal

**As a** SOMOS DER, **I want to** tener LABURO en producción con el email llegando bien y una contratación real completada de punta a punta, **so that** la app deja de ser un prototipo y pasa a ser la herramienta con la que Franco realmente contrata.

Slice de cierre de fase: SHIP-01 (deploy a Vercel project propio + SPF/DKIM) y SHIP-02 (1 hire real end-to-end). Ambos **FRANCO-GATED**: requieren la creación del Vercel project, credenciales SMTP, acceso DNS y una persona real. Este plan produce el runbook preciso (`05-USER-SETUP.md`), confirma la higiene pre-deploy (dev-login 404 en prod, build OK), y define los dos checkpoints humanos (deploy verificado + hire real verificado). **NO se deploya autónomamente** (D-07/D-08).

Purpose: llevar el ciclo completo a producción con deliverability real (el ítem históricamente sin probar) y validar con una contratación real.
Output: 05-USER-SETUP.md + app en producción + 1 hire real (crew en la app, board "cubierto").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-status-board-extras-real-hire/05-CONTEXT.md
@.planning/phases/05-status-board-extras-real-hire/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir 05-USER-SETUP.md + higiene pre-deploy (dev-login 404 + build)</name>
  <read_first>
    - app/dev-login/route.ts (confirmar la doble guardia: LABURO_DEV_BYPASS !== "1" || NODE_ENV === "production" → 404 en prod)
    - lib/email/mailer.ts (qué env de SMTP mira: SMTP_HOST/USER/PASSWORD, MAIL_FROM_ADDRESS/NAME)
    - lib/supabase/admin.ts (SUPABASE_SERVICE_ROLE_KEY + NEXT_PUBLIC_SUPABASE_URL)
    - 05-RESEARCH.md sección "Deploy (SHIP-01) — pasos exactos" (tabla de env vars + Supabase Auth redirect + SPF/DKIM/DMARC + checklist SHIP-02)
  </read_first>
  <action>
    (a) Crear `.planning/phases/05-status-board-extras-real-hire/05-USER-SETUP.md` como runbook Franco-gated, en secciones claras (voseo, sin em dash):
      1. **Crear el Vercel project** (propio, Hobby, separado de somosder-web; CLI logueado ridaofranco-8135).
      2. **Env vars** — tabla exacta con cada var, si es scriptable o secreto de Franco, y de dónde sale (copiar de la tabla de 05-RESEARCH): NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, SITE_URL (URL de PRODUCCIÓN, no localhost), CRON_SECRET (random nuevo), MAIL_FROM_NAME, MAIL_FROM_ADDRESS (branded @somosder.ar), SMTP_HOST/PORT/SECURE/USER/PASSWORD (Ferozo). Marcar NO setear LABURO_DEV_BYPASS y NO setear VERCEL_OIDC_TOKEN.
      3. **Supabase Auth redirect** — en Supabase Dashboard → Authentication → URL Configuration: Site URL = https://<dominio-prod>, Redirect URLs = https://<dominio-prod>/auth/callback (sin esto el login de Franco en prod falla; fácil de olvidar, no está en el código).
      4. **SPF/DKIM/DMARC** — la FORMA de los 3 registros TXT (SPF include del host que indique Ferozo, DKIM selector+clave que provee Ferozo, DMARC _dmarc con p=none rua). Aclarar que los VALORES exactos los da el panel Ferozo (A2 [ASSUMED]) y que el From debe alinear con el dominio SPF/DKIM. Verificar con mail-tester.com / MXToolbox tras propagar.
      5. **Checklist SHIP-02 (hire real)** — los 7 pasos: app deployada con SITE_URL correcta → SMTP funcionando (o reforzar por wa.me) → candidato real con email → Franco crea gig+oferta real → candidato abre /o/<token> y acepta con POST → accept_offer crea crew atómico → el board (/tablero) muestra ese rol como "cubierto".
      Anotar los supuestos [ASSUMED] A2 (valor SPF Ferozo) y A3 (dominio prod tipo laburo.somosder.ar), a confirmar por Franco.

    (b) Higiene pre-deploy autónoma: confirmar por lectura que app/dev-login/route.ts tiene la doble guardia (LABURO_DEV_BYPASS !== "1" || NODE_ENV === "production") y documentar la confirmación en el SUMMARY. Correr `npm run build` local para verificar que el build pasa con vercel.json presente (el cron no debe romperlo).
  </action>
  <verify>
    <automated>cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && f=.planning/phases/05-status-board-extras-real-hire/05-USER-SETUP.md && test -f $f && grep -qi "CRON_SECRET" $f && grep -qi "SPF" $f && grep -qi "DKIM" $f && grep -qi "Redirect URL" $f && grep -qi "LABURO_DEV_BYPASS" $f && grep -q 'NODE_ENV === "production"' app/dev-login/route.ts && npm run build > /tmp/laburo_build_05.log 2>&1 && echo OK || (tail -20 /tmp/laburo_build_05.log; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - 05-USER-SETUP.md cubre: crear Vercel project, env vars (con CRON_SECRET), Supabase Auth redirect, SPF/DKIM/DMARC, checklist de hire real.
    - Confirmado el doble guard del dev-login (404 en prod).
    - npm run build pasa con vercel.json presente.
  </acceptance_criteria>
  <done>Runbook de deploy completo y build verde; dev-login confirmado seguro en prod.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>05-USER-SETUP.md listo; la app buildea; el cron está cableado; dev-login 404ea en prod.</what-built>
  <how-to-verify>
    [FRANCO-GATED — SHIP-01] Seguí 05-USER-SETUP.md: 1) creá el Vercel project propio (Hobby); 2) cargá TODAS las env vars (incluida SITE_URL de producción y CRON_SECRET); 3) configurá Site URL + Redirect URL en Supabase Auth para el dominio de prod; 4) publicá los TXT SPF/DKIM/DMARC en el DNS de Ferozo con los valores del panel; 5) deploy. Verificá: la home carga logueado; /dev-login da 404; enviá una oferta de prueba a tu propio email y confirmá en mail-tester.com que SPF+DKIM+DMARC pasan (no cae en spam).
  </how-to-verify>
  <resume-signal>Escribí "deployado" con el dominio final + captura de mail-tester OK; o pegá el problema (spam, redirect, env faltante).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>App en producción con email verificado.</what-built>
  <how-to-verify>
    [FRANCO-GATED — SHIP-02] Contratación real end-to-end: 1) elegí un candidato real del pool con email válido; 2) creá un gig real + oferta real (monto informativo) y mandala; 3) que la persona abra el link /o/<token> desde el teléfono y ACEPTE con el botón; 4) verificá que quedó como crew en la app; 5) abrí /tablero y confirmá que ese rol del gig aparece como "cubierto". (El push a HITO NO va acá — es Fase 6; el gig queda hito_event_id NULL.)
  </how-to-verify>
  <resume-signal>Escribí "hire real OK" cuando el board muestre el rol cubierto; o describí dónde se trabó el flujo.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| producción → secretos | env vars secretas (service-role, SMTP, CRON_SECRET) viven sólo en Vercel |
| dominio → email | deliverability depende de SPF/DKIM/DMARC alineados |
| prod → dev-login | la ruta de bypass no debe existir en producción |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-26 | Elevation of Privilege | dev-login accesible en prod | mitigate | doble guardia LABURO_DEV_BYPASS!=1 || NODE_ENV===production (verificada); NO setear LABURO_DEV_BYPASS |
| T-5-27 | Information Disclosure | service-role/SMTP/CRON secreto expuesto | mitigate | secretos sólo en Vercel env server-side; nunca en NEXT_PUBLIC_*; documentado en el runbook |
| T-5-28 | Spoofing | emails DER spoofeados / a spam | mitigate | SPF + DKIM + DMARC publicados; From alineado al dominio verificado |
| T-5-29 | Spoofing | login prod mal configurado | mitigate | Supabase Auth Site URL + Redirect URL del dominio de prod |
| T-5-SC | Tampering | supply-chain en deploy | accept | Fase 5 no instala paquetes nuevos (package.json intacto); sin superficie de registry |
</threat_model>

<verification>
- 05-USER-SETUP.md cubre env + Auth redirect + SPF/DKIM/DMARC + checklist hire.
- dev-login doble-guard confirmado; npm run build verde.
- Checkpoints Franco: deploy + mail-tester OK; hire real + board "cubierto".
</verification>

<success_criteria>
- App en un Vercel project propio con SPF/DKIM verificados (SHIP-01).
- 1 contratación real completada, visible como crew + rol cubierto en el board (SHIP-02).
</success_criteria>

<output>
Create `.planning/phases/05-status-board-extras-real-hire/05-06-SUMMARY.md` when done.
</output>
