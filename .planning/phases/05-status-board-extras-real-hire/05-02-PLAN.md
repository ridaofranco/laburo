---
phase: 05-status-board-extras-real-hire
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/staff_app_0011_notes_ratings.sql
  - supabase/tests/staff_app_0011_notes_ratings_harness.sql
autonomous: false
requirements: [XTRA-01, XTRA-04]

must_haves:
  truths:
    - "existe staff_app.candidate_notes (is_favorite + note, UNIQUE(organization_id, staff_profile_id)) con RLS is_org_member para SELECT y is_org_writer para escritura"
    - "existe staff_app.staff_ratings (score int CHECK 1..5 + note, UNIQUE(organization_id, staff_profile_id, gig_id)) con la misma RLS"
    - "public.staff_app_candidate_notes y public.staff_app_staff_ratings son vistas security_invoker authenticated-only (anon revocado) que scopean por RLS al org del caller"
    - "public.staff_app_set_candidate_note y public.staff_app_rate_staff son upserts SECURITY DEFINER (ON CONFLICT DO UPDATE) que gatean is_org_writer con la org forzada, authenticated-only"
    - "NINGUNA de las tablas/vistas/RPCs nuevas es anon-callable ni aparece en ninguna superficie candidate-facing (get_public_offer / /o) — grep-negativo"
    - "get_advisors(security) no reporta ninguna clase de finding nueva vs el baseline de Fase 4"
  artifacts:
    - path: "supabase/migrations/staff_app_0011_notes_ratings.sql"
      provides: "2 tablas org-scoped + 2 vistas security_invoker + 2 RPCs upsert SECURITY DEFINER, todo con WR-05"
      contains: "candidate_notes"
    - path: "supabase/tests/staff_app_0011_notes_ratings_harness.sql"
      provides: "harness re-ejecutable seed→upsert→re-upsert(idempotencia)→assert→cleanup para notes y ratings"
      contains: "staff_app_rate_staff"
  key_links:
    - from: "public.staff_app_set_candidate_note(uuid,boolean,text)"
      to: "staff_app.candidate_notes"
      via: "INSERT ... ON CONFLICT (organization_id, staff_profile_id) DO UPDATE"
      pattern: "ON CONFLICT"
    - from: "public.staff_app_rate_staff(uuid,uuid,int,text)"
      to: "staff_app.staff_ratings"
      via: "INSERT ... ON CONFLICT (organization_id, staff_profile_id, gig_id) DO UPDATE + CHECK score 1..5"
      pattern: "is_org_writer"
---

<objective>
## Phase Goal

**As a** productor de SOMOS DER (Franco), **I want to** marcar candidatos como favoritos con notas privadas y calificarlos 1-5 después del evento, **so that** voy construyendo mi propio criterio de confianza sobre el pool sin que el candidato vea nada de eso.

Migración `staff_app_0011_notes_ratings`: la puerta de datos producer-only para XTRA-01 (favoritos + notas privadas, D-03) y XTRA-04 (rating post-evento, D-06). Dos tablas nuevas en `staff_app`, cada una con su vista `public` security_invoker (lecturas) y su RPC `public` SECURITY DEFINER upsert (escrituras), todo org-scoped y con el patrón WR-05 verbatim de 0008. **Aislamiento crítico (Pitfall 2): estas notas/ratings NUNCA se exponen a ninguna superficie por-token ni al candidato.**

Purpose: sin esta migración, el plan 05-04 (favoritos/notas + rating en el perfil) no tiene ni tablas ni RPCs. Es Ola 1 bloqueante para 05-04.
Output: 1 archivo de migración + 1 harness SQL. **[BLOCKING] aplicados y verificados EN VIVO por el orquestador** ANTES de la Ola 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-status-board-extras-real-hire/05-CONTEXT.md
@.planning/phases/05-status-board-extras-real-hire/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir la migración staff_app_0011 (candidate_notes + staff_ratings + vistas + RPCs)</name>
  <read_first>
    - supabase/migrations/staff_app_0008_create_offer.sql (molde EXACTO: RPC SECURITY DEFINER, org fija forzada, is_org_writer gate, search_path pinned, WR-05 REVOKE FROM PUBLIC,anon + GRANT authenticated, vista security_invoker + su GRANT/REVOKE)
    - supabase/migrations/staff_app_0007_read_layer.sql (patrón de vista security_invoker + GRANT SELECT ON la tabla base TO authenticated que la vista invoker necesita, Pitfall 5)
  </read_first>
  <action>
    Crear `supabase/migrations/staff_app_0011_notes_ratings.sql` con header comentado (estilo 0008: proyecto luillpzfqzbpoqkgvjuw, aplicado LIVE por el orquestador, notas de seguridad — enfatizar Pitfall 2: producer-only, jamás candidate-facing). Objetos:

    BLOQUE A — candidate_notes (XTRA-01):
    - Tabla `staff_app.candidate_notes`: id uuid PK default gen_random_uuid(); organization_id uuid NOT NULL REFERENCES staff_app.organizations(id) ON DELETE CASCADE; staff_profile_id uuid NOT NULL REFERENCES staff_app.staff_profiles(id) ON DELETE CASCADE; is_favorite boolean NOT NULL DEFAULT false; note text; updated_by uuid; updated_at timestamptz NOT NULL DEFAULT now(); UNIQUE(organization_id, staff_profile_id).
    - ENABLE ROW LEVEL SECURITY. Policy SELECT `candidate_notes_select` USING (staff_app.is_org_member(organization_id)); policy ALL `candidate_notes_write` USING (staff_app.is_org_writer(organization_id)) WITH CHECK (staff_app.is_org_writer(organization_id)).
    - Grants de tabla: `REVOKE ALL ON staff_app.candidate_notes FROM anon, authenticated;` luego `GRANT SELECT ON staff_app.candidate_notes TO authenticated;` (necesario para la vista security_invoker — Pitfall 5; la RLS igual scopea filas).
    - Vista `public.staff_app_candidate_notes WITH (security_invoker = true)` SELECT id, staff_profile_id, is_favorite, note, updated_at, organization_id FROM staff_app.candidate_notes. WR-05: `GRANT SELECT ... TO authenticated; REVOKE ALL ... FROM anon;`.
    - RPC `public.staff_app_set_candidate_note(p_staff_profile_id uuid, p_is_favorite boolean, p_note text) RETURNS jsonb` LANGUAGE plpgsql SECURITY DEFINER SET search_path = staff_app, public, pg_temp. Cuerpo: v_org constante fija aa29aa2f-...; gate `IF NOT staff_app.is_org_writer(v_org) THEN RETURN jsonb_build_object('ok',false,'reason','forbidden'); END IF;`; validar que el candidato pertenece al org (PERFORM 1 FROM staff_profiles WHERE id=p_staff_profile_id AND organization_id=v_org; NOT FOUND → reason candidate_not_found); INSERT INTO candidate_notes (organization_id, staff_profile_id, is_favorite, note, updated_by, updated_at) VALUES (v_org, p_staff_profile_id, coalesce(p_is_favorite,false), nullif(btrim(p_note),''), auth.uid(), now()) ON CONFLICT (organization_id, staff_profile_id) DO UPDATE SET is_favorite=excluded.is_favorite, note=excluded.note, updated_by=excluded.updated_by, updated_at=now(); RETURN jsonb_build_object('ok',true). WR-05: `REVOKE ALL ON FUNCTION ... FROM PUBLIC, anon; GRANT EXECUTE ON FUNCTION ... TO authenticated;`. COMMENT.

    BLOQUE B — staff_ratings (XTRA-04):
    - Tabla `staff_app.staff_ratings`: id uuid PK; organization_id uuid NOT NULL REFERENCES organizations ON DELETE CASCADE; staff_profile_id uuid NOT NULL REFERENCES staff_profiles ON DELETE CASCADE; gig_id uuid NOT NULL REFERENCES staff_app.gigs(id) ON DELETE CASCADE; score int NOT NULL CHECK (score BETWEEN 1 AND 5); note text; rated_by uuid; created_at timestamptz NOT NULL DEFAULT now(); UNIQUE(organization_id, staff_profile_id, gig_id).
    - Misma RLS (select is_org_member, write is_org_writer), mismos grants de tabla (REVOKE anon/authenticated, GRANT SELECT authenticated).
    - Vista `public.staff_app_staff_ratings WITH (security_invoker = true)` SELECT id, staff_profile_id, gig_id, score, note, created_at, organization_id. GRANT SELECT authenticated + REVOKE anon.
    - RPC `public.staff_app_rate_staff(p_staff_profile_id uuid, p_gig_id uuid, p_score int, p_note text) RETURNS jsonb` SECURITY DEFINER, search_path pinned. Gate is_org_writer(v_org fija); validar score BETWEEN 1 AND 5 (RETURN reason 'score_out_of_range' si no); validar candidato y gig pertenecen al org; INSERT ... ON CONFLICT (organization_id, staff_profile_id, gig_id) DO UPDATE SET score=excluded.score, note=excluded.note, rated_by=excluded.rated_by; RETURN ok. WR-05 authenticated-only. COMMENT.

    Recordatorio (research): NO usar las columnas legacy staff_profiles.rating / notas_internas — dejarlas intactas; las tablas nuevas org-scoped son el hogar correcto.
  </action>
  <verify>
    <automated>cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && f=supabase/migrations/staff_app_0011_notes_ratings.sql && grep -v '^--' $f | grep -q "CREATE TABLE staff_app.candidate_notes" && grep -v '^--' $f | grep -q "CREATE TABLE staff_app.staff_ratings" && grep -v '^--' $f | grep -q "CHECK (score BETWEEN 1 AND 5)" && grep -v '^--' $f | grep -c "ON CONFLICT" | awk '{if($1<2){print "FAIL upsert"; exit 1}}' && grep -v '^--' $f | grep -c "security_invoker" | awk '{if($1<2){print "FAIL views"; exit 1}}' && grep -v '^--' $f | grep -c "is_org_writer" | awk '{if($1<2){print "FAIL writer gate"; exit 1}}' && grep -v '^--' $f | grep -c "REVOKE ALL ON FUNCTION" | awk '{if($1<2){print "FAIL WR-05"; exit 1}}' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Ambas tablas con RLS + UNIQUE correcto + CHECK score 1..5.
    - Ambas vistas security_invoker authenticated-only.
    - Ambos RPCs upsert SECURITY DEFINER con is_org_writer gate, search_path pinned, WR-05 authenticated-only.
    - Ninguna concesión a anon en ningún objeto.
  </acceptance_criteria>
  <done>staff_app_0011 escrita con 2 tablas + 2 vistas + 2 RPCs upsert, todo org-scoped y WR-05.</done>
</task>

<task type="auto">
  <name>Task 2: Harness SQL re-ejecutable de notes + ratings (upsert idempotente + aislamiento)</name>
  <read_first>
    - supabase/tests/staff_app_0008_create_offer_harness.sql (molde seed→assert→cleanup con RAISE EXCEPTION)
  </read_first>
  <action>
    Crear `supabase/tests/staff_app_0011_notes_ratings_harness.sql` re-ejecutable: seedea un candidato + gig de prueba en la org fija; luego assert:
    (a) `staff_app_set_candidate_note(cand, true, 'nota privada')` devuelve ok:true y crea 1 fila; una segunda llamada con `false, 'otra nota'` NO crea fila nueva (UPSERT: sigue habiendo 1 fila para (org,cand)) y actualiza is_favorite/note.
    (b) la vista `public.staff_app_candidate_notes` devuelve esa fila con is_favorite y note actualizados.
    (c) `staff_app_rate_staff(cand, gig, 4, 'cumplidor')` devuelve ok:true; re-llamar con score 5 hace UPDATE (sigue 1 fila para (org,cand,gig)); un score=6 devuelve ok:false reason score_out_of_range y NO inserta.
    (d) (aislamiento) confirmar por consulta a los catálogos que anon NO tiene EXECUTE en ninguno de los 2 RPCs ni SELECT en las 2 vistas (has_function_privilege / has_table_privilege sobre 'anon').
    Cada assert con RAISE EXCEPTION. Cleanup total al final (DELETE de notes/ratings/gig/candidato de prueba). Comentar que el orquestador lo corre vía Supabase MCP.
  </action>
  <verify>
    <automated>cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && f=supabase/tests/staff_app_0011_notes_ratings_harness.sql && test -f $f && grep -v '^--' $f | grep -q "staff_app_set_candidate_note" && grep -v '^--' $f | grep -q "staff_app_rate_staff" && grep -v '^--' $f | grep -qi "RAISE EXCEPTION" && grep -v '^--' $f | grep -qi "has_function_privilege\|has_table_privilege" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Harness asserta upsert idempotente (1 fila tras 2 llamadas) para notes y ratings.
    - Harness asserta el rechazo de score fuera de rango.
    - Harness asserta el aislamiento anon (sin EXECUTE/SELECT).
  </acceptance_criteria>
  <done>El harness re-ejecutable existe y cubre upsert idempotente + CHECK + aislamiento anon.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>staff_app_0011 (SQL + harness) listo para aplicar en vivo.</what-built>
  <how-to-verify>
    [BLOCKING — ORQUESTADOR] Aplicar `staff_app_0011_notes_ratings.sql` vía Supabase MCP `apply_migration`. Correr el harness (todos los asserts pasan). `get_advisors(security)`: CERO clase de finding nueva (sin function_search_path_mutable en los 2 RPCs, sin security_definer_view en las 2 vistas, RLS habilitada en las 2 tablas). Confirmar grants: anon EXECUTE en set_candidate_note/rate_staff = false; anon SELECT en las 2 vistas = false; authenticated SELECT en las 2 vistas = true.
  </how-to-verify>
  <resume-signal>Escribí "aplicada" cuando la migración esté aplicada, el harness pase y get_advisors dé limpio; o pegá el finding a corregir.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated (Franco) → RPC/vista | escrituras/lecturas de notas y ratings, RLS org-scoped |
| candidato / anon → notas | NUNCA debe existir: superficie cero para el candidato |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-07 | Information Disclosure | nota privada leída por el candidato | mitigate | tablas/vistas authenticated-only + REVOKE anon; NO agregarlas a get_public_offer/ /o (grep-negativo en 05-04) |
| T-5-08 | Elevation of Privilege | objeto nuevo anon-callable por default privilege | mitigate | WR-05 REVOKE explícito por objeto + get_advisors post-apply |
| T-5-09 | Tampering | escritura por un no-writer / cross-org | mitigate | RPCs gatean staff_app.is_org_writer(org fija); org forzada, nunca de input |
| T-5-10 | Input Validation | score fuera de 1..5 | mitigate | CHECK (score BETWEEN 1 AND 5) en la tabla + re-validación en el RPC |
| T-5-11 | Spoofing | search-path hijack en SECURITY DEFINER | mitigate | SET search_path = staff_app, public, pg_temp pinned |
</threat_model>

<verification>
- grep gates confirman ambas tablas, CHECK, 2 upserts ON CONFLICT, 2 vistas security_invoker, 2 writer gates, WR-05 en ambos RPCs.
- El harness cubre upsert idempotente + rechazo de score + aislamiento anon.
- [BLOCKING] El orquestador confirma en vivo apply + harness + get_advisors + grants.
</verification>

<success_criteria>
- candidate_notes y staff_ratings existen con RLS org-scoped y UNIQUE correcto.
- Los 2 RPCs upsert son idempotentes, writer-gated, authenticated-only.
- Cero superficie anon; get_advisors limpio.
</success_criteria>

<output>
Create `.planning/phases/05-status-board-extras-real-hire/05-02-SUMMARY.md` when done.
</output>
