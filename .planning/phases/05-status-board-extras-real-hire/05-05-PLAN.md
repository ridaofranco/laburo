---
phase: 05-status-board-extras-real-hire
plan: 05
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - components/emails/reminder-email.tsx
  - app/api/cron/reminders/route.ts
  - vercel.json
autonomous: true
requirements: [XTRA-03]
user_setup:
  - service: vercel-cron
    why: "El cron real dispara sólo en producción (Vercel Hobby) y sólo envía con SMTP presente — ambos Franco-gated (SHIP-01)"
    env_vars:
      - name: CRON_SECRET
        source: "Generar random y setear en el Vercel project (Vercel manda el header Authorization: Bearer solo a las rutas cron)"

must_haves:
  truths:
    - "existe un route handler GET en /api/cron/reminders que rechaza (401) cualquier request cuyo Authorization no sea 'Bearer <CRON_SECRET>'"
    - "el route usa el cliente service-role (server-only) para llamar public.staff_app_offers_due_reminder y por cada oferta renderiza <ReminderEmail> + sendMail (mailer honesto)"
    - "el recordatorio NO incluye el link mágico ni ningún token (D-05/A1): nudgea al candidato a revisar el email original / responder"
    - "sin SMTP configurado el route es un no-op honesto (sendMail devuelve channel:none) y NUNCA tira — devuelve un JSON de resumen honesto"
    - "vercel.json define exactamente UN cron diario apuntando a /api/cron/reminders (Hobby: sub-diario rompe el deploy)"
  artifacts:
    - path: "app/api/cron/reminders/route.ts"
      provides: "route handler GET protegido por CRON_SECRET que dispara el recordatorio exactly-once vía el RPC"
      contains: "CRON_SECRET"
    - path: "components/emails/reminder-email.tsx"
      provides: "template react-email del recordatorio (sin link mágico), molde de offer-email.tsx"
      min_lines: 25
    - path: "vercel.json"
      provides: "un cron diario 0 9 * * * → /api/cron/reminders"
      contains: "/api/cron/reminders"
  key_links:
    - from: "app/api/cron/reminders/route.ts"
      to: "public.staff_app_offers_due_reminder"
      via: "createServiceRoleClient().rpc(...) tras validar el bearer CRON_SECRET"
      pattern: "staff_app_offers_due_reminder"
    - from: "app/api/cron/reminders/route.ts"
      to: "lib/email/mailer.ts"
      via: "render(<ReminderEmail/>) + sendMail() por cada oferta due"
      pattern: "sendMail"
---

<objective>
## Phase Goal

**As a** productor de SOMOS DER (Franco), **I want to** que a los candidatos les llegue exactamente UN recordatorio antes de que venza su propuesta, gratis, **so that** no pierdo contrataciones por ofertas que se enfrían sin perseguir a nadie.

Slice de XTRA-03 (vencimiento + UN recordatorio, D-05). Vercel Cron gratis (Hobby: 1/día) → route handler protegido por `CRON_SECRET` → cliente service-role → RPC `staff_app_offers_due_reminder` (05-01, exactly-once real por `reminded_at`) → mailer honesto de Fase 3. **DECISIÓN A1 BLOQUEADA: el recordatorio NO rota el token, NO reconstruye el link mágico (el raw no se persiste) y NO invalida el link original.** El email sólo nudgea: "tu propuesta para X vence el DD/MM, revisá el email original o respondé este mail". **El route es un no-op honesto hasta que Franco cargue el SMTP** (Franco-gated); se construye el código completo ahora, no necesita enviar de verdad para pasar.

Purpose: cierra el ciclo de vencimiento sin gasto y sin romper el link vigente.
Output: template + route + vercel.json. El cron real recién dispara en producción tras SHIP-01 + SMTP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-status-board-extras-real-hire/05-CONTEXT.md
@.planning/phases/05-status-board-extras-real-hire/05-RESEARCH.md
@.planning/phases/04-accept-close-loop/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Template react-email del recordatorio (sin link mágico)</name>
  <read_first>
    - components/emails/offer-email.tsx (molde EXACTO: tokens de marca inline, FONT_STACK, voseo sin em dash, children escapados por react-email)
  </read_first>
  <action>
    Crear `components/emails/reminder-email.tsx` como componente react-email, calcando el estilo de offer-email.tsx (mismos tokens SURFACE/ACCENT/FG inline, Container 480px, Section card). Props: `ReminderEmailProps { firstName: string; gigTitle: string; role: string; expiresText?: string | null }`. Copy en voseo argentino, cálido, SIN em dash: encabezado tipo "Hola {firstName}, tu propuesta está por vencer"; cuerpo con la línea `{role} · {gigTitle}` y "Vence el {expiresText}"; y un cierre que empuja a la acción SIN link accionable nuevo: "Revisá el email de la propuesta que te mandamos y confirmá desde ahí, o respondé este mail y lo vemos juntos." IMPORTANTE (A1): NO recibir ni renderizar ningún `link`/token — este email deliberadamente no trae link mágico (el original sigue válido). En react-email v2 `render()` es ASYNC (lo maneja el route). Sin HTML crudo sobre datos del pool: sólo children escapados.
  </action>
  <verify>
    <automated>cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && f=components/emails/reminder-email.tsx && test -f $f && grep -q "ReminderEmail" $f && ! grep -qiE "token|/o/|link:" $f && grep -qi "venc" $f && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - reminder-email.tsx existe, en voseo sin em dash, con firstName/gigTitle/role/expiresText.
    - NO contiene token ni link mágico (grep-negativo de token / /o/ / link:).
  </acceptance_criteria>
  <done>Template del recordatorio listo, sin link mágico (A1).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Route handler del cron (CRON_SECRET + service-role + RPC + sendMail) + vercel.json</name>
  <read_first>
    - lib/supabase/admin.ts (createServiceRoleClient — server-only, el único uso legítimo de service-role)
    - lib/email/mailer.ts (sendMail honesto: nunca tira, devuelve MailResult; smtpEnabled() gate)
    - app/(app)/staff/[id]/offer-actions.ts (molde de render(createElement(EmailComponent, props)) ASYNC + sendMail)
  </read_first>
  <action>
    (a) `app/api/cron/reminders/route.ts` — Route Handler App Router. Marcar `export const dynamic = "force-dynamic"` (nunca cachear). `export async function GET(request: Request)`:
    - PRIMERO validar auth: leer el header `authorization`; si `process.env.CRON_SECRET` no está seteado o el header no es exactamente `Bearer ${process.env.CRON_SECRET}`, devolver una Response 401 sin tocar nada más (T-5-20). Vercel manda ese header solo a las rutas de cron.
    - Instanciar `createServiceRoleClient()` (server-only; único lugar del repo además del webhook que usa service-role). Llamar `supabase.rpc("staff_app_offers_due_reminder", { p_within_days: 2 })`. Si error, loguear honesto y devolver un JSON `{ ok:false, error }` con 200 (no romper el scheduler).
    - Por cada oferta due (lista de { offer_id, email, first_name, gig_title, role, expires_at }): si no tiene email, saltear (contar skipped); si tiene, `render(createElement(ReminderEmail, { firstName, gigTitle, role, expiresText }))` (ASYNC) y `sendMail({ to, subject, html })` con subject en voseo "Tu propuesta está por vencer" + (gig ? ' · '+gig : ''). Acumular contadores { due, sent, failed, skipped }. NUNCA throw (el mailer es honesto; envolver el render en try/catch defensivo).
    - Devolver `Response.json({ ok:true, due, sent, failed, skipped, smtp: smtpEnabled() })`. Con SMTP ausente, sent=0 y smtp=false → no-op honesto, no error (D-05). Comentar el segundo trade-off (research Pitfall 1): `reminded_at` ya quedó marcado por el RPC aunque el SMTP falle; el recordatorio es best-effort (la oferta igual vence y se ve en el board).
    - NO usar el cliente autenticado ni anon acá; el cron no tiene sesión de usuario, por eso service-role + el RPC service_role-only.

    (b) `vercel.json` (archivo nuevo, hoy no existe): un único cron con path `/api/cron/reminders` y schedule `0 9 * * *`. UNA sola entrada diaria (Hobby: un schedule sub-diario como `0 * * * *` hace FALLAR el deploy — Pitfall 4). Vercel puede disparar en cualquier minuto de esa hora; por eso la ventana del RPC es "a ~2 días de vencer", no horas exactas.
  </action>
  <verify>
    <automated>cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && test -f "app/api/cron/reminders/route.ts" && test -f vercel.json && grep -q "CRON_SECRET" "app/api/cron/reminders/route.ts" && grep -q "401" "app/api/cron/reminders/route.ts" && grep -q "createServiceRoleClient" "app/api/cron/reminders/route.ts" && grep -q "staff_app_offers_due_reminder" "app/api/cron/reminders/route.ts" && grep -q "sendMail" "app/api/cron/reminders/route.ts" && grep -q "/api/cron/reminders" vercel.json && node -e "const c=require('./vercel.json'); if(!Array.isArray(c.crons)||c.crons.length!==1){process.exit(1)} const m=c.crons[0].schedule.split(' '); if(m.length!==5||m[0]==='*'||m[1]==='*'){process.exit(1)}" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - El route valida el bearer CRON_SECRET (401 si no coincide o falta el secret).
    - Usa service-role para llamar el RPC due-reminder; renderiza ReminderEmail + sendMail por oferta; nunca tira.
    - Devuelve un resumen honesto con smtp flag; sin SMTP es no-op limpio.
    - vercel.json tiene exactamente un cron diario (no sub-diario) a /api/cron/reminders.
  </acceptance_criteria>
  <done>El cron de recordatorio existe, protegido por CRON_SECRET, exactly-once, no-op honesto sin SMTP, un cron/día en vercel.json.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → /api/cron/reminders | endpoint público; sólo Vercel Cron con el bearer debe pasar |
| route (service-role) → RPC/DB | el cron corre sin sesión de usuario; usa service-role acotado |
| app → candidato (email) | el recordatorio no debe filtrar link/token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-20 | Spoofing / DoS | disparo externo del cron | mitigate | validar Authorization: Bearer $CRON_SECRET; 401 si falta o no coincide; sin CRON_SECRET seteado → 401 (fail-closed) |
| T-5-21 | Information Disclosure | service-role key filtrada | mitigate | createServiceRoleClient es "server-only"; service-role SÓLO en app/api/cron/*; nunca client-side |
| T-5-22 | Tampering | doble/replay del recordatorio | mitigate | exactly-once vive en el RPC (reminded_at IS NULL + UPDATE en la misma sentencia, 05-01) |
| T-5-23 | Information Disclosure | link mágico o token en el recordatorio | mitigate | ReminderEmail no recibe link/token (A1); grep-negativo en Task 1 |
| T-5-24 | Availability | SMTP caído tira el route / rompe el scheduler | mitigate | mailer honesto nunca tira; route envuelve el render; devuelve 200 con resumen aunque falle el envío |
| T-5-25 | Injection (email) | datos del pool en el HTML | mitigate | react-email escapa children; sin HTML crudo |
</threat_model>

<verification>
- Route: bearer CRON_SECRET (401), service-role, RPC due-reminder, sendMail, nunca throw, resumen honesto con smtp flag.
- Template: sin link/token, voseo sin em dash.
- vercel.json: un único cron diario válido a la ruta.
</verification>

<success_criteria>
- El recordatorio se dispara exactly-once vía cron protegido, sin romper el link original (A1).
- No-op honesto sin SMTP; envía de verdad recién con SMTP en producción.
</success_criteria>

<output>
Create `.planning/phases/05-status-board-extras-real-hire/05-05-SUMMARY.md` when done.
</output>
