---
phase: 04-accept-close-loop
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/(app)/staff/[id]/page.tsx
  - app/(app)/staff/[id]/offer-status.tsx
autonomous: true
requirements: [STAT-01]

must_haves:
  truths:
    - "En el perfil /staff/[id] Franco ve el estado de cada oferta del candidato: enviada / vista / aceptada / rechazada / vencida"
    - "El estado 'vencida' se deriva de now()>expires_at (no del enum, que nunca se auto-flipea)"
    - "La lectura está scopeada al org de Franco por la RLS de la vista security_invoker; un no-miembro no ve nada"
  artifacts:
    - path: "app/(app)/staff/[id]/offer-status.tsx"
      provides: "helper offerLabel + badge de estado por oferta (server-friendly, sin PII de otros)"
      exports: ["OfferStatusList"]
    - path: "app/(app)/staff/[id]/page.tsx"
      provides: "sección de ofertas del candidato leída de public.staff_app_offers"
      contains: "staff_app_offers"
  key_links:
    - from: "app/(app)/staff/[id]/page.tsx"
      to: "public.staff_app_offers"
      via: "supabase.from('staff_app_offers').select(...).eq('staff_profile_id', id) con el cliente autenticado"
      pattern: "staff_app_offers"
---

<objective>
El reflejo de estado para Franco (STAT-01, D-06): en el perfil del candidato `/staff/[id]` mostrar el estado de cada oferta (enviada / vista / aceptada / rechazada / vencida) leyendo la vista `public.staff_app_offers` que crea 04-01. `vista` la setea `get_public_offer` cuando el candidato abre el link (04-02); `vencida` se deriva en el render de now()>expires_at (el enum nunca se auto-flipea, no hay cron). Mínimo, donde Franco ya mira — un tablero completo por gig es Fase 5 (STAT-02).

Purpose: cierra el loop del lado de Franco — ve en vivo cómo cada oferta pasa de enviada a vista a aceptada/rechazada. Slice vertical paralelo a 04-02 (sin solape de archivos).
Output: 1 componente nuevo + edit del RSC de perfil.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-accept-close-loop/04-RESEARCH.md
@.planning/phases/04-accept-close-loop/04-01-PLAN.md
@app/(app)/staff/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Badge de estado de ofertas en /staff/[id] leyendo public.staff_app_offers</name>
  <read_first>
    - app/(app)/staff/[id]/page.tsx (el RSC de perfil: `createClient()`, `supabase.from('staff_app_profiles').select(...).eq('id', id).maybeSingle()`, el componente `Section`, tokens text-*/rounded-full/bg-surface-2, null-safety, voseo; dónde insertar la nueva sección — después del CTA "Crear oferta" o antes de "Oficios")
    - .planning/phases/04-accept-close-loop/04-RESEARCH.md sección "Reflejo de estado STAT-01 en /staff/[id]" (query exacta a la vista + función offerLabel con la derivación de 'Vencida' por now()>expires_at, mismo criterio que la página pública) y la definición de columnas de la vista en la migración 0009
    - .planning/phases/04-accept-close-loop/04-01-PLAN.md (columnas expuestas por public.staff_app_offers: id, gig_id, staff_profile_id, role, amount, conditions, status, expires_at, sent_at, viewed_at, responded_at, gig_title, organization_id)
  </read_first>
  <action>
    (a) Crear `app/(app)/staff/[id]/offer-status.tsx` (server component, sin `"use client"`): exportar `OfferStatusList` que recibe las ofertas ya cargadas y las renderiza, y un helper `offerLabel(o: { status: string; expires_at: string }): string` que deriva la etiqueta visible con ESTE orden (mismo criterio que la página pública, D-06): `accepted → "Aceptada"`, `declined → "Rechazada"`, `now()>expires_at → "Vencida"` (derivado, Pitfall 2: NO confiar en status==='expired'), `viewed → "Vista"`, else `"Enviada"`. Cada oferta es una fila cálida: `gig_title` + `role` + un badge con la etiqueta y un color por estado (verde aceptada, gris enviada/vista, ámbar vencida, apagado rechazada — usar los tokens de superficie del repo). Voseo, sin em dash. Si el candidato no tiene ofertas, no renderizar la sección (null-safety, molde del perfil).
    (b) Editar `app/(app)/staff/[id]/page.tsx`: tras cargar el perfil, agregar `const { data: offers } = await supabase.from("staff_app_offers").select("id,role,gig_title,status,expires_at,sent_at,viewed_at,responded_at").eq("staff_profile_id", id).order("sent_at", { ascending: false });` (cliente autenticado; la RLS is_org_member de la vista security_invoker scopea al org — un no-miembro obtiene 0 filas). Envolver la lista en un `<Section title="Ofertas">` con `<OfferStatusList offers={offers ?? []} />`, mostrado sólo si hay al menos una oferta. NO agregar campos de PII de otros candidatos; la vista ya viene scopeada. NO derivar 'vencida' del enum. NO tocar el schema staff_app directo (siempre la vista public).
  </action>
  <verify>
    <automated>test -f "app/(app)/staff/[id]/offer-status.tsx" && grep -q 'offerLabel' "app/(app)/staff/[id]/offer-status.tsx" && grep -q 'Vencida' "app/(app)/staff/[id]/offer-status.tsx" && grep -q 'expires_at' "app/(app)/staff/[id]/offer-status.tsx" && grep -q 'OfferStatusList' "app/(app)/staff/[id]/page.tsx" && grep -q 'staff_app_offers' "app/(app)/staff/[id]/page.tsx" && ! grep -q "status === 'expired'\|status === \"expired\"" "app/(app)/staff/[id]/offer-status.tsx" && npx tsc --noEmit 2>&1 | grep -c "app/(app)/staff/\[id\]" | grep -q '^0$' && echo OK</automated>
    <human-check>Con Franco logueado, abrir `/staff/[id]` de un candidato que tenga una oferta: se ve la sección "Ofertas" con el badge correcto. Tras que el candidato abra el link (04-02) el badge pasa de "Enviada" a "Vista"; tras aceptar, a "Aceptada"; una oferta con expires_at pasado muestra "Vencida" aunque su status siga 'sent'/'viewed'. (Verificación end-of-phase.)</human-check>
  </verify>
  <done>El perfil lee `public.staff_app_offers` scopeado por RLS, muestra por oferta gig_title+role+badge con la etiqueta derivada (enviada/vista/aceptada/rechazada/vencida), con 'vencida' calculada de now()>expires_at. Sin PII de terceros, sin acceso directo a staff_app, sin dependencia del enum 'expired'.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Franco (authenticated) → /staff/[id] → public.staff_app_offers | lectura de estado de ofertas scopeada por RLS al org de Franco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-12 | Information Disclosure | ver ofertas de otro org | mitigate | vista `security_invoker` + RLS `is_org_member` sobre `staff_app.offers`; un no-miembro obtiene 0 filas; el (app) layout ya gatea membresía (defensa en profundidad) |
| T-4-13 | Spoofing | mostrar estado equivocado ("vencida" mal) | mitigate | derivar `expired` de `now()>expires_at` en el render, NUNCA del enum status==='expired' (nunca se escribe, sin cron) |
</threat_model>

<verification>
- `/staff/[id]` muestra la sección "Ofertas" con el badge por estado leído de `public.staff_app_offers`.
- 'Vencida' se deriva de now()>expires_at; 'Vista' refleja el flip de get_public_offer.
- tsc sin errores en `app/(app)/staff/[id]/*`; grep-negativo de dependencia del enum 'expired' y de acceso directo a staff_app.
</verification>

<success_criteria>
Franco ve, en el perfil del candidato, el estado de cada oferta (enviada / vista / aceptada / rechazada / vencida) reflejando en vivo lo que pasa del lado del candidato (STAT-01).
</success_criteria>

<output>
Create `.planning/phases/04-accept-close-loop/04-03-SUMMARY.md` when done
</output>
