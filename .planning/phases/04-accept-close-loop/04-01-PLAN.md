---
phase: 04-accept-close-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/staff_app_0009_public_magic_link.sql
  - supabase/tests/staff_app_0009_public_magic_link_harness.sql
autonomous: false
requirements: [ACPT-01, ACPT-02, ACPT-03, STAT-01]

must_haves:
  truths:
    - "El cliente anon puede llamar public.staff_app_get_public_offer / staff_app_accept_offer / staff_app_decline_offer (no más PGRST106)"
    - "accept vía el wrapper anon crea exactamente 1 fila crew; un segundo accept devuelve {ok:false,reason:'invalid_or_expired'} sin insertar un segundo crew"
    - "authenticated puede SELECT public.staff_app_offers scopeado a su org; anon no puede leer esa vista"
    - "get_advisors(security) no reporta ninguna clase de finding nueva vs el baseline de Fase 1 (sin function_search_path_mutable nuevo, sin security_definer_view)"
  artifacts:
    - path: "supabase/migrations/staff_app_0009_public_magic_link.sql"
      provides: "3 wrappers public anon-callable a las RPCs staff_app + vista public.staff_app_offers + GRANT SELECT ON staff_app.offers TO authenticated + WR-05"
      contains: "staff_app_get_public_offer"
    - path: "supabase/tests/staff_app_0009_public_magic_link_harness.sql"
      provides: "harness re-ejecutable seed→assert→cleanup del ciclo completo por los wrappers"
      contains: "staff_app_accept_offer"
  key_links:
    - from: "public.staff_app_accept_offer(text,text)"
      to: "staff_app.accept_offer(p_token, p_user_agent)"
      via: "SELECT forward dentro del wrapper con search_path fijado"
      pattern: "staff_app\\.accept_offer"
    - from: "public.staff_app_offers"
      to: "staff_app.offers"
      via: "vista security_invoker con RLS is_org_member sobre la tabla base"
      pattern: "staff_app\\.offers"
---

<objective>
Migración `staff_app_0009_public_magic_link`: la plomería de datos que hace alcanzable todo el link mágico de Fase 4. Hoy las RPCs de Fase 1 (`get_public_offer`/`accept_offer`/`decline_offer`) viven en el schema `staff_app`, que NO está expuesto por PostgREST (PGRST106, verificado en 4 lugares del repo), así que `supabase.rpc(...)` desde el cliente anon falla. Este plan agrega (A) tres wrappers `public` anon-callable que reenvían a esas RPCs y (B) la vista `public.staff_app_offers` (security_invoker) + `GRANT SELECT ON staff_app.offers TO authenticated` para que Franco (autenticado) lea el estado de las ofertas (STAT-01, D-06). Reusa las RPCs existentes (D-01), no las reescribe.

Purpose: sin esta migración, la página pública `/o/[token]` (04-02) y el reflejo de estado en `/staff/[id]` (04-03) no tienen puerta de datos. Es la Ola 1 bloqueante.
Output: 1 archivo de migración + 1 harness SQL, aplicados y verificados EN VIVO por el orquestador (los subagentes no tienen las tools MCP de Supabase).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-accept-close-loop/04-RESEARCH.md
@.planning/phases/01-own-data-foundation/01-02-SUMMARY.md
@.planning/phases/03-create-send-offers/03-01-SUMMARY.md
@supabase/migrations/staff_app_0008_create_offer.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir la migración staff_app_0009 (3 wrappers public + vista de offers)</name>
  <read_first>
    - supabase/migrations/staff_app_0008_create_offer.sql (el patrón wrapper `public` a copiar VERBATIM: header WHY-public/PGRST106, WR-05 explícito REVOKE FROM PUBLIC/anon + GRANT scoped, security_invoker view, COMMENT ON)
    - .planning/phases/01-own-data-foundation/01-02-SUMMARY.md (firmas EXACTAS de las 3 RPCs inner: get_public_offer(text) VOLATILE, accept_offer(text,text) DOS args, decline_offer(text); anon ya tiene EXECUTE + USAGE sobre staff_app por 0003)
    - .planning/phases/04-accept-close-loop/04-RESEARCH.md sección "Migración recomendada: staff_app_0009" (Parte A wrappers + Parte B vista, forma exacta de columnas de la vista y grants)
  </read_first>
  <action>
    Crear `supabase/migrations/staff_app_0009_public_magic_link.sql` reflejando el molde de 0008. Header comentado que documente: WHY public (staff_app no es PostgREST-exposed, PGRST106 verificado Fase 1; el `GRANT EXECUTE ... TO anon` de 0003 es necesario pero inalcanzable sin exponer el schema; los wrappers son la única puerta correcta, mismo motivo que 0008 puso create_offer en public) y que NO se toca `pgrst.db_schemas` (proyecto compartido con HITO, afectaría a HITO).

    Parte A — tres funciones `public` que reenvían a `staff_app.*`, cada una LANGUAGE sql VOLATILE, SECURITY INVOKER, `SET search_path = staff_app, public, pg_temp`:
      - `public.staff_app_get_public_offer(p_token text) RETURNS jsonb` → `SELECT staff_app.get_public_offer(p_token);` (VOLATILE: el inner flipea sent→viewed, no marcar STABLE).
      - `public.staff_app_accept_offer(p_token text, p_user_agent text) RETURNS jsonb` → `SELECT staff_app.accept_offer(p_token, p_user_agent);` (DOS argumentos — reflejar la firma inner exacta, Pitfall 3).
      - `public.staff_app_decline_offer(p_token text) RETURNS jsonb` → `SELECT staff_app.decline_offer(p_token);`.
    SECURITY INVOKER es correcto y mínimo-privilegio porque anon ya tiene EXECUTE sobre las RPCs inner + USAGE sobre staff_app (otorgado en 0003): el wrapper no escala privilegios, sólo hace alcanzable la función. Agregar un comentario en el archivo que el orquestador confirmará con get_advisors; si apareciera un finding, el fallback es SECURITY DEFINER con el MISMO search_path fijado (dejar la nota, no cambiar preventivamente).
    WR-05 para los wrappers (estos SÍ deben ser anon-callable, a diferencia de create_offer): por cada función `REVOKE ALL ON FUNCTION public.<wrapper>(<args>) FROM PUBLIC;` y luego `GRANT EXECUTE ON FUNCTION public.<wrapper>(<args>) TO anon, authenticated;`. Usar las firmas de argumentos exactas en cada REVOKE/GRANT (accept lleva `(text, text)`).

    Parte B — vista de offers para STAT-01 (D-06):
      - `GRANT SELECT ON staff_app.offers TO authenticated;` (0007 otorgó SELECT sobre staff_profiles/members/crew/gigs pero NO offers; la vista security_invoker lo necesita).
      - `CREATE VIEW public.staff_app_offers WITH (security_invoker = true) AS SELECT o.id, o.gig_id, o.staff_profile_id, o.role, o.amount, o.conditions, o.status, o.expires_at, o.sent_at, o.viewed_at, o.responded_at, g.title AS gig_title, o.organization_id FROM staff_app.offers o JOIN staff_app.gigs g ON g.id = o.gig_id;` La RLS `offers_select` (is_org_member) de la tabla base scopea filas al org del caller.
      - WR-05 para la vista: `GRANT SELECT ON public.staff_app_offers TO authenticated;` + `REVOKE ALL ON public.staff_app_offers FROM anon;` (el default privileges del schema public auto-otorga anon → el REVOKE explícito es obligatorio).
    Agregar `COMMENT ON FUNCTION` a cada wrapper y `COMMENT ON VIEW public.staff_app_offers` describiendo propósito + WR-05, como en 0008. NO reescribir ni tocar las RPCs de staff_app (D-01). NO agregar reason-codes nuevos (el research recomienda manejar accepted/vencida por re-lectura en el frontend, sin tocar accept_offer). NO llamada ni columna a HITO (D-04: hito_event_id sigue NULL).
  </action>
  <verify>
    <automated>test -f "supabase/migrations/staff_app_0009_public_magic_link.sql" && grep -c -E 'staff_app_get_public_offer|staff_app_accept_offer|staff_app_decline_offer' supabase/migrations/staff_app_0009_public_magic_link.sql | grep -qv '^0$' && grep -q 'security_invoker = true' supabase/migrations/staff_app_0009_public_magic_link.sql && grep -q 'GRANT SELECT ON staff_app.offers TO authenticated' supabase/migrations/staff_app_0009_public_magic_link.sql && grep -q 'staff_app.accept_offer(p_token, p_user_agent)' supabase/migrations/staff_app_0009_public_magic_link.sql && grep -q 'REVOKE ALL ON public.staff_app_offers FROM anon' supabase/migrations/staff_app_0009_public_magic_link.sql && echo OK</automated>
  </verify>
  <done>El archivo existe con los 3 wrappers public (SECURITY INVOKER, search_path fijado, accept con 2 args), la vista security_invoker public.staff_app_offers, el GRANT base a authenticated sobre staff_app.offers, y WR-05 (REVOKE FROM PUBLIC/anon + GRANT scoped) por cada objeto. Sin cambios a las RPCs staff_app, sin exposición del schema, sin HITO.</done>
</task>

<task type="auto">
  <name>Task 2: Escribir el harness SQL re-ejecutable de staff_app_0009</name>
  <read_first>
    - supabase/tests/staff_app_0003_magic_link_rpcs_harness.sql (el molde de harness seed→assert→cleanup de Fase 1; leer side effects persistidos en statements SEPARADOS del call por el artefacto MVCC documentado en 01-02)
    - supabase/migrations/staff_app_0009_public_magic_link.sql (la Task 1: los nombres de wrapper/vista a ejercitar)
    - .planning/phases/01-own-data-foundation/01-02-SUMMARY.md (los 7 casos del harness original + la org fija aa29aa2f-4d34-4e53-b62c-7397e8a4d123 para sembrar filas)
  </read_first>
  <action>
    Crear `supabase/tests/staff_app_0009_public_magic_link_harness.sql`, versionado y re-ejecutable (seed → assert → cleanup, dejando staff_app.* vacío), que pruebe el ciclo completo A TRAVÉS de los wrappers `public` (no de las RPCs inner) y la visibilidad de la vista. Casos a incluir, con comentarios de valor esperado:
      1. Seed: insertar staff_profile + gig + offer (status 'sent', token_hash = sha256 de un raw token conocido) en el org fijo.
      2. Token malo: `public.staff_app_get_public_offer('garbage')` devuelve SQL NULL (no {ok:false}).
      3. Ver una oferta 'sent': `public.staff_app_get_public_offer(<raw>)` devuelve {ok:true, offer/gig/org/applicant} y (en statement separado) el status quedó persistido en 'viewed', viewed_at seteado.
      4. Accept: `public.staff_app_accept_offer(<raw>, 'test-ua')` devuelve {ok:true, crew_id}; verificar (statement separado) crew_count = 1 y offer.status = 'accepted'.
      5. Doble accept idempotente: segundo `public.staff_app_accept_offer(<raw>, 'test-ua')` devuelve {ok:false, reason:'invalid_or_expired'}; crew_count SIGUE en 1.
      6. Decline sobre otra oferta 'sent': `public.staff_app_decline_offer(<raw2>)` devuelve {ok:true, status:'declined'}.
      7. Derivación de vencimiento: sembrar una oferta con expires_at en el pasado y status 'sent'; documentar en comentario que get_public_offer la devuelve igual con status 'sent' (NO chequea expiry, NO auto-flip a 'expired') → "vencida" se deriva en el frontend por now()>expires_at (Pitfall 2). Verificar que accept sobre ella devuelve {ok:false, invalid_or_expired}.
      8. Vista offers: como el writer sembrado, `SELECT count(*) FROM public.staff_app_offers WHERE staff_profile_id = <seed>` es > 0; incluir comentario de que como anon la vista debe estar revocada (el orquestador lo confirma con has_table_privilege en la Task 3).
      Cleanup final: borrar todas las filas transitorias (crew/offers/gigs/staff_profiles del candidato de prueba) dejando las 4 tablas vacías, como el harness de 0003.
    El archivo es la fuente de verdad re-ejecutable; el orquestador lo corre en vivo en la Task 3.
  </action>
  <verify>
    <automated>test -f "supabase/tests/staff_app_0009_public_magic_link_harness.sql" && grep -q 'staff_app_get_public_offer' supabase/tests/staff_app_0009_public_magic_link_harness.sql && grep -q "staff_app_accept_offer" supabase/tests/staff_app_0009_public_magic_link_harness.sql && grep -q "invalid_or_expired" supabase/tests/staff_app_0009_public_magic_link_harness.sql && grep -q 'staff_app_offers' supabase/tests/staff_app_0009_public_magic_link_harness.sql && echo OK</automated>
  </verify>
  <done>El harness existe y cubre: token malo→NULL, ver→viewed flip, accept→accepted+1 crew, doble accept→invalid_or_expired idempotente, decline→declined, oferta vencida-por-fecha (status intacto, derivación en frontend), vista visible al writer. Deja staff_app.* vacío tras cleanup.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>La migración `staff_app_0009_public_magic_link.sql` (Task 1) y el harness (Task 2) están escritos en el repo pero NO aplicados. Los subagentes no tienen las tools MCP de Supabase; esta aplicación EN VIVO la hace la sesión orquestadora contra el proyecto `luillpzfqzbpoqkgvjuw` (mismo patrón que 03-01).</what-built>
  <how-to-verify>
    [BLOCKING] En la sesión orquestadora, con las tools MCP de Supabase:
    1. (Opcional, A2/A1 del research) Confirmar que `staff_app` sigue sin exponerse: un `supabase.rpc('get_public_offer', ...)` directo debería fallar con PGRST106 antes de aplicar; si ya funcionara, los wrappers son inofensivos igual.
    2. `apply_migration` con el contenido de `supabase/migrations/staff_app_0009_public_magic_link.sql`.
    3. Correr el harness `supabase/tests/staff_app_0009_public_magic_link_harness.sql` vía `execute_sql` y confirmar los 8 casos (get vía wrapper→payload, token malo→NULL, ver→viewed, accept→{ok:true}+1 crew, doble accept→{ok:false,invalid_or_expired} sin 2do crew, decline→declined, vencida-por-fecha, vista visible al writer). Leer los side effects persistidos en statements separados (artefacto MVCC de 01-02).
    4. Grants/hardening en vivo: `has_function_privilege('anon','public.staff_app_accept_offer(text,text)','EXECUTE') = true`; `has_function_privilege('anon','public.staff_app_get_public_offer(text)','EXECUTE') = true`; `has_table_privilege('anon','public.staff_app_offers','SELECT') = false`; `has_table_privilege('authenticated','staff_app.offers','SELECT') = true`.
    5. `get_advisors(security)`: DIFF contra el baseline de Fase 1 → CERO clase de finding nueva (sin `function_search_path_mutable` referenciando los 3 wrappers, sin `security_definer_view` sobre staff_app_offers porque es security_invoker). Si un wrapper dispara `function_search_path_mutable`, recrearlo como SECURITY DEFINER con el MISMO `SET search_path` y re-verificar.
    6. Limpiar cualquier fila transitoria del harness (staff_app.* de vuelta en 0 para el candidato de prueba).
  </how-to-verify>
  <resume-signal>Escribí "aplicada" cuando la migración esté aplicada, los 8 casos del harness pasen, los 4 grants sean correctos y get_advisors no tenga clase nueva. Si algo falla, describí el finding para revisar los wrappers (INVOKER→DEFINER).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente anon → wrappers public → RPCs staff_app | el candidato sin cuenta llega a las RPCs SÓLO por los 3 wrappers, por token |
| authenticated → vista public.staff_app_offers → staff_app.offers | Franco lee estado de ofertas, scopeado por RLS a su org |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Elevation | exponer staff_app entero vía PostgREST | mitigate | NO tocar `pgrst.db_schemas` (compartido con HITO); exponer exactamente 3 funciones + 1 vista en `public` |
| T-4-02 | Tampering | search-path hijack en los wrappers | mitigate | `SET search_path = staff_app, public, pg_temp` fijo en cada wrapper; orquestador confirma get_advisors sin `function_search_path_mutable` nuevo |
| T-4-03 | Information Disclosure | vista offers filtra filas de otro org | mitigate | vista `security_invoker` → RLS `is_org_member` sobre `staff_app.offers` scopea al org del caller |
| T-4-04 | Elevation | anon obtiene SELECT sobre offers por la vista public | mitigate | `REVOKE ALL ON public.staff_app_offers FROM anon` (el default del schema public auto-otorga anon) |
| T-4-05 | Information Disclosure | `GRANT SELECT ON staff_app.offers TO authenticated` demasiado amplio | accept | requerido por el security_invoker; las filas siguen scopeadas por la RLS `offers_select` de la tabla base, no hay bypass |
</threat_model>

<verification>
- El orquestador aplicó `staff_app_0009` en vivo; los 8 casos del harness pasan.
- `has_function_privilege('anon','public.staff_app_accept_offer(text,text)','EXECUTE')=true`; `has_table_privilege('anon','public.staff_app_offers','SELECT')=false`; `has_table_privilege('authenticated','staff_app.offers','SELECT')=true`.
- `get_advisors(security)` sin clase de finding nueva vs baseline Fase 1.
- Los dos archivos (.sql migración + .sql harness) existen en el repo y matchean lo aplicado.
</verification>

<success_criteria>
El cliente anon puede invocar los 3 wrappers public por token (ACPT-01/02/03 alcanzables); accept crea 1 crew idempotente; authenticated lee `public.staff_app_offers` scopeado a org y anon no (STAT-01 alcanzable); advisor limpio.
</success_criteria>

<output>
Create `.planning/phases/04-accept-close-loop/04-01-SUMMARY.md` when done
</output>
