---
phase: 03-create-send-offers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - lib/email/mailer.ts
  - lib/wa.ts
  - components/emails/offer-email.tsx
  - components/icons/whatsapp-glyph.tsx
autonomous: false
requirements: [OFER-02, OFER-03]

must_haves:
  truths:
    - "sendMail() returns a MailResult {ok, channel, error} and never throws — an SMTP failure surfaces ok:false, never a silent 250-OK"
    - "With no SMTP env set, smtpEnabled() is false and sendMail returns {ok:false, channel:'none'} rather than pretending success"
    - "OfferEmail renders to an HTML string (await render, async in react-email v2) with candidate data escaped by default"
    - "The wa.me link uses the REAL official WhatsApp glyph (#25D366), and waLink normalizes AR phone numbers (WR-06: strips a leading 0/15, prefixes 54) before building the deep link"
  artifacts:
    - path: "lib/email/mailer.ts"
      provides: "Ported HITO mailer, SMTP branch only (Resend disabled), honest MailResult, server-only"
      contains: "MailResult"
    - path: "components/emails/offer-email.tsx"
      provides: "react-email OfferEmail component (voseo, no em dash) rendered to HTML"
      exports: ["OfferEmail"]
    - path: "components/icons/whatsapp-glyph.tsx"
      provides: "Official WhatsApp glyph as a React component (verbatim path from WhatsAppFab.astro)"
      exports: ["WhatsAppGlyph"]
    - path: "lib/wa.ts"
      provides: "waLink/telLink with AR phone normalization (WR-06)"
      contains: "waLink"
  key_links:
    - from: "lib/email/mailer.ts"
      to: "process.env.SMTP_HOST/USER/PASSWORD"
      via: "nodemailer.createTransport"
      pattern: "createTransport"
    - from: "components/icons/whatsapp-glyph.tsx"
      to: "official WhatsApp path"
      via: "verbatim <path d> from WhatsAppFab.astro"
      pattern: "M16.004 0C"
---

<objective>
Assemble the SEND toolkit consumed by the offer flow: install `nodemailer` + `@react-email/components` (+ `@types/nodemailer`), port HITO's honest `mailer.ts` (SMTP-only), author the `OfferEmail` react-email component, extract the REAL WhatsApp glyph into a React component, and fix `waLink` to normalize AR phone numbers (WR-06). OFER-02 (email mechanism) + OFER-03 (wa.me building blocks).

Purpose: Everything hard here is already solved in the sibling repos — HITO's `mailer.ts` returns a `MailResult` and never throws (exactly D-02's honest feedback), and the official WhatsApp glyph already exists in `somosder-web/src/components/WhatsAppFab.astro`. This plan copies proven pieces; it invents nothing. The wa.me link is load-bearing this phase (the email may fail; WhatsApp is the fallback), so WR-06 normalization is fixed here, not deferred.
Output: A working mailer, a rendered offer email, an official-glyph component, and a normalized wa.me builder — all ready for 03-03 to wire.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-create-send-offers/03-RESEARCH.md
@.planning/phases/02-find-staff/02-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [PACKAGE GATE] Verify new packages before install</name>
  <what-built>Nothing yet — this gate precedes the install. The research marked nodemailer / @react-email/components / @types/nodemailer as legitimate but `slopcheck` was unavailable in the research environment, so provenance must be human-confirmed before `npm install` runs (project supply-chain rule T-3-SC).</what-built>
  <how-to-verify>
    1. Open https://npmjs.com/package/nodemailer — confirm it is the real long-lived package (~14M weekly downloads, repo github.com/nodemailer/nodemailer) and that `^9.0.3` is a current published version.
    2. Open https://npmjs.com/package/@react-email/components — confirm it is published by the Resend org (repo github.com/resend/react-email) and `^1.0.12` is current (it bundles `@react-email/render` 2.x whose `render()` is async).
    3. Open https://npmjs.com/package/@types/nodemailer — confirm it is the DefinitelyTyped types package (`^6`).
    None of the three has a network postinstall. Both nodemailer and the email lib are already used in HITO / somosder-web.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to install, or name any package that looks wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install deps, port mailer.ts (SMTP-only), fix wa.me AR normalization</name>
  <files>package.json, package-lock.json, lib/email/mailer.ts, lib/wa.ts</files>
  <read_first>
    - /Users/fridao/Proyectos/HITO-by-DER-main/lib/email/mailer.ts (the file to port verbatim — MailResult, resendEnabled/smtpEnabled, cascade; keep only the SMTP branch active)
    - /Users/fridao/Proyectos/SOMOS DER/somosder-web/src/lib/email.js (the Ferozo transport reference: host/port/secure/auth, lowercased SMTP_USER, connection timeouts, from header)
    - lib/wa.ts (current e164() strips non-digits only — the WR-06 bug: no leading-0/15 strip, no 54 country-code prefix)
    - .planning/phases/03-create-send-offers/03-RESEARCH.md § Q3 (transport + env vars) and § Q4 (WR-06)
    - STATE.md WR-06 note (waLink/telLink do not normalize AR phones)
  </read_first>
  <action>
    Run `npm install nodemailer@^9 @react-email/components@^1` and `npm install -D @types/nodemailer@^6`; commit the updated `package-lock.json`.
    Create `lib/email/mailer.ts` by porting HITO's `lib/email/mailer.ts` with `import "server-only"` at the top. Keep the `MailResult {ok, channel, error}` type and the never-throws contract exactly. Leave `RESEND_API_KEY` unset so `resendEnabled()` is false and `sendMail()` falls straight through to the SMTP branch; do NOT install Resend. The transport reads `SMTP_HOST`, `SMTP_PORT` (default 465), `SMTP_SECURE` (default true), `SMTP_USER` (lowercased, per email.js, to avoid 535 casing errors), `SMTP_PASSWORD`, with `connectionTimeout`/`greetingTimeout`/`socketTimeout` set (Ferozo is slow → fail fast). `from` header = `"${MAIL_FROM_NAME || 'SOMOS DER'}" <${MAIL_FROM_ADDRESS || SMTP_USER}>`. Add a `smtpEnabled()` helper that returns false when the SMTP env is absent so `sendMail()` returns `{ok:false, channel:'none'}` (honest — D-02) instead of throwing or faking success.
    Fix `lib/wa.ts` (WR-06): add an AR-normalizing helper used by `waLink` and `telLink` — strip all non-digits, then if the number starts with `54` leave it; else strip a leading `0` (trunk) and a leading `15` on the subscriber part where present, then prefix `54`. Keep `waLink` returning `https://wa.me/<digits, no +>?text=...` and `telLink` returning `tel:+<digits>`. Add short unit-level assertions as comments or a tiny inline test note documenting: `011 15 1234 5678` → `54111234...`, `+54 9 351 ...` → unchanged `54...`. Do NOT over-engineer libphonenumber (zero-budget, no new dep) — a deterministic AR-only heuristic is sufficient.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && grep -q "server-only" lib/email/mailer.ts && grep -q "MailResult" lib/email/mailer.ts && node -e "const {waLink}=require('./lib/wa.ts')" 2>/dev/null; grep -q "54" lib/wa.ts</automated>
  </verify>
  <acceptance_criteria>
    - `nodemailer@^9`, `@react-email/components@^1` in dependencies and `@types/nodemailer@^6` in devDependencies; `package-lock.json` updated and committed.
    - `lib/email/mailer.ts` starts with `import "server-only"`, exports a `sendMail` returning `MailResult`, and NEVER throws (no unguarded throw path); with SMTP env absent it returns `{ok:false, channel:'none'}`.
    - Transport uses `SMTP_HOST/PORT/SECURE/USER/PASSWORD` with `SMTP_USER` lowercased and connection timeouts set; `from` header uses `MAIL_FROM_NAME`/`MAIL_FROM_ADDRESS` with the documented fallbacks.
    - `lib/wa.ts` normalizes AR numbers before building the link: a `011 15 …` input yields a `54`-prefixed digits string with the leading 0 and 15 removed; an already-`54…` input is unchanged. `npm run typecheck` and `npm run lint` pass.
  </acceptance_criteria>
  <done>Deps installed; honest SMTP-only mailer and AR-normalized wa.me builder exist and typecheck. Resend is NOT wired.</done>
</task>

<task type="auto">
  <name>Task 3: OfferEmail react-email component + official WhatsApp glyph component</name>
  <files>components/emails/offer-email.tsx, components/icons/whatsapp-glyph.tsx</files>
  <read_first>
    - /Users/fridao/Proyectos/SOMOS DER/somosder-web/src/components/WhatsAppFab.astro (lines ~28-30: the official `<svg viewBox="0 0 32 32"><path d="M16.004 0C7.17 …"/>` — copy the path VERBATIM)
    - app/(app)/staff/[id]/quick-actions.tsx (the current generic lucide MessageCircle usage this glyph replaces in the offer button; do NOT edit this file here — 03-03 wires it)
    - app/globals.css (@theme tokens to echo in the email: surface-0 #0A0F1F, accent #2F80FF, fg #F2F5FA — the email is dark-brand consistent but self-contained inline styles)
    - .planning/phases/03-create-send-offers/03-RESEARCH.md § Q3 (react-email component shape, async render pitfall) and § Q4 (glyph reuse)
    - .planning/phases/02-find-staff/02-UI-SPEC.md § Copywriting / Voice & Tone (voseo, warm, no em dash)
  </read_first>
  <action>
    Create `components/icons/whatsapp-glyph.tsx` exporting `WhatsAppGlyph({ size = 20, className })` — an inline `<svg viewBox="0 0 32 32" fill="currentColor" aria-hidden="true">` wrapping the OFFICIAL `<path d="M16.004 0C7.17 …">` copied VERBATIM from WhatsAppFab.astro (the full `d` string ending `…1.886-.462z`). This is the real glyph D-03 requires — never a lucide/generic icon. The consumer sets `text-white` on a `bg-[#25D366]` button (wired in 03-03).
    Create `components/emails/offer-email.tsx` exporting `OfferEmail(props)` where props = `{ firstName, gigTitle, role, amount?, conditions?, whenText?, link }`. Build it with `@react-email/components` primitives (`Html lang="es"`, `Body`, `Container`, `Heading`, `Text`, `Section`, `Button`). Copy is Argentine voseo, warm, NO em dash: heading `Hola {firstName}, tenés una propuesta de laburo`, a line `{role} · {gigTitle}` (+ ` · {whenText}` when present), an informational pago line only when `amount` is set (label it as informativo), a conditions block only when present, and a primary `Button href={link}` labelled `Ver la oferta`. Dark-brand inline styles echoing the tokens (surface-0 background, accent button, fg text) but fully self-contained (email clients ignore external CSS). Rely on react-email's default child escaping for `firstName`/`conditions`/`gigTitle` (pool data) — never `dangerouslySetInnerHTML`. Note for the consumer (03-03): `render()` is ASYNC in react-email v2 → callers MUST `await render(<OfferEmail .../>)`.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && grep -q "M16.004 0C" components/icons/whatsapp-glyph.tsx && grep -q "1.886-.462z" components/icons/whatsapp-glyph.tsx && grep -q "export function OfferEmail\|export const OfferEmail" components/emails/offer-email.tsx && ! grep -q "dangerouslySetInnerHTML" components/emails/offer-email.tsx && ! grep -qi "—" components/emails/offer-email.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `components/icons/whatsapp-glyph.tsx` exports `WhatsAppGlyph` and contains the official path (starts `M16.004 0C`, ends `1.886-.462z`) copied verbatim — the glyph signature is present, not a lucide import.
    - `components/emails/offer-email.tsx` exports `OfferEmail`, uses `@react-email/components` primitives, renders role/gig/amount(when set)/conditions(when set)/CTA link, and contains NO em dash and NO `dangerouslySetInnerHTML`.
    - Amount and conditions rows are conditionally rendered (omitted when null), never shown as empty.
    - `npm run typecheck` and `npm run lint` pass.
  </acceptance_criteria>
  <done>The official-glyph component and the voseo offer email exist and typecheck; 03-03 can `await render(<OfferEmail/>)` and drop the glyph into the wa.me button.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | New third-party packages enter the build (supply chain) |
| candidate pool data → email HTML | Free-text pool fields (nombre/conditions) rendered into an email body |
| app server → Ferozo SMTP | Credentials + message leave the app over SMTP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-SC | Tampering | npm installs (nodemailer / @react-email / @types/nodemailer) | mitigate | Blocking-human package gate (Task 1) verifies provenance on npmjs.com before install; no network postinstall; all three are pre-existing in HITO/somosder-web |
| T-3-06 | Tampering | HTML/script injection via pool fields in the email | mitigate | react-email escapes children by default; no `dangerouslySetInnerHTML`; verified by grep gate |
| T-3-07 | Repudiation | silent success when Ferozo SMTP fails | mitigate | Ported `MailResult` never throws; `smtpEnabled()` false → `{ok:false, channel:'none'}`; consumer must check `ok` (enforced in 03-03) |
| T-3-08 | Info disclosure | SMTP creds committed to git | mitigate | Creds live only in env (03-USER-SETUP); mailer reads `process.env.*`; `.env.local` is git-ignored |
</threat_model>

<verification>
- Package gate approved before any install (autonomous:false).
- Mailer ported with honest MailResult (never throws; honest {ok:false} when unconfigured), wa.me AR-normalized, official glyph + voseo email authored — all typecheck + lint clean.
</verification>

<success_criteria>
- OFER-02 mechanism ready: an SMTP-only honest mailer + a rendered offer email.
- OFER-03 building blocks ready: the official WhatsApp glyph + an AR-normalized wa.me builder.
- No paid ESP; no generic WhatsApp icon; no em dash in the email copy.
</success_criteria>

<output>
Create `.planning/phases/03-create-send-offers/03-02-SUMMARY.md` when done.
</output>
