# Deferred Items — Phase 03

Out-of-scope discoveries found during execution. NOT fixed (not caused by the current plan's changes).

- **Lint warning (pre-existing):** `app/(app)/staff/[id]/cv-actions.ts:52` — "Unused eslint-disable directive (no problems were reported from 'no-control-regex')". Owned by Phase 2 code, unrelated to plan 03-02. Warning only, build not affected. Fix when that file is next touched.
