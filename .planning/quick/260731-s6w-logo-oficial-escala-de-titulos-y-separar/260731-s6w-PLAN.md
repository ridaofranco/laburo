---
phase: quick/260731-s6w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/laburo-wordmark.tsx
  - components/staff-nav.tsx
  - app/page.tsx
  - app/login/login-form.tsx
  - app/acceso-staff/staff-login-form.tsx
  - app/definir-contrasena/form.tsx
  - app/acceso-proveedor/[token]/page.tsx
  - app/o/[token]/page.tsx
  - app/baja/page.tsx
  - app/blog/chrome.tsx
  - app/onboarding-staff/page.tsx
  - app/sumate/page.tsx
  - app/not-found.tsx
  - app/globals.css
  - app/fichaje/page.tsx
  - app/editar-perfil-staff/page.tsx
  - app/panel-staff/page.tsx
  - app/sumate/registro-form.tsx
  - app/sumate/registro-rapido.tsx
  - app/sumate/sumate-client.tsx
  - app/(portal)/portal-nav.tsx
  - app/(portal)/layout.tsx
  - app/(portal)/dashboard/page.tsx
  - app/(portal)/leads/page.tsx
  - app/(portal)/leads/lead-actions.ts
  - app/(portal)/pagos/page.tsx
  - app/(portal)/rentabilidad/page.tsx
  - app/(portal)/config/page.tsx
  - app/(portal)/calendario/calendario-client.tsx
  - app/(portal)/confirmacion/confirmacion-view.tsx
  - app/(portal)/tablero/page.tsx
  - app/(portal)/tablero/gig-form.tsx
  - app/(portal)/favoritos/page.tsx
  - app/(portal)/notificaciones/page.tsx
  - app/(portal)/billetera/page.tsx
  - app/(portal)/mensajes/page.tsx
  - app/(portal)/buscar/candidate-card.tsx
  - app/(portal)/buscar/results-states.tsx
  - app/(portal)/buscar/search-client.tsx
  - app/(portal)/staff/[id]/page.tsx
  - app/(portal)/staff/[id]/oferta/page.tsx
  - lib/org.ts
  - supabase/migrations/staff_app_0044_org_plataforma.sql
  - supabase/tests/staff_app_0044_org_plataforma_harness.sql
autonomous: true
requirements: [QUICK-260731-s6w]

must_haves:
  truths:
    - "En toda pantalla donde antes decia LABURO escrito con la tipografia Syne, ahora se ve el PNG oficial del wordmark"
    - "Ningun archivo .tsx de app/ o components/ vuelve a renderizar el wordmark LABURO como texto"
    - "Ningun titulo del producto usa un tamano absoluto suelto de 40px o mas: todos salen de la escala"
    - "El parche de emergencia con !important que achicaba titulos abajo de 768px ya no existe, porque no hay nada que parchear"
    - "Una productora que NO es SOMOS DER entra al portal y no ve el item Leads en el menu"
    - "Una productora que NO es SOMOS DER que escribe /leads a mano recibe un 404, no la pantalla"
    - "Un llamado a las server actions de leads desde una org que no es la plataforma tira forbidden"
    - "El UUID de SOMOS DER no aparece escrito en ningun archivo de app/, lib/ ni components/"
    - "La barra lateral del portal dice el nombre de la productora del que mira, no una etiqueta generica en ingles"
    - "En /rentabilidad la metrica principal es lo que gana la productora, no la tasa de aceptacion de ofertas"
    - "En /pagos se distingue lo que la productora cobra de su cliente de lo que le debe al staff"
    - "El cobro de plataforma (SOMOS DER a la productora) NO se construye en este plan"
    - "El cobro al cliente final por MercadoPago sigue funcionando igual, sin un solo cambio"
  artifacts:
    - path: "components/laburo-wordmark.tsx"
      provides: "Componente unico del wordmark oficial sobre next/image"
      contains: "laburo-wordmark.png"
    - path: "app/globals.css"
      provides: "Escala tipografica del producto (t-display, t-stat, t-stat-sm, t-section)"
      contains: "@utility t-display"
    - path: "supabase/migrations/staff_app_0044_org_plataforma.sql"
      provides: "Columna es_plataforma en organizations + vista staff_app_my_orgs ampliada"
      contains: "es_plataforma"
    - path: "supabase/tests/staff_app_0044_org_plataforma_harness.sql"
      provides: "Harness SQL de la 0044, corre contra la base antes de dar la tarea por buena"
      contains: "es_plataforma"
    - path: "lib/org.ts"
      provides: "OrgActual.esPlataforma + gate exigirPlataforma()"
      contains: "exigirPlataforma"
  key_links:
    - from: "app/(portal)/layout.tsx"
      to: "app/(portal)/portal-nav.tsx"
      via: "props esPlataforma y orgNombre"
      pattern: "esPlataforma"
    - from: "app/(portal)/leads/lead-actions.ts"
      to: "lib/org.ts"
      via: "exigirPlataforma()"
      pattern: "exigirPlataforma"
    - from: "lib/org.ts"
      to: "public.staff_app_my_orgs"
      via: "select es_plataforma"
      pattern: "es_plataforma"
---

<objective>
Franco entro a la app como productora y no reconocio el producto. Tres cosas concretas lo causan, y las tres estan verificadas contra el codigo:

1. El wordmark LABURO se dibuja con la tipografia Syne en 16 lugares. La regla dura de Franco es que el wordmark va como imagen real. El PNG existe desde hace rato (`public/brand/laburo-wordmark.png`, 716x128 RGBA) y hoy solo lo usan los mails.
2. Hay 35 titulos con tamano absoluto de 40px para arriba (siete de 120px) portados uno por uno desde mockups de Stitch, sin ninguna escala que los ordene. El unico control que existe es un bloque de emergencia con `!important` en `app/globals.css` que los achica abajo de 768px, o sea un parche sobre el sintoma.
3. El portal es el panel de SOMOS DER operando como productora. `/leads` son las consultas comerciales que entran por la landing publica de LABURO: eso es de la plataforma, no de una productora cliente. En la base solo existen `owner` y `writer` dentro de una org, no existe el concepto de "esta org es la duena del producto".

Purpose: que Franco abra la app como productora y vea SU producto. Primero lo que se ve (logo y titulos), despues la separacion de roles.

Output: componente unico del wordmark, escala tipografica aplicada, columna `es_plataforma` con su gate en servidor, y `/rentabilidad` y `/pagos` reencuadrados hacia la productora.

FUERA DE ALCANCE, decidido por Franco y no se vuelve a preguntar:
- El cobro de plataforma (SOMOS DER le cobra a la productora). Queda para cuando defina el precio. Este plan solo deja el lugar limpio.
- El marketplace de trabajos (movimiento 3). Ya esta planificado aparte.
- El cobro al cliente final, que YA funciona en `app/(portal)/tablero/payment-actions.ts` y `app/api/mp/webhook/route.ts`. No se toca ni una linea.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/design-system/DESIGN-SYSTEM.md
@app/globals.css
@lib/org.ts
@app/(portal)/layout.tsx
@app/(portal)/portal-nav.tsx
@supabase/migrations/staff_app_0035_org_base.sql
@supabase/migrations/staff_app_0043_fix_publicar_faltan_array.sql
@supabase/tests/staff_app_0042_puerta_proveedor_harness.sql
@components/emails/encabezado.tsx
</context>

<reglas_duras>
- Wordmark: imagen real siempre, nunca texto estilizado.
- Animaciones: solo `motion`, import desde `motion/react`. Nada de GSAP, anime.js ni transiciones CSS a mano. Los sitios que hoy envuelven el wordmark en `motion.h1` con `{...up(0)}` conservan esa animacion tal cual.
- Mobile first.
- Presupuesto CERO: ninguna dependencia nueva. `next/image` y `motion` ya estan.
- Toda tabla o columna nueva pasa por RLS, y la migracion es aditiva.
- LECCION DEL 31/7, OBLIGATORIA: typecheck, lint y build NO ejecutan SQL. La 0043 existe porque un bug de tipos de plpgsql paso los tres gates y reventaba en produccion. La tarea 3 NO se da por terminada hasta correr su harness contra la base.
- Estilo Franco: nunca el guion largo como separador de frase, ni en el copy visible ni en los comentarios nuevos. Comas, puntos o parentesis.
</reglas_duras>

<tasks>

<task type="auto">
  <name>Tarea 1: el wordmark oficial reemplaza a las 16 copias escritas a mano</name>
  <files>
components/laburo-wordmark.tsx (nuevo),
app/page.tsx,
app/login/login-form.tsx,
app/acceso-staff/staff-login-form.tsx,
app/definir-contrasena/form.tsx,
app/acceso-proveedor/[token]/page.tsx,
app/o/[token]/page.tsx,
app/baja/page.tsx,
app/blog/chrome.tsx,
app/onboarding-staff/page.tsx,
app/sumate/page.tsx,
app/(portal)/buscar/results-states.tsx,
app/(portal)/portal-nav.tsx,
components/staff-nav.tsx,
app/globals.css
  </files>
  <action>
Crear `components/laburo-wordmark.tsx`. NO existe ningun componente asi todavia (lo unico que usa el PNG hoy es `components/emails/encabezado.tsx`, y ese es para correo con `@react-email` `Img`, no sirve para la web). Verificarlo antes de escribir.

API del componente, elegida para que los 16 sitios sean un cambio de una linea y para que los casos responsive salgan solos:

    export function LaburoWordmark({ className, priority }: { className?: string; priority?: boolean })

Envuelve `next/image` con `src="/brand/laburo-wordmark.png"`, `alt="LABURO"`, `width={716}` y `height={128}` (las medidas intrinsecas reales del archivo, verificadas). El tamano visible lo maneja el consumidor por `className` con `h-[Npx] w-auto`, que es lo que hace que un caso como 18px en movil y 26px en desktop sea `h-[18px] md:h-[26px] w-auto` sin agregar props numericas. Poner `priority` solo donde el wordmark esta arriba del pliegue (header de la landing, pantallas de login).

El PNG es RGBA con fondo transparente y letra off-white, asi que no lleva placa. No usar `unoptimized`.

Reemplazar en estos 16 renderizados. Los tamanos de abajo son los que hay hoy, y hay que conservarlos tal cual salvo donde se indica:

  1. `app/page.tsx:158` header de la landing, `h-[18px] md:h-[26px]`, con `priority`. El header padre tiene `mix-blend-difference`: dejarlo, el blend sigue funcionando sobre la imagen.
  2. `app/page.tsx:430` footer, `h-[22px]`.
  3. `app/login/login-form.tsx:81` wordmark monumental dentro de `motion.h1 {...up(0)}`. Conservar el `motion.h1`, sus props de animacion y el margen (`mb-[80px] md:mb-[120px]`); adentro va el componente con `h-[64px] md:h-[88px]`. Con `priority`.
  4. `app/acceso-staff/staff-login-form.tsx:204` igual que el anterior, `h-[64px] md:h-[88px]`, margen `mb-4`, con `priority`.
  5. `app/definir-contrasena/form.tsx:70` igual, `h-[64px] md:h-[88px]`, margen `mb-4`, con `priority`.
  6. `app/acceso-proveedor/[token]/page.tsx:45` header del Shell, `h-[24px]`.
  7. `app/o/[token]/page.tsx:68` header del Shell, `h-[24px]`.
  8. `app/baja/page.tsx:35` header del Shell, `h-[24px]`.
  9. `app/blog/chrome.tsx:17` header del blog, dentro del `Link href="/"`, `h-[22px]`.
 10. `app/blog/chrome.tsx:46` footer del blog, dentro del `Link href="/"`, `h-[22px]`.
 11. `app/(portal)/buscar/results-states.tsx:34` corona del estado vacio, `h-[20px] opacity-80`.
 12. `app/(portal)/portal-nav.tsx:74` barra lateral del portal, `h-[28px]` (hoy es Syne 32px; como imagen, 28 de alto es el equivalente optico).
 13. `components/staff-nav.tsx:41` barra lateral del staff, `h-[22px]`.
 14. `components/staff-nav.tsx:78` topnav movil del staff, `h-[22px]`.
 15. `app/onboarding-staff/page.tsx:69` header fijo, `h-[26px]`.
 16. `app/sumate/page.tsx:19` header, dentro del `Link href="/"`, `h-[22px]`.

Al reemplazar desaparecen `select-none` y `text-glow` de esos nodos (no aplican a una imagen). En los `h1`/`h2` que quedan vacios de texto, mantener el elemento si lleva la animacion o el margen; si queda solo como envoltorio inutil, colapsarlo.

DOS SITIOS QUE NO SE TOCAN, y hay que dejarlo escrito en el comentario del componente:
  - `app/blog/chrome.tsx:95`: es un eyebrow en `label-tech` (Geist, tracking amplio) que dice LABURO como etiqueta de seccion, no como lockup de marca.
  - `components/emails/welcome-legacy-email.tsx:140`: la marca mencionada adentro de una frase en prosa.

Ultimo paso: despues del reemplazo, `font-lockup` se queda sin un solo consumidor. Borrar el bloque `@utility font-lockup` de `app/globals.css` y ajustar el comentario de arriba que dice que se conservan los nombres "para no tocar los 14 archivos que los usan" (ya no aplica al lockup). NO tocar `text-glow` ni `box-glow`, que siguen en uso.
  </action>
  <verify>
    <automated>
cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && \
echo "G1 font-lockup sin consumidores (0):" && \
grep -rn "font-lockup" app components --include="*.tsx" | wc -l && \
echo "G2a lockup con punto renderizado como texto, sin los mails (0):" && \
grep -rniE "^[[:space:]]*LABURO\.[[:space:]]*$|>[[:space:]]*LABURO\.[[:space:]]*<" app components --include="*.tsx" | grep -v "components/emails/" | wc -l && \
echo "G2b lockup sin punto renderizado como texto, sin el eyebrow del blog (0):" && \
grep -rnE "^[[:space:]]*LABURO[[:space:]]*$" app components --include="*.tsx" | grep -v "blog/chrome.tsx" | wc -l && \
echo "G3 el componente usa el PNG real (>=1):" && \
grep -c "brand/laburo-wordmark.png" components/laburo-wordmark.tsx && \
echo "G4 nadie mas instancia el Image del wordmark suelto (1, solo el componente):" && \
grep -rln "brand/laburo-wordmark" app components --include="*.tsx" | grep -v "components/emails/" | wc -l && \
npm run typecheck && npm run lint && npm run build
    </automated>
  </verify>
  <done>G1=0, G2a=0, G2b=0, G3>=1, G4=1, y typecheck, lint y build en verde. En las 16 pantallas se ve el PNG oficial y no queda una sola letra del wordmark dibujada con la tipografia. Los baseline antes de tocar nada son G1=13, G2a=15, G2b=1, G4=0: si al empezar no dan eso, alguien ya movio algo y hay que revisar. Ojo con G2a: es case-insensitive a proposito, porque `app/(portal)/portal-nav.tsx` escribe "Laburo." con minusculas y una version sensible a mayusculas lo dejaba pasar. Y G2b existe separado porque `app/onboarding-staff/page.tsx` escribe el wordmark SIN punto: el unico "LABURO" sin punto que puede sobrevivir es el eyebrow de `app/blog/chrome.tsx`. NO filtrar por el archivo `blog/chrome.tsx` entero en G2a: ese archivo tiene DOS wordmarks reales (header y footer) que si hay que reemplazar.</done>
</task>

<task type="auto">
  <name>Tarea 2: una escala tipografica real, y se tira el parche de !important</name>
  <files>
app/globals.css,
app/not-found.tsx,
app/fichaje/page.tsx,
app/editar-perfil-staff/page.tsx,
app/onboarding-staff/page.tsx,
app/panel-staff/page.tsx,
app/sumate/registro-form.tsx,
app/sumate/registro-rapido.tsx,
app/sumate/sumate-client.tsx,
app/(portal)/dashboard/page.tsx,
app/(portal)/leads/page.tsx,
app/(portal)/pagos/page.tsx,
app/(portal)/rentabilidad/page.tsx,
app/(portal)/config/page.tsx,
app/(portal)/calendario/calendario-client.tsx,
app/(portal)/confirmacion/confirmacion-view.tsx,
app/(portal)/tablero/page.tsx,
app/(portal)/tablero/gig-form.tsx,
app/(portal)/favoritos/page.tsx,
app/(portal)/notificaciones/page.tsx,
app/(portal)/billetera/page.tsx,
app/(portal)/mensajes/page.tsx,
app/(portal)/buscar/candidate-card.tsx,
app/(portal)/buscar/search-client.tsx,
app/(portal)/staff/[id]/page.tsx,
app/(portal)/staff/[id]/oferta/page.tsx
  </files>
  <action>
Definir la escala en `app/globals.css` como cuatro `@utility`, al lado de `font-display` y `label-tech` (mismo lugar, mismo estilo de comentario). Se usa `clamp()` y no media queries porque el proyecto ya trabaja asi en 23 titulos y porque una escala fluida es mobile first de verdad, sin escalones bruscos. Cada utility trae familia, peso, `line-height` y `letter-spacing`, para que el consumidor deje de repetir `font-[family-name:var(--font-syne)] font-bold tracking-tighter leading-none` en cada nodo:

  - `t-display`: Syne 700, `clamp(34px, 6vw, 56px)`, line-height 1.05, letter-spacing -0.03em. Titulo de pantalla.
  - `t-stat`: Syne 800, `clamp(44px, 9vw, 80px)`, line-height 1, letter-spacing -0.04em. El numero heroe de un bento.
  - `t-stat-sm`: Syne 700, `clamp(28px, 5vw, 44px)`, line-height 1, letter-spacing -0.03em. Numero secundario.
  - `t-section`: Syne 600, `clamp(20px, 3vw, 28px)`, line-height 1.15, letter-spacing -0.02em. Cabecera de card o de seccion.

Aplicarlas reemplazando las clases arbitrarias. Hoy hay 35 usos de `text-[Npx]` con N mayor o igual a 40 (tres de ellos son wordmark y desaparecen en la tarea 1) y 23 usos de `text-[clamp(...)]`. La tabla de equivalencias, por rol y no por numero:

  - `text-[40px] md:text-[64px]` (8 sitios), `text-[40px] md:text-[72px]` (2), `text-[44px] md:text-[80px]` (1), `text-[40px] md:text-[120px]` (1) y los `text-[40px]` sueltos: van a `t-display`.
  - `text-[80px] md:text-[120px]` (4) y `text-[64px] md:text-[120px]` (2): van a `t-stat`.
  - `text-[48px] md:text-[64px]` (1), `text-[40px] md:text-[56px]` (3) y `text-[72px]` (la inicial del avatar en `buscar/candidate-card.tsx`): van a `t-stat-sm`.
  - `text-[28px] md:text-[56px]` (3, cabeceras de seccion en `panel-staff`): van a `t-section`.
  - Los `text-[clamp(...)]` en archivos dentro de alcance: por rol. Si es el titulo de la pantalla, `t-display`. Si es un numero grande (el 404 de `app/not-found.tsx`, que hoy es `clamp(4rem,20vw,180px)`), `t-stat`.

Al poner la utility, sacar del mismo `className` lo que ahora aporta ella: `font-[family-name:var(--font-syne)]`, `${SYNE}`, `font-bold` / `font-extrabold` / `font-semibold`, `tracking-*` y `leading-*`. Dejar el color, los margenes y el `uppercase` donde estaba (el `uppercase` es decision de cada pantalla, no de la escala).

ALCANCE, con su motivo, para que no sea un hueco silencioso: quedan FUERA `app/page.tsx` (la landing comercial) y `app/blog/**`. Son superficies de marketing, ya usan `clamp` responsable, y Franco no se quejo de ellas: su queja es sobre el producto. Anotar esa frontera en el comentario de la escala en `globals.css` y en el SUMMARY, como trabajo pendiente explicito, no como algo terminado.

Ultimo paso, y es el que prueba que la escala sirve: borrar entero el bloque `@media (max-width: 767px)` de `app/globals.css` que hoy achica `text-[120px]`, `[80px]`, `[64px]`, `[48px]`, `[40px]` y `[32px]` con `!important`. Ese bloque era el parche de emergencia del 28/7 sobre el sintoma. Cuidado: tambien pisaba `text-[32px]`, que NO entra en esta migracion (24 usos, muchos de cuerpo y no de titulo). Antes de borrarlo, revisar los usos de `text-[32px]` que sobrevivan dentro de los archivos de esta tarea y, si son titulo, pasarlos a `t-section`; si queda alguno legitimo de 32px que no sea titulo, conservar SOLO esa linea del bloque y borrar las otras cinco, y explicar por que en el comentario.
  </action>
  <verify>
    <automated>
cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && \
echo "G1 las cuatro utilities existen (4):" && \
grep -cE "^@utility (t-display|t-stat|t-stat-sm|t-section) \{" app/globals.css && \
echo "G2 tamanos absolutos >=40px fuera de landing y blog (0):" && \
grep -rnE "text-\[(4[0-9]|[5-9][0-9]|[0-9]{3,})px\]" app components --include="*.tsx" | grep -vE "^app/page\.tsx|^app/blog/" | wc -l && \
echo "G3 clamps sueltos fuera de landing y blog (0):" && \
grep -rn "text-\[clamp" app components --include="*.tsx" | grep -vE "^app/page\.tsx|^app/blog/" | wc -l && \
echo "G4 no quedan overrides de font-size con !important salvo el de 32px (0 o 1):" && \
grep -v '^[[:space:]]*[*/-]' app/globals.css | grep -c "px !important" && \
echo "G5 la escala se usa de verdad (>=20):" && \
grep -rcE "t-display|t-stat|t-section" app components --include="*.tsx" | awk -F: '{s+=$2} END {print s}' && \
npm run typecheck && npm run lint && npm run build
    </automated>
    <human-check>Abrir el portal en un viewport de 390px de ancho y recorrer /dashboard, /rentabilidad, /pagos, /tablero y /config: ningun titulo ocupa mas de dos renglones ni se come media pantalla.</human-check>
  </verify>
  <done>G1=4, G2=0, G3=0, G4 es 0 o 1 (y si es 1, es la linea de 32px con su explicacion), G5>=20, y typecheck, lint y build en verde. El bloque de emergencia con !important ya no existe porque no hay nada que parchear.</done>
</task>

<task type="auto">
  <name>Tarea 3: la org duena de la plataforma se separa de la productora cliente</name>
  <files>
supabase/migrations/staff_app_0044_org_plataforma.sql (nuevo),
supabase/tests/staff_app_0044_org_plataforma_harness.sql (nuevo),
lib/org.ts,
app/(portal)/layout.tsx,
app/(portal)/portal-nav.tsx,
app/(portal)/leads/page.tsx,
app/(portal)/leads/lead-actions.ts
  </files>
  <action>
El problema de fondo: en la base solo existen `owner` y `writer` dentro de una org. No hay forma de decir "SOMOS DER es la duena del producto y esta otra es un cliente". Por eso `/leads`, que son las consultas comerciales que entran por la landing publica de LABURO, aparece en el panel de cualquier productora.

NO se hardcodea el UUID de SOMOS DER. El 31/7 se desharcodeo la org de todo el proyecto (migracion 0035) y volver a escribirlo seria revertir ese trabajo. Se agrega un marcador en la tabla, igual que se hizo con `is_default`.

(a) Migracion `supabase/migrations/staff_app_0044_org_plataforma.sql`, aditiva de punta a punta, con la cabecera comentada al estilo de la 0035 y la 0043 (que problema resuelve, por que asi, que no toca):

  - `ALTER TABLE staff_app.organizations ADD COLUMN IF NOT EXISTS es_plataforma boolean NOT NULL DEFAULT false;`
  - Indice unico parcial `organizations_one_plataforma ON staff_app.organizations ((es_plataforma)) WHERE es_plataforma`, copiando el patron de `organizations_one_default` de la 0035: la base garantiza que haya UNA sola org de plataforma.
  - Backfill sin UUID: `UPDATE staff_app.organizations SET es_plataforma = true WHERE is_default;`. `is_default` ya resuelve a SOMOS DER como DATO desde la 0035, que es exactamente el punto.
  - `CREATE OR REPLACE VIEW public.staff_app_my_orgs` agregando `o.es_plataforma` AL FINAL de la lista de columnas. Postgres solo deja agregar columnas al final en un REPLACE, y el orden actual es `organization_id, role, org_name, org_slug, is_default, created_at`, asi que agregarla ultima funciona. Reemitir `REVOKE ALL ... FROM anon` y `GRANT SELECT ... TO authenticated` igual que hace la 0035, por las dudas.
  - `COMMENT ON COLUMN` explicando que marca a la org duena del producto (la que ve las pantallas de plataforma como /leads), que no la puede escribir nadie desde el cliente, y que el cobro de plataforma queda para cuando Franco defina el precio.
  - `NOTIFY pgrst, 'reload schema';` al final.

  NO agregar ninguna RPC ni policy de UPDATE. Sobre `staff_app.organizations` hoy existe solo la policy `organizations_select` (0001) y solo se granteo `SELECT` a authenticated (0035): eso ya deja `es_plataforma` fuera del alcance de escritura del cliente, y es la mitigacion de T-s6w-03. Verificarlo, no asumirlo.

(b) Harness `supabase/tests/staff_app_0044_org_plataforma_harness.sql`, modelado sobre `staff_app_0042_puerta_proveedor_harness.sql` (misma cabecera, mismo simulacro de identidad con `request.jwt.claims` a nivel SESION porque `auth.uid()` no resuelve bajo `execute_sql` crudo, misma nota de MVCC de leer el efecto en un statement separado, mismo paso 0 de limpieza para poder re-correrlo, misma limpieza final con reset del JWT). Sembrar una org de prueba que NO es la plataforma, con su miembro. Asserts:

  1. Hay exactamente UNA org con `es_plataforma = true`, y es la misma que tiene `is_default = true`.
  2. Intentar marcar una segunda org como plataforma falla por el indice unico (envolver en un `DO $$ ... EXCEPTION WHEN unique_violation ...` y devolver true).
  3. La vista `public.staff_app_my_orgs` expone la columna, y simulando al miembro de la org de prueba devuelve `es_plataforma = false`.
  4. Simulando a un miembro de la org plataforma, la vista devuelve `es_plataforma = true`.
  5. `authenticated` NO tiene privilegio de UPDATE sobre `staff_app.organizations` (leer `has_table_privilege('authenticated', 'staff_app.organizations', 'UPDATE')` y esperar false).

  Correr el harness contra la base con Supabase MCP `execute_sql` DESPUES de aplicar la migracion. La tarea no esta hecha hasta que los cinco asserts den lo esperado. Esta es la leccion de la 0043: typecheck, lint y build no ejecutan SQL.

(c) `lib/org.ts`:
  - Sumar `esPlataforma: boolean` a la interfaz `OrgActual` y a `FilaMyOrgs` (`es_plataforma: boolean | null`).
  - Pedir `es_plataforma` en el select de `staff_app_my_orgs`.
  - En el camino de fallback (`staff_app_my_membership`, el que cubre que el deploy llegue antes que la migracion) devolver `esPlataforma: false`. Documentarlo en el comentario del archivo: si la 0044 todavia no esta aplicada, el select nuevo falla, cae al fallback y nadie ve las pantallas de plataforma. Es la direccion segura del error (fail closed) y ademas el portal sigue cargando.
  - Agregar `export async function exigirPlataforma(): Promise<OrgActual>`: llama a `exigirOrg()` y, si `esPlataforma` no es true, tira `new Error("forbidden")`. Mismo contrato que `exigirOrg`, para que los consumidores no cambien de forma.

(d) `app/(portal)/leads/lead-actions.ts`: cambiar `exigirOrg()` por `exigirPlataforma()` en la linea 39. ESTE es el gate real: esconder un item del menu no es seguridad.

(e) `app/(portal)/leads/page.tsx`: al principio del server component, `const org = await orgActual(); if (!org?.esPlataforma) notFound();` (importar `notFound` de `next/navigation`). Se devuelve 404 y no un cartel de "no tenes permiso" porque el punto es que la pantalla no forme parte del producto de la productora. Conservar tal cual el manejo tolerante de que falte la migracion 0039 que ya tiene la pagina.

(f) `app/(portal)/layout.tsx`: pasar `esPlataforma={org.esPlataforma}` y `orgNombre={org.nombre}` a `<PortalNav>`.

(g) `app/(portal)/portal-nav.tsx`: recibir esas dos props.
  - Filtrar el item `Leads` del array `MAIN` cuando `esPlataforma` es false. Ojo: `MOBILE` hoy es `MAIN.slice(0, 5)` calculado a nivel de modulo; pasarlo adentro del componente para que corte sobre la lista ya filtrada, si no el bottom nav movil queda desalineado con el sidebar.
  - Reemplazar la bajada `Production Portal` (resto en ingles del mockup de Stitch) por el nombre de la productora: `orgNombre?.trim() || "Panel de productora"`. Este es el cambio de un renglon que mas hace por el reclamo de Franco: la barra lateral pasa a decir de quien es el panel.

Verificado y NO hace falta tocar: `/billetera` y `/mensajes` NO estan en el array `MAIN` del menu, asi que no hay nada que sacar. Son placeholders honestos y no aparecen en la navegacion. Dejarlos como estan y decirlo en el SUMMARY.
  </action>
  <verify>
    <automated>
cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && \
echo "G1 el UUID de DER no vuelve al codigo (0):" && \
grep -rn "aa29aa2f-4d34-4e53-b62c-7397e8a4d123" app lib components --include="*.ts" --include="*.tsx" | wc -l && \
echo "G2 el gate real esta en la server action (>=1):" && \
grep -c "exigirPlataforma" "app/(portal)/leads/lead-actions.ts" && \
echo "G3 la pagina de leads devuelve 404 al que no es plataforma (>=1):" && \
grep -c "notFound" "app/(portal)/leads/page.tsx" && \
echo "G4 el layout le pasa la senal al nav (>=1):" && \
grep -c "esPlataforma" "app/(portal)/layout.tsx" && \
echo "G5 ya no dice Production Portal (0):" && \
grep -c "Production Portal" "app/(portal)/portal-nav.tsx" && \
echo "G6 la migracion no hardcodea el UUID (0):" && \
grep -c "aa29aa2f-4d34-4e53-b62c-7397e8a4d123" supabase/migrations/staff_app_0044_org_plataforma.sql && \
npm run typecheck && npm run lint && npm run build
    </automated>
    <automated>HARNESS SQL, OBLIGATORIO Y NO OPCIONAL: aplicar supabase/migrations/staff_app_0044_org_plataforma.sql y despues correr supabase/tests/staff_app_0044_org_plataforma_harness.sql con Supabase MCP execute_sql contra el proyecto luillpzfqzbpoqkgvjuw. Los cinco asserts tienen que dar lo esperado (una sola org plataforma y coincide con is_default, la segunda es rechazada por el indice, la vista devuelve false para la org de prueba, true para la plataforma, y authenticated no tiene UPDATE sobre organizations). Sin esta corrida la tarea NO esta terminada: ni typecheck ni lint ni build ejecutan SQL, y eso es lo que dejo pasar el bug de la 0043.</automated>
    <human-check>Entrar al portal con el usuario de SOMOS DER: se ve Leads en el menu y la barra lateral dice SOMOS DER. Entrar con un usuario de otra productora: no se ve Leads, /leads a mano da 404, y la barra lateral dice el nombre de esa productora.</human-check>
  </verify>
  <done>G1=0, G2>=1, G3>=1, G4>=1, G5=0, G6=0, typecheck, lint y build en verde, y los cinco asserts del harness dando lo esperado corridos contra la base.</done>
</task>

<task type="auto">
  <name>Tarea 4: /rentabilidad, /pagos y el dashboard hablan como la productora</name>
  <files>
app/(portal)/dashboard/page.tsx,
app/(portal)/rentabilidad/page.tsx,
app/(portal)/pagos/page.tsx
  </files>
  <action>
Estas tres pantallas son porteos fieles de mockups de Stitch y hablan desde SOMOS DER o directamente en ingles. Se cambia el encuadre y el copy. NO se cambian las rutas (no vale la pena romper URLs) ni se agrega el cobro de plataforma.

(a) `app/(portal)/dashboard/page.tsx`. Es la primera pantalla que ve una productora y hoy dice `Dashboard Cliente` de eyebrow y `Resumen de Operaciones` de titulo, que es voz de agencia mirando a su cliente, no de la productora mirando su operacion. Cambiar por copy en la voz de ella (por ejemplo eyebrow `Tu operacion` y titulo `Tus eventos`, o la variante que quede mejor con el resto). El resto de la pantalla, con sus datos reales, no se toca.

(b) `app/(portal)/rentabilidad/page.tsx`. Hoy es el "Yield Analysis" de Stitch: la metrica dominante, a 120px, es la tasa de aceptacion de ofertas, que es una metrica de reclutamiento y no responde lo que una productora necesita saber. Lo que ella pregunta es cuanto gana. Reordenar el bento:
  - El bloque grande pasa a ser el MARGEN (la seccion que hoy vive mas abajo y calcula ingreso del cliente menos costo del staff), con su ingreso y su costo al lado como cifras de apoyo.
  - La tasa de aceptacion y las ofertas por mes bajan a metricas secundarias.
  - Conservar intacto el estado vacio honesto que ya existe cuando no hay `client_budget` cargado, con su invitacion a cargarlo desde el tablero. Ese texto es bueno y ya esta en la voz correcta.
  - Sumar una bajada abajo del titulo que diga en una linea que esto responde cuanto gana con sus eventos.
  - No cambiar ni una de las queries ni un solo calculo: los numeros ya son reales y RLS-scopeados. Es reordenamiento y copy.

(c) `app/(portal)/pagos/page.tsx`. Dos arreglos:
  - El comentario de cabecera dice literal "lo que Franco tiene comprometido con el staff". Esta escrito desde DER. Reescribirlo en la voz del producto (lo que la productora tiene comprometido con el staff que ya confirmo).
  - La pantalla mezcla sin separar dos plata distintas: los cobros al cliente (MercadoPago, lo que a ella le ENTRA) y lo comprometido con el staff (lo que a ella le SALE). Separarlas visualmente con dos encabezados de seccion explicitos (por ejemplo `Lo que cobras` y `Lo que le debes al staff`) y una bajada por seccion. La logica, las queries y el `PagoListoBoton` no se tocan.
  - Recordatorio duro: el cobro al cliente final por MercadoPago (`tablero/payment-actions.ts` y `api/mp/webhook/route.ts`) YA funciona y no se toca. Y el cobro de plataforma (SOMOS DER a la productora) NO se construye acá: Franco lo posterga hasta definir el precio. Si aparece la tentacion de dejar un lugar preparado, que sea un comentario y nada mas, nunca UI.

Al tocar estos tres archivos, respetar la escala tipografica de la tarea 2 (usar `t-display`, `t-stat`, `t-stat-sm` y `t-section`, no volver a poner tamanos absolutos) y la regla del guion largo en todo el copy nuevo.
  </action>
  <verify>
    <automated>
cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && \
echo "G1 pagos no habla desde Franco (0):" && \
grep -ci "franco" "app/(portal)/pagos/page.tsx" && \
echo "G2 el dashboard no dice Dashboard Cliente ni Resumen de Operaciones (0):" && \
grep -cE "Dashboard Cliente|Resumen de Operaciones" "app/(portal)/dashboard/page.tsx" && \
echo "G3 las tres pantallas usan la escala y no tamanos sueltos (0):" && \
grep -cE "text-\[(4[0-9]|[5-9][0-9]|[0-9]{3,})px\]|text-\[clamp" "app/(portal)/dashboard/page.tsx" "app/(portal)/rentabilidad/page.tsx" "app/(portal)/pagos/page.tsx" | awk -F: '{s+=$2} END {print s}' && \
echo "G4 el cobro al cliente que ya funciona quedo intacto (0 archivos cambiados):" && \
git diff --name-only -- "app/(portal)/tablero/payment-actions.ts" "app/api/mp/webhook/route.ts" | wc -l && \
echo "G5 sin guion largo usado como separador de frase (0, baseline 1):" && \
grep -c " — " "app/(portal)/dashboard/page.tsx" "app/(portal)/rentabilidad/page.tsx" "app/(portal)/pagos/page.tsx" | awk -F: '{s+=$2} END {print s}' && \
npm run typecheck && npm run lint && npm run build
    </automated>
    <human-check>Franco entra como productora: en /rentabilidad lo primero y mas grande que ve es cuanto gana, en /pagos distingue de una lo que cobra de lo que paga, y el dashboard le habla a ella. Ninguna de las tres se siente el panel de SOMOS DER.</human-check>
  </verify>
  <done>G1=0, G2=0, G3=0, G4=0, G5=0, y typecheck, lint y build en verde. Nota sobre G5: busca el guion largo RODEADO DE ESPACIOS, que es el uso como separador de frase que Franco prohibe. El baseline hoy es 1 y esta en la primera linea del comentario de cabecera de `app/(portal)/pagos/page.tsx`, que esta tarea reescribe igual. NO buscar el caracter suelto: los cinco `"—"` entrecomillados que hay en pagos y rentabilidad son el glifo de dato faltante, un uso legitimo que no se toca.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descripcion |
|----------|-------------|
| navegador de una productora cliente -> portal (server components y server actions) | Un miembro autenticado de una org que NO es la plataforma cruza aca. Es el caller que este plan aprende a distinguir |
| navegador -> `staff_app.organizations` via PostgREST | Aca vive `es_plataforma`, que es lo que decide si alguien es dueno del producto |
| build de Next -> `public/brand/laburo-wordmark.png` | Asset estatico que pasa a servirse en 16 pantallas |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposicion | Plan de mitigacion |
|-----------|-----------|------------|-------------|--------------------|
| T-s6w-01 | Elevation of Privilege | `app/(portal)/leads/lead-actions.ts` | mitigate | `exigirPlataforma()` reemplaza a `exigirOrg()` en la server action. El gate esta en el servidor, no en el menu. La vista `staff_app_producer_leads` es security_invoker y ya scopea por org, asi que el cruce de datos entre productoras estaba cerrado; esto cierra ademas la mutacion de estado de un lead que no es tuyo |
| T-s6w-02 | Information Disclosure | `app/(portal)/leads/page.tsx` | mitigate | La pagina llama `notFound()` si `esPlataforma` no es true. Se devuelve 404 y no un cartel de permiso denegado, para no confirmar que la ruta existe. Filtrar el item del menu es cosmetico y no cuenta como mitigacion |
| T-s6w-03 | Tampering | columna `staff_app.organizations.es_plataforma` | mitigate | No se crea ninguna RPC ni policy de UPDATE. Sobre `organizations` existe solo `organizations_select` (0001) y solo se granteo SELECT a authenticated (0035), asi que el cliente no puede escribir la columna. El harness lo verifica con `has_table_privilege` en vez de asumirlo |
| T-s6w-04 | Tampering | mas de una org marcada como plataforma | mitigate | Indice unico parcial `organizations_one_plataforma`, mismo patron que `organizations_one_default` de la 0035. Lo garantiza la base, no el codigo. El harness intenta la segunda y espera `unique_violation` |
| T-s6w-05 | Denial of Service | deploy aplicado antes que la migracion 0044 | accept | `lib/org.ts` ya tiene el fallback a `staff_app_my_membership` de la 0035 y devuelve `esPlataforma: false`. El portal sigue cargando y nadie ve las pantallas de plataforma hasta que la migracion aterrice: el error cae del lado seguro |
| T-s6w-06 | Spoofing | `next/image` sobre el wordmark | accept | Asset local en `public/`, mismo origen, sin remote patterns nuevos en `next.config.ts`. Sin superficie |
| T-s6w-SC | Tampering (cadena de suministro) | instalaciones de npm | accept | Cero dependencias nuevas. `next/image` y `motion` ya estan en `package.json`. No hay `npm install` en este plan, asi que no aplica el checkpoint de legitimidad de paquetes |
</threat_model>

<verification>
Al terminar las cuatro tareas, correr de una:

```
cd "/Users/fridao/Proyectos/SOMOS DER/staff-app"
npm run typecheck && npm run lint && npm run build
grep -rn "font-lockup" app components --include="*.tsx" | wc -l                                    # 0
grep -rniE "^[[:space:]]*LABURO\.[[:space:]]*$|>[[:space:]]*LABURO\.[[:space:]]*<" app components --include="*.tsx" | grep -v "components/emails/" | wc -l   # 0
grep -rnE "^[[:space:]]*LABURO[[:space:]]*$" app components --include="*.tsx" | grep -v "blog/chrome.tsx" | wc -l   # 0
grep -rnE "text-\[(4[0-9]|[5-9][0-9]|[0-9]{3,})px\]" app components --include="*.tsx" | grep -vE "^app/page\.tsx|^app/blog/" | wc -l   # 0
grep -rn "aa29aa2f-4d34-4e53-b62c-7397e8a4d123" app lib components --include="*.ts" --include="*.tsx" | wc -l   # 0
```

Y el harness de la 0044 corrido contra la base, con sus cinco asserts. Sin eso, la tarea 3 no cuenta como hecha.
</verification>

<success_criteria>
- Franco abre la app como productora y ve el logo oficial en toda pantalla donde antes habia letras.
- En un telefono de 390px ningun titulo del producto se come media pantalla, y el parche de `!important` ya no hace falta.
- Una productora que no es SOMOS DER no ve `/leads` en el menu, recibe 404 si la escribe a mano, y las server actions le dicen forbidden.
- La barra lateral dice el nombre de su productora.
- `/rentabilidad` responde cuanto gana y `/pagos` separa lo que cobra de lo que paga.
- El cobro al cliente final por MercadoPago sigue funcionando sin un solo cambio, y el cobro de plataforma no se construyo.
</success_criteria>

<output>
Crear `.planning/quick/260731-s6w-logo-oficial-escala-de-titulos-y-separar/260731-s6w-SUMMARY.md` al terminar.

Anotar explicitamente en el SUMMARY, como pendientes declarados y no como huecos:
1. `app/page.tsx` y `app/blog/**` quedaron fuera de la escala tipografica (superficies de marketing, ya usan clamp responsable).
2. `/billetera` y `/mensajes` siguen existiendo como placeholders honestos; se verifico que NO estan en el menu, asi que no hubo nada que sacar.
3. El cobro de plataforma (SOMOS DER le cobra a la productora) sigue sin construirse, por decision de Franco, hasta que defina el precio. La columna `es_plataforma` deja el lugar listo.
</output>
