---
phase: quick/260731-q8a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/sumate/actions.ts
  - app/sumate/registro-rapido.tsx
  - app/sumate/sumate-client.tsx
  - app/sumate/registro-form.tsx
  - app/sumate/page.tsx
  - app/acceso-staff/staff-login-form.tsx
autonomous: true
requirements: [QUICK-260731-q8a]
must_haves:
  truths:
    - "Alguien que nunca se registro llega a /acceso-staff y ve la salida a /sumate sin scrollear, con el mensaje de que alcanza con el CV"
    - "El texto de /acceso-staff sigue sin decir si el mail esta o no en el pool (ni antes ni despues de mandar el form)"
    - "En /sumate se puede terminar el registro adjuntando solo el CV, sin escribir ningun campo, cuando el parser saca nombre y email"
    - "Si el parser no encuentra nombre o email, /sumate pide SOLO eso"
    - "El consentimiento Ley 25.326 se pide siempre, en un click, y el servidor lo sigue exigiendo"
    - "El que entra por la via corta queda con perfil_confirmado_at en NULL, asi que el recordatorio de la 0034 lo agarra"
    - "El que llena el formulario completo (telefono + oficio + ubicacion) sigue quedando marcado como confirmado y no recibe el recordatorio"
    - "Un payload parcial (sin las claves opcionales) no rompe el server action"
  artifacts:
    - path: "app/sumate/registro-rapido.tsx"
      provides: "Camino corto: adjuntar CV, autolectura, confirmar lo que falte, consentimiento, enviar"
      min_lines: 90
    - path: "app/sumate/sumate-client.tsx"
      provides: "Switch de modo (rapido | completo) + pantalla de exito compartida"
      min_lines: 40
    - path: "app/sumate/actions.ts"
      provides: "registerApplicant tolerante a payload parcial + gate del mark_perfil_confirmado"
      contains: "perfilCompleto"
    - path: "app/acceso-staff/staff-login-form.tsx"
      provides: "Salida a /sumate arriba del formulario, sin oraculo de enumeracion"
  key_links:
    - from: "app/sumate/registro-rapido.tsx"
      to: "/api/parse-cv"
      via: "useCvAutofill (components/staff-form-shared)"
      pattern: "useCvAutofill"
    - from: "app/sumate/registro-rapido.tsx"
      to: "registerApplicant"
      via: "FormData con payload.modo = rapido + archivo cv"
      pattern: "modo: \"rapido\""
    - from: "app/sumate/actions.ts"
      to: "staff_app_mark_perfil_confirmado"
      via: "solo si perfilCompleto"
      pattern: "if \\(profileId && perfilCompleto\\)"
    - from: "app/acceso-staff/staff-login-form.tsx"
      to: "/sumate"
      via: "bloque arriba del formulario"
      pattern: "href=\"/sumate\""
---

<objective>
Dos cosas, una sola sesion.

**A. La salida deja de estar escondida en /acceso-staff.** Hoy el que no tiene ficha llega, no puede entrar, y el camino a /sumate esta al pie, despues del submit, de dos botones de cambio de modo y del de Google. Sube arriba y se vuelve protagonista, con el mensaje de que alcanza con adjuntar el CV. Lo que NO cambia: el texto sigue sin confirmar ni desmentir si el mail esta en el pool, y `app/acceso-staff/actions.ts` no se toca.

**B. /sumate se termina solo con el CV.** Camino corto por defecto: adjunto el CV, el parser que ya existe (`/api/parse-cv`) saca los datos, y lo unico que queda es el consentimiento. Nombre y email solo se piden si el CV no los trajo. El formulario largo sigue existiendo, a un click.

**Y el detalle que se pasa por alto:** hoy al final de `registerApplicant` se estampa `staff_app_mark_perfil_confirmado` porque "el que se registra por /sumate lleno el formulario ENTERO". Con la via corta esa premisa es falsa. Si no se contempla, esas fichas quedan a medias para siempre: la marca las saca del alcance del recordatorio de la migracion 0034 (que persigue justo a los `perfil_confirmado_at IS NULL`) y nadie las va a ir a buscar nunca.

Purpose: el registro deja de ser un muro de ~21 campos y 64 checkboxes, y el que cae en el login sin ficha ve su camino antes de frustrarse. Sin regalar informacion de quien esta en el pool.
Output: 2 archivos nuevos en /sumate, 4 editados, cero dependencias nuevas, cero migraciones.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@app/sumate/actions.ts
@app/sumate/registro-form.tsx
@app/sumate/page.tsx
@app/acceso-staff/staff-login-form.tsx
@components/staff-form-shared.tsx
@app/api/parse-cv/route.ts
@supabase/migrations/staff_app_0034_recordatorio_perfil.sql
</context>

<contratos_verificados>
Leido del codigo real, no inventado. Usar estos nombres exactos:

- `registerApplicant(formData: FormData): Promise<{ ok: boolean; reason?: string }>` en `app/sumate/actions.ts`. Lee `formData.get("payload")` (JSON string) y `formData.get("cv")` (File opcional).
- El server hoy exige tres cosas: `input.nombre`, `input.email`, `input.consentimiento === true`. Todo el resto entra al RPC con `|| null`.
- RPC: `supabase.rpc("staff_app_register_applicant", { p_nombre, p_apellido, p_email, ... })`, anon, ya granteada.
- Email normalizado con `.trim().toLowerCase()` antes del RPC. NO tocar esa normalizacion: toda la resolucion de identidad del staff hace `where lower(email) = ...`.
- `useCvAutofill()` en `components/staff-form-shared.tsx` devuelve `{ status, run }`, con `status: "idle" | "loading" | "ok" | "error" | "nokey"` y `run(file: File, oficios: string[]): Promise<CvParsed | null>`.
- `CvParsed` tiene: `nombre, apellido, email, telefono, ciudad, pais, linkedin_url, portfolio_url, anios_experiencia, experiencia_detalle, oficios[]`.
- Catalogo de oficios: `oficios` de `@/lib/data/oficios`, aplanado con `oficios.flatMap(g => g.items.map(it => it.es))`.
- `YEARS_OPTS` y `paises` ya se usan como allowlist en el form largo (`registro-form.tsx` lineas 68-71). Misma guarda en el camino corto.
- Migracion 0034: `staff_app_mark_perfil_confirmado(p_profile_id uuid)` estampa `perfil_confirmado_at` si estaba NULL. El batch `staff_app_perfil_reminder_batch` selecciona `bienvenida_enviada_at` de hace 5+ dias AND `perfil_confirmado_at IS NULL` AND `recordatorio_perfil_enviado_at IS NULL`. O sea: NO estampar la marca es exactamente lo que hace que el recordatorio agarre a la ficha. No hace falta migracion nueva.
- El proyecto no tiene runner de tests (no hay vitest ni playwright en package.json y la regla es CERO dependencias nuevas). Los gates automaticos son `npm run typecheck`, `npm run lint` y greps sobre el codigo. La prueba de navegador va como human-check.
</contratos_verificados>

<tasks>

<task type="auto">
  <name>Task 1: el server acepta el payload corto y deja de mentir sobre el perfil confirmado</name>
  <files>app/sumate/actions.ts</files>
  <action>
Tres cambios en `registerApplicant`, en este orden.

**1.1 Tolerar el payload parcial.** Hoy `RegisterInput` declara todos los campos como string y el codigo hace `input.apellido.trim()`, `input.oficios.length`, `input.donde_trabajar.length`. El camino corto manda un payload sin la mayoria de esas claves y eso reventaria el action con un TypeError. Cambiar `RegisterInput` a `Partial` sobre los campos opcionales (nombre, email y consentimiento siguen siendo lo unico esperado) y agregar dos helpers locales (no exportados, el archivo es "use server" y solo puede exportar funciones async):
- `txt(v: unknown): string` devuelve `typeof v === "string" ? v.trim() : ""`.
- `arr(v: unknown): string[]` devuelve `Array.isArray(v) ? v.filter(x => typeof x === "string") : []`.
Reemplazar cada `input.X.trim() || null` por `txt(input.X) || null` y cada `input.X.length ? input.X : null` por una const previa del tipo `const oficiosSel = arr(input.oficios)`. Los tres booleanos (`disponibilidad_finde`, `disponibilidad_viajar`, `movilidad_propia`) pasan a `input.X === true`. `input.experiencia` se manda tal cual solo si es boolean, si no `null`. `input.fecha_nacimiento` igual: string no vacio o `null`.
No tocar la normalizacion del email ni el orden de las validaciones ni el manejo del CV huerfano.

**1.2 Filtrar los oficios contra el catalogo.** En el camino corto el array de oficios viene de un modelo de lenguaje, no de checkboxes. Importar `oficios` de `@/lib/data/oficios`, armar el set plano una sola vez a nivel de modulo (`const OFICIOS_VALIDOS = new Set(oficios.flatMap(g => g.items.map(it => it.es)))`) y quedarse solo con los valores que estan en el set antes de mandar `p_oficios`. Comentar por que: es salida de IA, no input de usuario.

**1.3 El gate del `mark_perfil_confirmado`.** Agregar al payload la clave `modo?: "rapido" | "completo"`. Antes del bloque que llama la RPC, calcular:
- `const modo = input.modo === "completo" ? "completo" : "rapido";` (ausente o desconocido cae en "rapido": si nos equivocamos, que sea mandando un recordatorio de mas, nunca abandonando una ficha).
- una funcion local `perfilUtil(...)` que devuelve true solo si hay telefono, hay al menos un oficio (del set filtrado) u `oficios_otro`, y hay provincia o ciudad. Esos tres son los que la busqueda y el contacto necesitan de verdad.
- `const perfilCompleto = modo === "completo" && perfilUtil(...);`
Y cambiar la condicion final a exactamente `if (profileId && perfilCompleto) {`.

Reescribir el comentario de ese bloque (hoy dice "El que se registra por /sumate lleno el formulario ENTERO"). El texto nuevo tiene que decir: la marca se estampa SOLO cuando la persona mando el formulario largo y quedo una ficha usable; el que entra por la via corta queda con `perfil_confirmado_at` en NULL a proposito, porque su ficha esta a medias y el recordatorio de la 0034 tiene que agarrarlo a los 5 dias. Sin guion largo en el comentario.

No tocar el bloque de la bienvenida ni `staff_app_mark_bienvenida`: el welcome sale igual por los dos caminos, y encima es lo que le da el ancla `bienvenida_enviada_at` que el recordatorio necesita para contar los dias.
  </action>
  <verify>
    <automated>cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && npm run typecheck && grep -q 'if (profileId && perfilCompleto)' app/sumate/actions.ts && grep -q 'OFICIOS_VALIDOS' app/sumate/actions.ts && grep -cq 'input.apellido.trim()' app/sumate/actions.ts; test $? -ne 0 && echo GATES_OK</automated>
    <automated>cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && test "$(grep -c '—' app/sumate/actions.ts)" = "0" && echo SIN_EM_DASH</automated>
  </verify>
  <done>`registerApplicant` no explota con un payload que solo trae `{ email, consentimiento, modo }`; los oficios que no estan en el catalogo se descartan; `staff_app_mark_perfil_confirmado` solo se llama cuando `modo === "completo"` y la ficha tiene telefono + oficio + ubicacion.</done>
</task>

<task type="auto">
  <name>Task 2: /sumate se termina solo con el CV</name>
  <files>app/sumate/registro-rapido.tsx, app/sumate/sumate-client.tsx, app/sumate/registro-form.tsx, app/sumate/page.tsx</files>
  <action>
**2.1 `app/sumate/sumate-client.tsx` (nuevo, "use client").** Componente `SumateClient` que maneja dos estados: `modo: "rapido" | "completo"` (arranca en "rapido") y `done: null | "rapido" | "completo"`.
- Si `done` no es null, renderiza `RegistroListo` (ver 2.2) con ese modo.
- Si no, renderiza `<RegistroRapido onDone={() => setDone("rapido")} onFormularioLargo={() => setModo("completo")} />` o `<RegistroForm onDone={() => setDone("completo")} onVolver={() => setModo("rapido")} />`.
Antes de escribirlo, confirmar con `grep -rn "RegistroForm" app/ components/` que el unico que lo importa es `app/sumate/page.tsx`; si aparece otro consumidor, avisar antes de cambiarle la firma.

**2.2 `RegistroListo`.** Extraer tal cual la pantalla de exito que hoy vive en el branch `if (done)` de `registro-form.tsx` (lineas 120-138: el `CheckCircle2`, el titulo "Quedaste registrado/a", el parrafo y el link a /acceso-staff). Ponerla en `sumate-client.tsx` y exportarla desde ahi. Recibe `modo`. Diferencia: cuando `modo === "rapido"` el parrafo agrega que los datos salieron del CV y que cuando pueda entre a revisarlos desde su perfil (eso es verdad: el recordatorio de la 0034 lo va a invitar a hacerlo). Mantener el `motion.div` con `initial/animate/transition` que ya tiene.

**2.3 `app/sumate/registro-rapido.tsx` (nuevo, "use client").** El camino corto. Estructura, de arriba a abajo:
- Encabezado corto: "Sumate con tu CV" + una linea diciendo que no hace falta llenar nada, que con el CV alcanza. Reusar el `SYNE` y la paleta del form largo para no romper la estetica.
- **El adjunto.** `<label htmlFor="cv-rapido">` visible + `<input id="cv-rapido" ref type="file" accept=".pdf,.doc,.docx,image/*">`. Debe ser un input nativo enfocable con teclado y con foco visible (`focus-visible:ring`), nada de div con onClick. En `onChange`, si hay archivo, disparar el autollenado de una (`runAutofill(file, allOficios)`), sin boton intermedio: adjunto el CV y listo. Un solo intento por seleccion (nada de reintento automatico: cada llamada gasta cuota de Gemini y la ruta tiene freno de 6 por minuto por IP).
- **El estado de lectura, anunciado.** Un `<p role="status" aria-live="polite">` que diga "Leyendo tu CV..." mientras `afStatus === "loading"`, y el resultado cuando termina. El bloque del adjunto lleva `aria-busy={afStatus === "loading"}`. Sin esto un lector de pantalla no se entera de nada.
- **Lo que el CV trajo.** Si el parseo devolvio nombre y email, mostrarlos como texto ("Te registramos como {nombre}, {email}") con un boton "Corregir" que revela los dos inputs. Si falta alguno de los dos, mostrar SOLO el input que falta, con su label. Si el parseo fallo (`afStatus === "error"` o `"nokey"`), mostrar los dos inputs vacios con una linea honesta: no pudimos leer el CV, escribi tu nombre y tu mail y listo, el CV se sube igual. Nunca pedir mas que nombre y email en este camino.
- **El consentimiento.** El mismo checkbox y el mismo texto legal exacto que el form largo (lineas 289-294 de `registro-form.tsx`), un solo click, al lado del boton. Es Ley 25.326 y el server lo rechaza sin el, no se puede sacar ni esconder.
- **Enviar.** Boton deshabilitado mientras `sending` o `afStatus === "loading"`. Arma el payload con lo que haya: nombre, email (los dos, obligatorios en el cliente igual que hoy), mas todo lo que el parser saco (`telefono`, `ciudad`, `pais_residencia` solo si esta en `paises`, `linkedin_url`, `portfolio_url`, `anios_experiencia` solo si esta en `YEARS_OPTS`, `experiencia_detalle`, `experiencia: d.experiencia_detalle ? true : null`, `oficios` filtrados contra el catalogo aplanado), `consentimiento: consent` y `modo: "rapido"`. `fd.append("payload", JSON.stringify(payload))` y `fd.append("cv", file)`. Llama `registerApplicant` y en `res.ok` llama `onDone()`; si no, `toast.error(res.reason || "No se pudo enviar.")`.
- **La salida al form largo.** Un link discreto al pie: "Prefiero completar todo a mano" que llama `onFormularioLargo()`.
- Mobile-first: una sola columna, targets tactiles grandes, el adjunto arriba de todo. La mayoria entra del telefono y saca una foto del CV.
- Animacion de entrada con `motion` importado de `"motion/react"` (misma libreria que ya usa el form largo). Nada de transiciones CSS a mano.

**2.4 `app/sumate/registro-form.tsx` (editar).** Sacarle el estado `done` y el branch de exito (ahora vive en `sumate-client.tsx`). Nueva firma: `RegistroForm({ onDone, onVolver }: { onDone: () => void; onVolver: () => void })`. En `onSubmit`, agregar `modo: "completo"` al payload y llamar `onDone()` en vez de `setDone(true)`. Agregar arriba del `<header>` un link chico "Volver al registro con CV" que llama `onVolver()`. Todo lo demas del formulario queda igual, incluidos los comentarios de conversion (L4/L5) y el `PAGO_TEXTO`.

**2.5 `app/sumate/page.tsx` (editar).** Importar y renderizar `<SumateClient />` en vez de `<RegistroForm />`.

**2.6** De paso, sacar el guion largo del comentario de cabecera de `page.tsx` (linea 2) y de `registro-form.tsx` (linea 4): es regla dura del proyecto y el gate lo mide.
  </action>
  <verify>
    <automated>cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && npm run typecheck && npm run lint</automated>
    <automated>cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && grep -q 'modo: "rapido"' app/sumate/registro-rapido.tsx && grep -q 'modo: "completo"' app/sumate/registro-form.tsx && grep -q 'aria-live' app/sumate/registro-rapido.tsx && grep -q 'htmlFor="cv-rapido"' app/sumate/registro-rapido.tsx && grep -q 'Ley 25.326' app/sumate/registro-rapido.tsx && grep -q 'motion/react' app/sumate/registro-rapido.tsx && echo GATES_OK</automated>
    <automated>cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && test "$(cat app/sumate/registro-rapido.tsx app/sumate/sumate-client.tsx app/sumate/registro-form.tsx app/sumate/page.tsx | grep -c '—')" = "0" && echo SIN_EM_DASH</automated>
  </verify>
  <done>Con `npm run dev`, /sumate abre en el camino corto: adjunto un PDF, se lee solo, aparecen nombre y email sacados del CV, marco el consentimiento, envio, y queda registrado sin haber escrito un caracter. El link al formulario largo funciona y el largo sigue enviando igual.</done>
</task>

<task type="auto">
  <name>Task 3: en /acceso-staff la salida sube arriba, sin oraculo</name>
  <files>app/acceso-staff/staff-login-form.tsx</files>
  <action>
Solo UI y copy en este archivo. **`app/acceso-staff/actions.ts` no se toca**: la respuesta uniforme y los frenos de abuso quedan exactamente como estan.

**3.1 El bloque protagonista, arriba del formulario.** Entre el subtitulo "Portal de Staff" (linea 167) y el `motion.form`, agregar un bloque envuelto en `motion.div {...up(0.12)}` (el helper `up` ya existe en el archivo, linea 34, y usa `motion/react`). Contenido:
- Una linea de copy tipo: "Primera vez aca? Sumate al pool con tu CV. No hace falta llenar nada, solo adjuntarlo."
- Un boton/ancla `<a href="/sumate">` con el mismo tratamiento visual del boton de `InvitacionSumate` (borde, `uppercase`, `tracking`, `ArrowRight`), que se lea como accion principal alternativa.
- Debajo, un separador sutil con la palabra "o" y una linea tipo "Ya estas en el pool? Entra con tu cuenta.", que ata el bloque de arriba con el formulario de abajo.
Compacto: en 390px no puede empujar el campo de email fuera de la pantalla. Una linea de texto, un boton, el separador.

**3.2 Renderizarlo solo en las vistas de formulario.** El bloque va cuando `vista` es `"clave"` o `"link"` y `sent` es false. En `"mandado"`/`"clave-mandada"` sigue mandando `InvitacionSumate` al pie, que es el momento donde el que no tiene ficha mas lo necesita.

**3.3 Sacar el bloque duplicado del pie.** Borrar el `<div className="pt-8 border-t ...">` con "Primera vez en LABURO?" (lineas 283-296): eso es lo que sube arriba. Los otros tres links del pie (cambio de modo, Google, "Sos productor?") quedan igual.

**3.4 Actualizar el copy de `InvitacionSumate`** para que tambien diga que alcanza con el CV. Manteniendo la regla dura: **nunca** decir ni sugerir si el mail esta o no en el pool. Prohibido "no encontrado", "no estas registrado", "no figura", "ese mail no existe". El texto tiene que servir igual para el que esta en el pool y para el que no. El parrafo de resultado ("Si {email} esta registrado en nuestro pool...") no se toca.

**3.5** Actualizar el comentario de cabecera de `InvitacionSumate` (lineas 42-49) para reflejar que ahora la salida esta arriba, dejando escrito por que el mensaje sigue siendo uniforme. Sin guion largo.
  </action>
  <verify>
    <automated>cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && npm run typecheck && npm run lint</automated>
    <automated>cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && git status --porcelain app/acceso-staff/actions.ts | grep -q . && echo "FALLA: se toco actions.ts" || echo "OK actions.ts intacto"</automated>
    <automated>cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && grep -Ein 'no (esta|está|figura) registrad|mail no encontrado|no encontramos ese|no existe ese' app/acceso-staff/staff-login-form.tsx && echo "FALLA: oraculo" || echo "OK sin oraculo"</automated>
    <automated>cd "/Users/fridao/Proyectos/SOMOS DER/staff-app" && test "$(grep -c 'Primera vez en LABURO' app/acceso-staff/staff-login-form.tsx)" = "0" && test "$(grep -c '—' app/acceso-staff/staff-login-form.tsx)" = "0" && echo GATES_OK</automated>
    <human-check>
Con `npm run dev` y el navegador en 390px de ancho:
1. Entrar a /acceso-staff. El bloque "Sumate con tu CV" tiene que verse SIN scrollear, arriba del campo de email.
2. Leer el copy: no puede decir ni sugerir si un mail esta o no en el pool.
3. Tocar el boton, tiene que llevar a /sumate.
4. En /sumate, adjuntar un PDF de CV. Se lee solo, se anuncia "Leyendo tu CV", y despues aparecen nombre y email sacados del CV.
5. Marcar el consentimiento y enviar sin escribir nada. Tiene que llegar a "Quedaste registrado/a".
6. Verificar en la base que esa ficha quedo con `perfil_confirmado_at` en NULL:
   `select email, perfil_confirmado_at from staff_app.staff_profiles order by created_at desc limit 1;`
   (si la 0034 todavia no esta aplicada, la columna no existe y el chequeo es solo el log "mark_perfil_confirmado failed" que NO tiene que aparecer para el registro rapido).
7. Probar el formulario largo completo (con telefono, un oficio y ciudad): esa ficha SI tiene que quedar con `perfil_confirmado_at` estampado.
    </human-check>
  </verify>
  <done>El que llega a /acceso-staff sin ficha ve su camino sin scrollear y con el mensaje del CV; el copy sigue siendo uniforme; `app/acceso-staff/actions.ts` no aparece en el diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador anonimo → /acceso-staff | cualquiera puede pedir el link para cualquier mail, la pantalla no puede filtrar quien esta en el pool |
| navegador anonimo → /sumate + registerApplicant | payload JSON armado por el cliente, archivo subido por el cliente |
| /sumate → /api/parse-cv → Gemini | ruta publica que gasta cuota compartida con somosder-web |
| salida de Gemini → RPC staff_app_register_applicant | texto generado por un modelo que termina en columnas de la base |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Q8A-01 | Information disclosure | copy de `staff-login-form.tsx` | mitigate | El bloque nuevo no hace ninguna consulta y su texto es identico para el que esta y el que no esta en el pool. Gate automatico que falla si aparece "no esta registrado" / "mail no encontrado" y otro que falla si `app/acceso-staff/actions.ts` aparece en el diff |
| T-Q8A-02 | Tampering | `modo` en el payload | mitigate | Valor desconocido o ausente cae en "rapido" (fail hacia mandar el recordatorio, nunca hacia abandonar la ficha). El peor caso de un spoof es que alguien se ahorre un mail de recordatorio |
| T-Q8A-03 | Tampering | `oficios` salidos de Gemini | mitigate | Filtrado contra `OFICIOS_VALIDOS` (catalogo de `lib/data/oficios`) antes de `p_oficios`. El RPC ya fuerza `organization_id`, `estado` y `source` |
| T-Q8A-04 | Denial of service | `registerApplicant` con payload parcial | mitigate | Helpers `txt()`/`arr()`: ninguna clave ausente puede tirar un TypeError en el server action |
| T-Q8A-05 | Denial of service | `/api/parse-cv` con autolectura al adjuntar | accept | Se mantiene el freno existente (6 por minuto y 30 por hora por IP, `rateLimitOr429`) y se dispara una sola llamada por seleccion de archivo, sin reintento automatico. La superficie no crece respecto de hoy |
| T-Q8A-06 | Elevation of privilege | subida del CV | accept | Camino ya resuelto y sin cambios: bucket privado `staff-cvs` con service-role, MIME real por magic bytes (`sniffCvMime`), cap de 10MB, borrado del huerfano si el registro falla |
| T-Q8A-SC | Tampering | npm installs | mitigate | Cero dependencias nuevas en este plan (regla dura de presupuesto). Si la ejecucion cree necesitar un paquete, para y pregunta antes de instalar |
</threat_model>

<verification>
1. `npm run typecheck` limpio.
2. `npm run lint` limpio.
3. `git status --porcelain app/acceso-staff/actions.ts` vacio.
4. Cero guiones largos en los seis archivos tocados.
5. `grep -q 'if (profileId && perfilCompleto)' app/sumate/actions.ts`.
6. Recorrido manual del human-check del Task 3 en 390px.
</verification>

<success_criteria>
- Se puede terminar el registro en /sumate adjuntando solo el CV, sin escribir nada, cuando el CV trae nombre y mail.
- Si el CV no trae nombre o mail, se pide solo eso, nada mas.
- El consentimiento Ley 25.326 sigue siendo obligatorio y validado en el servidor.
- La ficha creada por la via corta queda con `perfil_confirmado_at` en NULL y entra al alcance del recordatorio de la 0034.
- La ficha creada por el formulario largo con telefono, oficio y ubicacion sigue quedando confirmada.
- En /acceso-staff la salida a /sumate se ve sin scrollear en 390px y menciona que alcanza con el CV.
- El copy de /acceso-staff no confirma ni desmiente que un mail este en el pool, y `app/acceso-staff/actions.ts` quedo intacto.
- Cero dependencias nuevas, cero migraciones nuevas.
</success_criteria>

<output>
Crear `.planning/quick/260731-q8a-registro-solo-con-el-cv-y-la-salida-visi/260731-q8a-SUMMARY.md` al terminar, y actualizar la tabla "Quick Tasks Completed" de `.planning/STATE.md`.
</output>
