/**
 * Crear evento (productor). Monta el GigForm en modo creación. El layout del
 * portal ya aporta el sidebar + <main>.
 */

import { GigForm } from "../gig-form";

export default function NuevoEventoPage() {
  return <GigForm />;
}
